# 아이템 한줄 설명(플레이버 텍스트) 시스템 — 전수조사 + 구축 계획

> **상태 (2026-07-27): Phase 1~3 완료.** 669종 전부 집필, 린트 전항목 통과(경고 0), 빌드 통과.
> 코드는 blockship-plugin `ed8a297`에 포함. 남은 것 = **dev 인게임 육안 확인 → prod 배포**(별도 요청 시).
> 권위 데이터 = `plugins/BlockShip/item-flavor.json` (이 저장소 루트 사본은 git 백업용).

작성 2026-07-27. 목표 = 물고기·낚싯대·부품·채집물·작물·요리·재료·통발 **모든 콘텐츠 아이템에 세계관 한줄 설명**을 붙인다.
설명은 기능 설명(`[획득]`/`[사용]`/스탯)과 **별개 레이어** — 바르칸 열도라는 장소가 실재하는 것처럼 느끼게 하는 게 목적.

---

## 1. 전수조사 — 대상 아이템 총량

| 카테고리 | 종수 | 권위 데이터 | 현재 설명 | 아이템 빌드 지점 |
|---|---:|---|---|---|
| **물고기** | **538** | `BlockShip/fish.json` `fish` | ✗ 없음 | `fishing/FishingListener.java:994` `buildFishItem` |
| ↳ 지역 배정 | 424 | `fish.json` `regions` | | |
| ↳ 환경 전용 | 17 | `fish.json` `environment` (오로라/새벽/밤/비/뇌우/땡볕/모래바람/유성우) | | |
| ↳ **미배정(고아)** | **97** | 어디에도 없음 — 도감 「미분류 물고기」 탭에 노출 중 | | |
| ↳ 퀘스트 오브젝트 | 15 중 4~5 | 물고기 아님(교단의인장·녹슨열쇠·밀랍봉인문서·상단화물꼬리표·별똥별 조각) | | |
| **부품** | **86** | `BlockShip/parts.json` | ✗ 없음 | `parts/EquipmentManager.java:246` `buildPartDisplay` |
| ↳ 낚싯대 22 / 릴 12 / 줄 14 / 바늘 14 / 미끼 13 / 찌 11 | | 포맷 `이름\|등급\|가격\|내구\|스탯\|레벨제한\|출처` (설명 필드 없음) | | |
| **조합 재료** | **43** | `BlockShip/materials.json` `desc` | ✓ **있음**(기능 위주) | `crafting/CraftingManager.java:507` |
| ↳ 레시피 전용 재료 | 4 | `recipes.json` 직접 lore (강화사과·강화사탕수수·강화코코아·강화호박) | △ 1줄 있음 | `recipes.json` `result.lore[0]` |
| **채집물** | **31** | `pixel-forge/manifest.json` → `forage-types.json` + CE `forage_custom.yml` | ✗ 없음 | `pixel-forge/build.py:104` (CE lore 생성) |
| **특수작물** | **7** | `crop/CropSpecs.java:62-68` | ✗ 없음 | `CropSpecs.seedItem:89` / `outputItem:104` |
| **요리** | **47** | `cooking/DishSpecs.java` | △ 버프 수치만 | `DishSpecs.buildDish:367` |
| **통발** | **12** | `trap/TrapSpecs.java` | △ 지역·내구만 | `TrapSpecs.item:125` |
| **합계(신규 집필 필요)** | **≈ 720줄** | | | |

> 제외: 캐시샵 소모품·스크롤·워프주문서(기능 설명이 곧 전부), 건축블록 상점(바닐라), 드릴(기능 아이템 2종 — 선택).

### 노출 화면(설명이 보여야 하는 곳)
- 물고기: 인벤 아이템 lore / 채팅 낚음 알림 hover(`FishingListener.fishNameWithHover`) / 도감 물고기탭(`dex/MainDexGui.java:622 renderFish`) / 판매·아이스박스·마켓
- 부품: 장비 작업대(`WorkbenchGui:77~`) / 부품상점(`PartShopGui:258~`) / 조합대 / 도감 부품·낚싯대탭(`MainDexGui:313,356`) / 분해·수리
- 재료: 아이템 lore / 도감 재료탭(`MainDexGui:404`)
- 채집물·작물·요리·통발: 각 아이템 lore + 조합/요리 GUI 미리보기

---

## 2. 조사 중 발견한 문제 (설명 작업과 함께 처리해야 함)

1. **물고기 아이템 빌더가 2개로 갈라져 있다.**
   `FishingListener.buildFishItem:994`(정식) vs `trap/TrapManager.java:477 makeFish`(통발) — 통발 물고기는 `[물고기]` 태그·`[획득]/[사용]`·**커스텀 모델(item_model)이 전부 빠져 있다.** 설명 줄을 두 곳에 중복 추가하지 말고 **공용 빌더로 통합**하면서 이 결손도 같이 고친다. (`WetTreasureChestManager:131`도 별도 lore를 쌓지만 상자 아이템이라 별개)
2. **고아 어종 97종.** 도감 「미분류 물고기」 탭에 뜨지만 어디서도 잡을 수 없다. 동굴 계열 ~20종, 양식/상단 계열, 정상·늪 계열 등. 설명을 쓰기 전에 **배치할지 삭제할지 결정**해야 한다(안 그러면 잡을 수 없는 물고기 97개 분량을 헛집필).
3. **부품 데이터 포맷에 설명 필드가 없다.** `이름|등급|...|출처` 7필드 고정 — 8번째 필드를 늘리면 기존 파서·에디터가 전부 영향을 받는다. → 중앙 레지스트리 방식이 안전(3장).
4. **재료 43종의 기존 `desc`는 기능 설명**("낚싯대 조합에 사용되는 오래된 갈고리"). `[사용]` 줄과 의미가 겹친다 → 세계관 설명으로 **교체**할지, 기능줄은 두고 위에 플레이버를 **추가**할지 결정 필요(권장: 교체. `[획득]/[사용]`이 이미 기능을 담당).
5. lore 파싱은 전부 **내용 기반**(`contains("§8f:")`, `mat:`, `dish:`)이고 인덱스 고정이 아니다 → **설명 줄을 끼워 넣어도 판매/신선도/조합 파싱은 안 깨진다.** (확인 완료: `economy/FishItem.java`, `CraftingManager`, `DishSpecs.dishIdOf`)

---

## 3. 설계

### 3-1. 단일 레지스트리 `BlockShip/item-flavor.json`
카테고리별로 흩어진 데이터 파일을 각각 고치는 대신, **설명만 모은 파일 하나**를 만든다.

```json
{
  "물고기": {
    "붕어": "바르칸 항구에서 가장 흔하게 잡히는 물고기. 이 도시 식탁의 절반은 붕어다.",
    "바르칸의심연": "..."
  },
  "부품": { "나뭇가지": "...", "녹슨 릴": "..." },
  "재료": { "낡은갈고리": "..." },
  "채집": { "흰들국화": "..." },
  "작물": { "밀": "..." },
  "요리": { "들꽃꿀차": "..." },
  "통발": { "강": "..." }
}
```

**이유**
- 부품 파이프 구분 포맷을 안 건드린다 (문제 3 회피).
- 집필·검수·번역이 파일 하나에서 끝난다. 리뷰 diff가 깨끗하다.
- `/데이터리로드`(`BlockShipPlugin.java:1786`)에 얹으면 **서버 재시작 없이** 문구 수정 반영.
- `pixel-forge/build.py`가 같은 파일을 읽어 채집물 CE lore를 생성 → **채집물도 같은 단일 소스**(CE가 lore를 소유하지만 원문은 여기 하나).

### 3-2. 헬퍼 `util/ItemFlavor.java`
```java
ItemFlavor.lines("물고기", "붕어")   // → List<String>, 없으면 빈 리스트
```
- 로드/리로드는 `FlavorLoader`(다른 Loader와 동일 패턴, 파일 없으면 경고 후 무동작 — 설명 없어도 서버는 정상).
- **자동 줄바꿈**: 한 줄 원문을 어절 단위로 **28자 기준 최대 2줄**로 접는다. 툴팁이 화면 밖으로 나가는 걸 방지하고, 집필자는 줄바꿈을 신경 쓰지 않는다.
- 색: `§7` 회색, **이탤릭 해제**(전 프로젝트 관행), **볼드 금지**(전역 훅).

### 3-3. lore 삽입 위치 — 카테고리 태그 바로 아래, 빈 줄로 분리
```
붕어                          ← 이름
§8[물고기]                    ← 기존 카테고리 태그
§7바르칸 항구에서 가장 흔하게    ← 신규 설명 (최대 2줄)
§7잡히는 물고기. 이 도시 식탁의…
                              ← 빈 줄
§7등급 : E                    ← 기존 스탯 블록
§7크기 : 12.4cm
...
§8[획득] §7낚시 — 바르칸 항구
§8[사용] §7판매, 요리, 제출
§8f:1753...
```
설명을 맨 위에 두는 이유: 유저가 아이템을 처음 볼 때 **"이게 뭔지"가 숫자보다 먼저** 읽혀야 한다. 기능 정보(`[획득]/[사용]`)는 지금처럼 맨 아래 유지.

### 3-4. 적용 지점 (총 12곳)
| # | 파일 | 처리 |
|---|---|---|
| 1 | `fishing/FishingListener.java:994` + `trap/TrapManager.java:477` | **공용 `FishItemFactory`로 통합** 후 설명 1회 삽입 (문제 1 동시 해결) |
| 2 | `fishing/FishingListener.java:1061` `fishNameWithHover` | 채팅 hover에도 설명 추가 |
| 3 | `dex/MainDexGui.java:622` `renderFish` | 발견한 어종만 설명 노출(미발견은 `???` 유지) |
| 4 | `parts/EquipmentManager.java:246` `buildPartDisplay` | 부품 설명 |
| 5 | `economy/PartShopGui.java:258` | 상점 미리보기 |
| 6 | `economy/WorkbenchGui.java:77` | 장비 작업대 |
| 7 | `dex/MainDexGui.java:313,356` | 낚싯대·부품 도감탭 |
| 8 | `crafting/CraftingManager.java:507` | 재료 `desc` → `ItemFlavor` 치환 |
| 9 | `dex/MainDexGui.java:404` | 재료 도감탭 |
| 10 | `crop/CropSpecs.java:89,104` | 씨앗·수확물 |
| 11 | `cooking/DishSpecs.java` `loreLines` | 요리 |
| 12 | `trap/TrapSpecs.java:125` + `pixel-forge/build.py:104` | 통발 / 채집물 CE lore |

---

## 4. 문체 가이드 (집필 규칙)

### 기본형
> `<무엇인지 / 어디서 나는지> + <바르칸 세계에서 어떤 취급을 받는지>`

- **1~2문장, 원문 60자 이내.** 문장 2개면 두 번째는 짧게.
- 종결은 `~다 / ~이다`체. 존댓말·설명서 말투 금지.
- **지명·세력은 기존 것만** 쓴다. **없는 지명을 새로 만들지 않는다.**
  - 지역 ID의 권위는 `plugins/BlockShip/regions.json`, 세력·서사 지명의 권위는 [story.md](story.md)다.
  - 사용 가능: 바르칸 본섬, 스폰도시, 영주성, 항구, 강, 강_상류, 정상, 늪지대, 폭포, 왕도, 기억의 연못,
    사막마을, 오아시스, 붉은사막, 레드 로드, 광산, 탄광, 깊은 숲, 대수림, 상단마을, 상단기함 /
    낚시사 길드, 바르칸 왕실, 바르칸 영주가, 왕국 상선단, 심해 교단
  - ⚠ `대양` `물보라 동굴` `깊은 호수` `붉은 호수` 는 **regions.json에 미등록**이다(bgm.json·일부 퀘스트만 참조).
    플레이버에 쓰면 유저가 갈 수 없는 곳을 가리키게 되니 지역이 실제로 생길 때까지 쓰지 말 것.
- 스탯·확률·수치 언급 금지(밸런스 변경 시 즉시 거짓말이 된다).

### 유머 — 전체의 **15~20%만**, 나머지는 담백하게
유저 제시 기준선:
- 독그늘버섯 → `둘이 먹고 둘이 죽는다는 독버섯. 무슨 용도가 있을까...?`
- 야광버섯 → `알 수 없는 이유로 빛나는 버섯. 먹으면 나도 빛날까?`
- 가시배 선인장 → `최상의 맛을 가진다는 선인장. 따다가 다치지 않게 조심하자.`

**통하는 유머** = ① 실용적 걱정("조심하자", "무슨 용도가 있을까") ② 소소한 현지 사정("어부들이 제일 싫어한다") ③ 담백한 체념. 웃기려고 힘주지 않고 **정보를 주다가 슬쩍 흘리는 톤**.

### 금지 — "AI티" 체크리스트
| 금지 | 나쁜 예 |
|---|---|
| 감탄사·이모지·느낌표 남발 | `놀랍게도 빛난다!!` |
| 자의식 개그 / 4th wall | `개발자도 왜 넣었는지 모른다` |
| 관용구 반복 | `~라고 전해진다`, `~로 유명하다`가 여러 항목에 연속 |
| 과한 형용사 | `신비롭고 아름다운 전설의 영롱한` |
| 수사적 질문 남발 | 20% 유머 중에서도 `~일까?`는 **최대 1/3** |
| 물음표 3개 이상, 말줄임표 남발 | `과연...?!` |
| 밈·현대어·인터넷 슬랭 | `가성비 갑`, `혜자` |
| 같은 문장 구조 연속 | 인접 5개 항목이 같은 골격이면 리라이트 |

### 등급별 톤 차등 (물고기)
- **E~D**: 일상적·생활밀착. 시장 가격, 누가 먹는지, 어부의 잡담. 유머 비중 높음.
- **C~B**: 지역 특성·습성 중심. 담백.
- **A~S**: 희소성·전문가의 언급. 유머 거의 없음.
- **M·L·G(신화/전설)**: 완전 진지. 전승·교단·심연어 세계관과 연결. **유머 0%.**

---

## 5. 작업 단계 (리뷰 게이트 포함)

**Phase 0 — 결정 (2026-07-27 확정)**
- ✅ **고아 어종 97종 = 집필 제외·보류.** 집필 대상 물고기 = **441종**(지역 424 + 환경전용 17). 배치/삭제는 별도 작업.
- ✅ **재료 43종 기존 `desc` = 세계관 설명으로 교체.** `[획득]/[사용]`이 기능을 담당하므로 중복 제거.
- ✅ **설명 위치 = lore 맨 위**(카테고리 태그 바로 아래, 스탯 블록 위).
- 퀘스트 오브젝트(인장·열쇠·문서류): 물고기 톤 아님 → 별도 톤
- 총 집필량 **538→441**로 감소 → **≈ 620줄**

**Phase 1 — 배관 (코드, 집필 전)**
- `item-flavor.json` 스키마 + `FlavorLoader`/`ItemFlavor` + 28자 자동 줄바꿈
- 물고기 빌더 통합(`FishingListener`↔`TrapManager`) + 통발 물고기 모델/태그 결손 수정
- 12개 지점 삽입 + `/데이터리로드` 연결
- **빈 JSON으로 dev 배포해 회귀 없음 확인** (설명 없으면 기존과 100% 동일해야 함)

**Phase 2 — 샘플 40종 집필 → 리뷰 게이트 ★**
- 물고기 20(E~G 등급 고루) + 부품 8 + 채집 6 + 재료 3 + 요리 2 + 통발 1
- dev 서버에서 실제 툴팁 스크린샷으로 확인(줄바꿈·길이·색)
- **유저 승인 전까지 대량 집필 착수 금지.** 톤이 어긋난 채 700줄을 쓰면 전량 폐기다.

**Phase 3 — 본 집필 (배치별, 각 배치 후 짧은 확인)**
1. 채집 31 + 작물 7 (유저 예시가 여기라 기준점이 가장 명확)
2. 재료 43 + 요리 47 + 통발 12
3. 부품 86 (낚싯대 22부터 — 강화·상점에서 가장 자주 보이는 아이템)
4. 물고기 — 지역 단위로 쪼갠다: 강/강_상류 → 스폰도시·항구 → 늪지대 → 정상 → 붉은사막·오아시스 → 대양·원양어선 → 동굴·호수 → 환경 전용 17 → 신화/전설(M·L·G 47종은 마지막, 스토리 정합성 가장 중요)

**Phase 4 — 검수·배포**
- 린트 스크립트(6장) 전항목 통과
- dev 전수 스크린샷 샘플링 → prod 배포는 별도 요청 시(`~/stage-blockship.sh` 스테이징 권장)

---

## 6. 검증 — `fish-tools/lint_flavor.py` (신규)
집필 품질을 사람 눈에만 맡기지 않는다.
- **커버리지**: 카테고리별 대상 목록 대비 누락/오타 키(존재하지 않는 아이템명) 리포트
- **길이**: 원문 60자 초과, 줄바꿈 후 3줄 되는 항목
- **중복**: 동일 문장·80% 이상 유사 문장 쌍 (색칠놀이 방지)
- **관용구 편중**: `~라고 전해진다` 류 어미 빈도 상위 N개 — 한 표현이 전체의 5% 넘으면 실패
- **유머 비율**: 물음표·감탄부호 포함 항목 비율이 25% 초과면 경고
- **금칙어**: 이모지, 밈, 스탯 수치, story.md에 없는 지명
- **등급 톤**: M/L/G 등급 항목에 물음표·감탄부호 있으면 실패

---

## 7. 미결정 (소소한 것 — 기본값으로 진행)
1. 캐시샵 소모품·스크롤·워프주문서 — **제외**(기능 설명이 곧 전부)
2. 드릴·광질모자 등 도구류 — **제외**, 필요하면 나중에 추가
3. 고아 어종 97종의 배치/삭제 — 이번 작업 범위 밖, 별도 처리
