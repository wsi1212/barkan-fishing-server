# 퀘스트 전면 개편 — 설계 (2026-08-07)

서사는 [story.md](story.md)가 권위. 이 문서는 **목표(goal) 설계·난이도 곡선·수여 NPC 배치**만 다룬다.
수치의 최종 권위는 `plugins/BlockShip/quests.json` (prod).

> **구현 반영 (2026-08-07)** — prod에서 전면 반영 완료. 최신 레벨 게이트는
> 튜토 1~5 · 2챕터 1~15 · 3챕터 15~28 · 4챕터 28~32 · 5챕터 32~40 · 6챕터 40~50이다.
> 이 문서의 구 난이도 표는 설계 기록이며, 레벨·경험치 수치는 `codex-handoff.md`와
> `2026-08-07-level-pacing.md`의 확정치를 따른다.

## 개편 이유 — 측정된 문제 3개

**① 난이도 역행 12군데.** `본섬10b`가 A등급을 요구한 뒤 3챕터가 D등급 1마리로 시작하고,
5챕터도 C등급 5마리로 시작한다. 중반 전체가 평평하다.

**② 목표가 단조롭다.** 목표 문법이 23종인데 메인 라인은 `fish` 하나로 149회를 쓴다.
`fish_fresh`(신선도) · `equip`(장착) · `deliver`(납품)은 **사용 0회**.

**③ 5챕터 무대에 물고기가 없다.**

| 무대 | 어종 | 등급 분포 |
|---|---|---|
| 2챕터 (강·강_상류·정상·스폰도시) | 89종 | E23 D22 C13 B11 A12 S7 M1 |
| 2챕터 늪지대 | 46종 | E1 C8 B17 A9 S7 M2 L1 G1 |
| 3챕터 (오아시스·붉은사막) | 61종 | E14 D11 C10 B11 A7 S5 |
| **5챕터 (상단마을)** | **9종** | **E9 — 전부 E등급** |
| 폭포_뒤_동굴_1층 | 26종 | E3 C4 B9 A8 S2 |

`배상단07`(A등급 2)·`배상단08`(S등급 1)을 상단 해역에서 채울 수 없다.
→ **결정: 상단 해역 어종 20~30종(E~S) 신설.** 5챕터가 자기 어장을 갖는다.

## 제약 — 베타 등급 천장은 S

레벨 마일스톤(balance.md §3.3)이 최대 등급을 제한한다.

| 레벨 | 최대 등급 |
|---|---|
| 1–29 | **S** |
| 30–39 | M |
| 40–49 | L |
| 50+ | G |

베타는 Lv25에서 끝나므로 **등급만으로는 5챕터에서 더 올릴 수 없다.**
곡선은 **등급 × 수량 × 크기 × 신선도 × 복합**의 곱으로 세운다.

장비 천장도 A다 (balance.md §7 — 상점은 A까지, S는 히든). A장비로 S를 노리는 구도.

---

## 목표 문법 팔레트 (23종 · 실측)

`QuestManager.checkProgress` 기준. **★목록에 없는 verb를 쓰면 `target = 0`이 되어 퀘스트가 즉시 완료된다.**

| verb | 문법 | 목표치 위치 | 비고 |
|---|---|---|---|
| `fish` | `fish\|어종\|등급\|수\|최소크기` | 4번째 | 어종 `아무` 가능 |
| `fish_fresh` | `fish_fresh\|어종\|등급\|수\|최소크기\|최소신선도` | 4번째 | **미사용** — 신선도 축 |
| `area` | `area\|지역` | (1) | 지역 진입. 도착=자동완료 |
| `visit` | `visit\|npc키\|표시명` | (1) | NPC 대화 |
| `action` | `action\|메뉴\|퀘스트` | (1) | GUI 열기 |
| `dogam` | `dogam\|지역,지역\|종수` | 3번째 | 지역 도감 |
| `sell` | `sell\|횟수` | 2번째 | 판매 |
| `money` | `money\|금액` | 2번째 | 소지금 |
| `earn` | `earn\|금액` | 2번째 | ⚠️ **주간 카운터** 기반 — 메인에 쓰면 주간 리셋 영향 |
| `login` `quest_daily` | `<verb>\|수` | 2번째 | ⚠️ 동일하게 주간 카운터 |
| `craft` | `craft\|아이템\|수` | 3번째 | 제작 이벤트 카운트 |
| `deliver` | `deliver\|부품명\|수` | 3번째 | **미사용** — 인벤 즉석 판정, **무손상 부품만**, 완료 시 회수 |
| `material` | `material\|재료\|수` | 3번째 | 재료 획득 |
| `forage` | `forage\|노드id\|수` | 3번째 | 채집 |
| `eatdish` | `eatdish\|요리\|수` | 3번째 | 요리 먹기 |
| `equip` | `equip\|타입\|이름` | 3번째 | **미사용** — 장착 확인 (비소모) |
| `trap` `enhance` `skill` `collectible` | `<verb>\|수` | 2번째 | 통발/강화/특성/수집품 |
| `casino` | `casino\|키\|수` | 3번째 | 카지노 |
| `level` | `level\|레벨` | 2번째 | 레벨 게이트 |
| `harpoon` ★신설 | `harpoon\|어종\|등급\|수\|최소크기` | 4번째 | **작살 전용** — `fish`와 달리 작살로만 집계 |
| `mine` ★신설 | `mine\|블록id\|수` | 3번째 | 드릴 채굴 |
| `harvest` ★신설 | `harvest\|작물id\|수` | 3번째 | 특수작물 수확 |
| `sail` ★신설 | `sail\|블록거리` | 2번째 | 배 항해 |
| `submit` ★신설 | `submit\|횟수` | 2번째 | 제출소 제출 |

### 등급 표기 — 범위가 지원된다 (미활용)

`gradeMatch`는 `~`를 파싱한다.

| 표기 | 의미 |
|---|---|
| `B` | **B 이상** (reqMax=10). 현재 모든 퀘스트가 이 형태 |
| `C~B` | **정확히 C~B 구간** — A 이상은 카운트 안 됨 |
| `아무` | 전부 |

`C~B`처럼 상한을 걸면 "고등급으로 뭉개기"를 막고 **그 구간을 실제로 낚게** 만든다. 초중반에 유용.

---

## Phase 0 — 비낚시 활동 훅 5종 신설 (2026-08-07 · prod 배포 완료)

**문제의 근본 원인**: 등급·크기·신선도·도감은 전부 *낚시의 파라미터*다. 축을 늘려도 활동은 하나뿐이었다.
낚시 외 활동을 목표로 쓰려면 **플러그인에 훅이 있어야** 한다 — 없으면 설계상 `fish` 변형밖에 안 나온다.

| verb | 문법 | 훅 위치 | 활동 |
|---|---|---|---|
| `harpoon` | `harpoon\|어종\|등급\|수\|최소크기` | `HarpoonListener` (onFish 옆) | **작살로만** 낚기 |
| `mine` | `mine\|블록id\|수` | `DrillManager.complete()` | 드릴 채굴 (블록id = `iron_ore` 형태) |
| `harvest` | `harvest\|작물id\|수` | `CropManager` 수확 | 특수작물 재배·수확 |
| `sail` | `sail\|블록거리` | `ShipMover.move()` | 배 항해 (이동 블록 누적) |
| `submit` | `submit\|횟수` | `IslandSubmitManager.onClick()` | 제출소 제출 |

설계 규약:
- **`fish`는 낚싯대·작살 둘 다 집계**되고, **`harpoon`은 작살로만** 올라간다.
  → "그냥 낚으세요"는 `fish`, "작살로만 낚으세요"는 `harpoon`.
- `mine`은 연쇄 채굴로 같이 부수는 옆 광석을 세지 않는다 (주 채굴 1칸 = 1카운트).
- `harvest`는 풍년 수량 배수를 세지 않는다 (한 포기 = 1카운트).
- 4종은 `BlockShipPlugin`에서 `setQuestManager(questManager)`로 배선. 미주입 시 조용히 스킵.

## 발견한 버그

### ① `튜토_작살2`는 작살을 안 써도 깨진다

목표가 `fish|아무||1|0` — 등급 필드가 **빈 문자열**이다. `gradeMatch("")` → `gradeNum("")`이 0을 반환해
`got >= 0`이 항상 참 → **아무 물고기나 1마리**면 완료된다.

→ **수정안: `harpoon|아무|아무|1|0`으로 교체.** Phase 0에서 verb가 생겼으니 이제 진짜 작살 전용이 된다.

> ⚠️ 정정: 초기 조사에서 "작살은 퀘스트에 집계되지 않는다"고 적었으나 **틀렸다.**
> `HarpoonManager.java`만 grep해서 놓쳤고, 실제 호출은 `HarpoonListener.java:270`의 `mgr.quests().onFish(...)`다.
> 작살은 원래부터 `fish` 목표에 집계됐다. 없었던 건 "작살 전용"을 표현할 방법이었고, 그게 `harpoon` verb다.

### ② 오타 난 verb는 조용히 즉시 완료된다

`checkProgress`의 `default -> target = 0`. 모르는 verb는 목표치 0 → 즉시 충족.
새 퀘스트를 넣을 때 **verb 스펠링 검증이 필수**다. 아래 검증 스니펫을 쓸 것.

```python
OK={'fish','fish_fresh','area','visit','action','dogam','sell','money','earn','login',
    'quest_daily','craft','deliver','material','forage','eatdish','equip','trap',
    'enhance','skill','collectible','casino','level'}
# quests.json 전체 목표의 verb가 OK에 있는지 확인
```

---

## 다양성 규칙 (하드 룰)

> **"N등급 N마리"만 반복하지 않는다.**

1. 한 챕터에서 **같은 verb를 3연속 금지.**
2. `fish` 단순형(어종=`아무` + 최소크기=0 + 단일 목표)은 **챕터당 최대 2회.**
3. 챕터마다 **그 챕터에서 처음 등장하는 축**을 최소 1개 넣는다 (신선도·크기·범위등급·장착·납품·도감·채집…).
4. 졸업 게이트는 항상 **복합(AND) 3중 이상** — 낚시 + 레벨 + 도감 축을 섞는다.
5. 서사 비트가 있는 퀘스트는 **그 비트에 맞는 verb**를 쓴다 (폐사당 조사 = `area`+`dogam`, 수비대 식량 = `fish_fresh`, 등록 = `equip`).

## 난이도 곡선 — 「중간」 채택

유저 결정: **메인은 적당하게, 진짜 깊이는 사이드로.**
메인이 막혀서 짜증나는 상황을 피하고, 폭포 뒤 동굴(1~6층, Lv30~80)·하든·사이드에 높은 목표를 둔다.

| 축 | 1챕터 | 2챕터 | 3챕터 | 4챕터 | 5챕터 |
|---|---|---|---|---|---|
| 최고 등급 | B | A | S | S | S |
| 그 등급 수량 | 1 | 1 | 2 | 3 | 3 |
| 최소크기(대물) | — | 25 → 50 | 40 → 60 | 60 | 70 |
| 최소신선도 | — | 80 | 85 | — | 90 |
| 도감 | 5 | 22/89 | 40/61 | — | 상단 어장 |
| 복합(AND) 최대 | 1 | 4중 | 3중 | 3중 | 3중 |

졸업 게이트 계단:

| 게이트 | 현재 | 개편안 |
|---|---|---|
| `튜토11` 1챕터 | B등급 1 | 유지 |
| `본섬10b` 2챕터 | A등급1 + Lv12 + 도감22 | **A등급1(50cm) + Lv12 + 도감22 + 강화3** |
| `사막12b` 3챕터 | A등급1 + Lv20 + 도감32 | **S등급1 + 60cm 대물1 + Lv20 + 도감40** |
| `배상단08` 베타 종점 | S등급 1 | **S등급3 + 70cm 대물1 + Lv25** |

---

## 권위 계단 — 높은 사람은 마지막에

각 무대의 최고 권위자는 그 라인의 **마지막 퀘스트에서 처음 만난다.**

| 무대 | 최고 권위 | 첫 대면 시점 | 그 앞을 채우는 사람 |
|---|---|---|---|
| 스폰마을 길드 | **길드장 하겐** | `본섬10b` (2챕터 졸업) | 요한(접수원) → 어부 노인 → 세르간 |
| 스폰마을 영주성 | **영주 발데마르** | `영주05` (2챕터 끝) | 근위병 라이너 → 사관 게르하르트 |
| 왕도 | **국왕** | `왕도05` (4챕터 끝) | 성문 위병 로타르 → 대사서 → 위병 쿠르트 |
| 사막마을 (길드) | **지부장 유세프** | `사막12b` (3챕터 졸업 게이트) | 유누스·파티마 → 사피르·카림·할릴 |
| 사막마을 (마을) | **촌장 나디아** | `사막13~14` (조각) | 사막 노파 → 레일라 |
| 상단 | **선장 이자벨라** | `배상단07` | 마테오 → 엔리코 → 피노 → 조반니 |

### 같이 손봐야 하는 것

- **하겐 사이드 3개의 선행이 `본섬01`** → `본섬10b`로 올린다. 안 하면 첫 퀘스트만 깨고 하겐과 대화 가능
- **하겐 위치** 마을 중심(393,77,859) → 길드 회관 안쪽
- **위병1·2 이름표** `&f` → `&a[Q]` (퀘스트 주는 NPC는 초록 `[Q]`)
- **`사막02` 이름이 "촌장 나디아"** → 나디아가 뒤로 가므로 개명
- **`왕도02` "국왕 알현"** → "성문의 검문"(위병) / **`왕도04`가 국왕에게 배정됨** → 위병으로 이동
- **`왕도05`** → 국왕 첫 대면 + 첫 왕명

---

## 2챕터 재설계 (Lv5–12 · 16개)

### 길드 라인 — 요한 4 → 노인 2 → 세르간 5 → 하겐 1

| ID | 이름 | 목표(개편안) | 수여 | 새 축 |
|---|---|---|---|---|
| `본섬01` | 길드 등록 | `equip\|낚싯대\|초보_낚싯대` + `fish\|붕어\|아무\|3\|0` | 요한 | **장착** |
| `본섬02` | 바르칸 물길 | `area\|강` + `fish\|아무\|C\|2\|0` | 요한 | 지역+등급 복합 |
| `본섬03` | 절벽 아래 동굴 | `area\|폭포_뒤_동굴_1층` + `fish\|아무\|D~C\|4\|0` | 어부 노인 | **범위 등급** |
| `본섬04` | 폭포 뒤에 뭐가 있을까 | `fish\|아무\|아무\|8\|25` | 어부 노인 | **최소크기** |
| `본섬05` | 해안가의 노인 | `fish_fresh\|아무\|B\|1\|0\|80` | 세르간 | **신선도** |
| `본섬06` | 잠든 자의 등 | `area\|늪지대` + `dogam\|늪지대\|6` | 세르간 | 도감 |
| `본섬07` | 교단의 물고기 | `fish\|검은비늘붕어\|아무\|1\|0` + `material\|아무\|8` | 세르간 | 재료 |
| `본섬08` | 바르칸을 읽어라 | `dogam\|강,강_상류,스폰도시\|18` + `sell\|12` | 요한 | 도감+판매 |
| `본섬09` | 비 오는 밤의 전설 | `fish\|바르칸의심연\|아무\|1\|0` | 세르간 | 조건부 어종 |
| `본섬10` | 심연어의 조각 | `area\|정상` + `dogam\|정상\|8` | 세르간 | — |
| `본섬10b` | 바르칸의 자격 ★졸업 | `fish\|아무\|A\|1\|50` + `level\|12` + `dogam\|강,강_상류,정상,스폰도시\|22` + `enhance\|3` | **하겐 (첫 대면)** | 4중 복합 |

`본섬08`의 50마리(단순 반복)를 **도감 18종 + 판매 12회**로 교체 — 같은 시간이 들지만 하는 일이 다르다.

### 영주성 라인 — 라이너 2 → 게르하르트 2 → 발데마르 1

| ID | 이름 | 목표(안) | 수여 | 비트 |
|---|---|---|---|---|
| `영주01` | 닫힌 성문 | `visit\|근위병` | 라이너 | 신참은 못 들어간다. 하겐의 소개로 통과 |
| `영주02` | 수비대의 저녁 | `fish_fresh\|아무\|C\|6\|0\|70` | 라이너 | 성문 값 = 신선한 것만 |
| `영주03` | 서고로 | `visit\|사관` | 게르하르트 | 사관이 안으로 들여보낸다 |
| `영주04` | 사관의 청 | `fish\|아무\|아무\|1\|55` + `dogam\|늪지대,정상\|14` | 게르하르트 | 기록용 대물 + 옛 봉인 기록 사본 발견 (6챕터 복선) |
| `영주05` | 영주 알현 | `visit\|영주` (+칭호) | **발데마르 (첫 대면)** | 동쪽 사막을 맡긴다 → `본섬11` |

---

## 3챕터 재설계 (Lv12–20 · 22개) — 방향

권위 계단: 유누스·파티마(도착) → 사피르·카림·할릴(실무) → 노파·레일라(정보) → 마르코(상단)
→ **유세프(졸업 게이트)** → **나디아(조각)**

역행 제거 원칙: **3챕터의 최저 요구는 2챕터의 최고(A)보다 낮아도 되지만, 단순 `fish`가 아니어야 한다.**
낮은 등급을 쓸 때는 반드시 크기·신선도·범위·수량 중 하나를 얹는다.

| 구간 | 개편 방향 | 주요 축 |
|---|---|---|
| `본섬11`·`본섬12` 이동 | `area` 유지 | — |
| `사막01~03` 도착·적응 | `fish\|아무\|C~B\|5\|0` · `sell` · `eatdish` | 범위등급, 요리 |
| `사막04~06` 실무 | `dogam\|오아시스\|18` · `deliver\|<사막부품>\|2` · `craft` | **납품(미사용 축)** |
| `사막07~09` 정보 | `fish\|아무\|B\|3\|40` · `material` · `forage` | 크기+채집 |
| `사막10~12` 상단 | `교단의인장` · `casino` · `fish_fresh\|아무\|A\|2\|0\|85` | 카지노, 신선도 |
| `붉은사막01~03` | `area` · `dogam\|붉은사막\|16` · `자하린` | 도감 상향(12→16) |
| `사막12b` ★졸업 | `fish\|아무\|S\|1\|0` + `fish\|아무\|아무\|1\|60` + `level\|20` + `dogam\|오아시스,붉은사막\|40` | **S 첫 요구 + 대물** |
| `사막13~14` 조각 | `실러캔스` · `visit\|나디아` | — |

**[미정의]** 사막 전용 부품 이름(`deliver` 대상), 사막 요리 id(`eatdish` 대상), 카지노 키.

## 4챕터 재설계 (Lv20–22 · 5개)

| ID | 이름(개편) | 목표(안) | 수여 |
|---|---|---|---|
| `왕도01` | 왕성의 부름 | `area\|왕도` | (자동) |
| `왕도02` | 성문의 검문 | `visit\|위병1` | 성문 위병 로타르 |
| `왕도03` | 대사서의 부탁 | `fish_fresh\|아무\|B\|3\|0\|90` | 대사서 (표본은 싱싱해야 한다) |
| `왕도04` | 근위대의 시험 | `fish\|아무\|A\|2\|60` | 성문 위병 쿠르트 |
| `왕도05` | 옥좌 앞에서 | `visit\|국왕` (+칭호 `왕실어부`) | **국왕 (첫 대면)** |

필요레벨 12·13·14·15·16 → **28·29·30·31·32** (x8 곡선 반영)

## 5챕터 재설계 (Lv20–25 · 13개) — 방향

**선결: 상단 해역 어종 20~30종(E~S) 신설.** 이게 없으면 아래 목표를 채울 수 없다.

| 구간 | 개편 방향 | 주요 축 |
|---|---|---|
| `본상단01~05` 상단마을 | `area` · `fish\|아무\|A\|2\|0` · `dogam\|상단마을\|12` · `earn`→`sell` 교체 · `상단화물꼬리표` | 신설 어장 도감 |
| `배상단01~04` 승선 | `area` · `fish_fresh\|아무\|B\|5\|0\|90`(선원 식량) · `material\|아무\|10` · `fish\|아무\|아무\|3\|50` | 신선도, 대물 |
| `배상단05~06` 증거 | `녹슨열쇠` · `밀랍봉인문서` | 퀘스트 전용 어종 |
| `배상단07` 선장 | `fish\|아무\|S\|1\|0` + `fish\|아무\|아무\|2\|60` | **선장 첫 대면** |
| `배상단08` ★베타 종점 | `fish\|아무\|S\|3\|0` + `fish\|아무\|아무\|1\|70` + `level\|25` (+칭호 `은빛갈매기`) | 3중 복합 |

⚠️ `본상단02`가 현재 `earn|100000`류 주간 카운터를 쓰는지 확인 필요 — 메인에서 `earn`은 피한다.

---

## 보스전은 이미 있다 — 전리품 퀘스트가 오늘 가능하다

`com/blockship/boss/` 4파일 2,095줄에 **이무기 전투 v4**가 완성돼 있다.
설계는 「보스 공격 회피 + 약점 낚시터에서 낚시로 딜」 1인 인스턴스전.

| 요소 | 상태 |
|---|---|
| HP·페이즈 | MAX_HP 60 · 약점 낚시 1회 6딜 · Phase NORMAL/BREACH_DIVE/BREACH_WARN/BREACH_LEAP |
| 공격 | 가시 볼트(4) · 수압탄(6) · 브레스(6, 점프 회피) · 브리칭(8, 38초 주기) · 몸통 접촉(4) |
| 예고 | 빨간 유리판 BlockDisplay + 임박할수록 얇아지는 카운트다운. 약점만 노란 유리판 |
| 약점 | 30초마다 25초 개방, OP `/이무기 약점`으로 지정 가능 |
| **전리품** | **용비늘**(G급 전용 재료) · **여의주**(용도 미정) 독립 확률 롤 |
| **퀘스트 훅** | 이미 `crafting.questManager().onMaterial(viewer, "용비늘")` 호출 (`ImugiBattle.java:712·719`) |
| 관전 | OP `/이무기 관전 추가 <플레이어>` — 촬영용 |

**따라서 `material|용비늘|1` / `material|여의주|1`로 "전리품을 가져오라" 퀘스트를 지금 바로 만들 수 있다.**

남은 보스 작업 (작음):
- [ ] 폭포 뒤 동굴 **6층 지역 생성 + Lv80 게이트** (현재 dev flatroom 테스트 호수에만 존재)
- [ ] **「꽃」 정의** — 유저가 말한 "이무기가 지키는 꽃". `여의주`가 "용도 미정"이라 이 자리일 가능성
- [ ] 전리품을 가져가는 **사이드 퀘스트라인**
- [ ] prod `imugi_rig.json`이 2026-07-10판 — 직선 리그(`imugi_s_rig.json`) 최신 여부 확인

---

## Codex 인수인계 (2026-08-07)

이 문서의 설계를 실제 JSON에 반영하는 작업. **설계 판단은 이미 끝났으니 추가 설계 없이 스펙대로 구현할 것.**

### 반드시 지킬 규약

1. **작업 대상은 prod다.** dev는 Citizens NPC가 12명 적어(식당 주인 없음) 튜토 검증이 불가.
   `scp`로 prod 파일을 받아 고치고 되올린다.
   ```
   scp -i ~/.ssh/oracle-mc.key ubuntu@168.107.8.107:~/mcserver/plugins/BlockShip/quests.json .
   # 편집 후
   scp -i ~/.ssh/oracle-mc.key quests.json ubuntu@168.107.8.107:~/mcserver/plugins/BlockShip/
   ssh -i ~/.ssh/oracle-mc.key ubuntu@168.107.8.107 '~/mcserver/scripts/rcon.py 데이터리로드'
   ```
2. **리로드 범위** — `/npc동기화` = npc.json + dialogue.json · `/데이터리로드` = quests/fish/parts/enhance/recipes/materials/config.
   **npc.json을 올린 직후 반드시 `/npc동기화`** (`NpcManager.save()`가 메모리를 파일로 덮어써서, 리로드 전에 save가 돌면 방금 올린 파일이 날아간다).
3. **prod에서 JSON을 고치면 반드시 dev로 역동기화.** `deploy-blockship.sh`가 JSON 6종을 dev→prod로 덮으므로, dev가 낡으면 다음 jar 배포에 수정이 조용히 되돌아간다. 작업 후 해시 일치 확인.
4. **편집 전 prod 백업**: `cp quests.json quests.json.bak-<작업명>-$(date +%s)`
5. **verb 스펠링 검증 필수** — 모르는 verb는 `target=0`이 되어 퀘스트가 즉시 완료된다. 위 「목표 문법 팔레트」의 28종만 사용.
6. `&l`·`§l` **볼드 전면 금지** (전역 훅이 차단). 색코드는 `&`.
7. **NPC 이름표 색** — 퀘스트를 주는 NPC는 `&a` + `[Q]`, 기능형은 `&b`, 대화만은 `&f`.
8. **메인 퀘스트는 반드시 NPC `quests` 배열에 배정**. 미배정이면 영구 수락 불가(story.md 「수락 메커니즘」).

### 검증 스니펫 (작업 후 매번)

```python
import json, collections
Q = json.load(open('quests.json'))['퀘스트']
N = json.load(open('npc.json'))['npcs']
OK = {'fish','fish_fresh','harpoon','area','visit','action','dogam','sell','money','earn','login',
      'quest_daily','craft','deliver','material','forage','eatdish','equip','trap','enhance',
      'skill','collectible','casino','level','mine','harvest','sail','submit'}
assigned = {q for v in N.values() for q in (v.get('quests') or [])}
print('불량 verb:', [(k,g) for k,v in Q.items() for g in v.get('목표',[]) if g.split('|')[0] not in OK])
print('메인 미배정:', [k for k,v in Q.items() if v.get('카테고리')=='메인' and k not in assigned])
print('체인 끊김:', [k for k,v in Q.items()
                  if v.get('다음퀘스트') not in (None,'-','') and v['다음퀘스트'] not in Q])
```
셋 다 빈 리스트여야 한다.

### 함정

- `earn` `login` `quest_daily`는 **주간 카운터**를 읽는다. 메인 퀘스트에 쓰면 주간 리셋에 진행도가 날아간다 — `sell`/`money`로 대체.
- 등급 단일 표기(`B`)는 "**B 이상**"이다. 구간을 강제하려면 `C~B`.
- `deliver`는 완료 시 아이템을 **회수**하고 **무손상 부품만** 인정한다.
- `fish`는 낚싯대·작살 둘 다 집계. 작살 전용은 `harpoon`.
- `상단마을` 어장이 E등급 9종뿐이다 — 5챕터 목표를 쓰기 전에 어종 신설이 선행이다.
- 퀘스트를 **삭제하지 말 것.** 플레이어 `questStatus`에 잔재가 남는다(`pruneOrphanQuests`가 정리하지만 진행 중이면 손실). 이름·목표·수여자만 바꾼다.

## 작업 순서

1. **[완료] 상단 해역 어종 25종(E~S) 신설** — 5챕터 목표를 상단마을 어장에서 달성 가능
2. **[완료]** 체인 재배선 4곳 + 왕도 28~32 재배치 + 명분 2곳 반영
3. 2챕터 목표 개편 11개 + 수여 NPC 재배치(요한 4·노인 2·세르간 5·하겐 1) + 하겐 사이드 선행 상향
4. 영주성 NPC 3인 + `영주01~05` 신설
5. 3챕터 목표 개편 22개 + 권위 계단 배정 (사막 미배정 18개 해소)
6. 4챕터 5개 (`왕도02`·`왕도04`·`왕도05` 이름·목표·수여 변경)
7. 5챕터 13개 + 상단 NPC 배정
8. 버그 2건 — `튜토_작살2` 목표 수정 / verb 검증 스니펫을 배포 절차에 넣기
9. 사이드: `폭포_뒤_동굴_1층` requiredLevel 0→30, 3~6층 신설

각 단계는 prod에서 작업하고 dev로 역동기화한다. 리로드는 `/npc동기화`(npc·dialogue) + `/데이터리로드`(quests).
