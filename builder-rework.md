# AIBuilder 건축 성능 리워크 — 전면 검토 및 로드맵 (2026-07-10)

> 목표: 마크 건축 MCP(minecraft-ai-builder)의 출력물을 "찍어낸 티 나는 절차 생성물" → "사람 빌더 수준의 그럴싸한 건축물"로 끌어올린다.
> 소스: `/Users/user/development/ai-builder-plugin/` (Java 플러그인 + Python MCP 서버, 툴 121개)

---

## 0. 한 줄 진단

**지형 엔진은 이미 월드젠급(8.5/10)인데, 그 위에 올라가는 건물 어휘가 빈약하다.**
2024~2026 SOTA(GDMC 우승작·Creative Agents·T2BM)의 일관된 신호는 **"LLM이 짓게 하지 말고, LLM이 *고르게* 하라"** — 사람 수준 프리팹/팔레트 어휘를 최대한 쌓고, LLM은 스펙 결정과 미학 비평만, 시공은 결정론 엔진이 독점. 현 시스템은 이 구조를 이미 반쯤 갖췄으므로(스펙→build_house→검증), 병목인 **어휘(팔레트·프리팹·풋프린트 다양성)** 를 채우는 것이 최대 효율.

## 1. 계층별 현황 평가

| 계층 | 점수 | 핵심 발견 |
|------|------|----------|
| 지형 (terrain/) | ★★★★½ | 도메인워프 fBm + 수력/열 침식(Sebastian Lague 포트) + massif 능선 + 스플라인 고도매핑(1.18+ 방식) + flat_zones. 절벽 생성기(WIP 미커밋)는 Gaea급. **강점 계층 — 손대지 말 것** |
| 절차 생성기 (HouseGenerator 등) | ★★ | **최대 병목.** 스타일 8종 전부 switch문 하드코딩(팔레트 필드 9개 고정), footprint는 사실상 사각형+L윙만(T/U/plus는 생성기만 있고 미사용), 크기 캡 28×28×3층, 창문 간격 규칙 단일(slotW+3, 면당 최대 3개), 지붕 3종에 slope=1 고정, 아키타입이 지붕을 강제 |
| 패턴 라이브러리 | ★★★ | 148종 저장돼 있으나 전부 소형 부품(창/문/가구). 파사드 리듬·지붕 시스템·통짜 모듈 같은 중대형 어휘 없음. 회전은 구현됐으나 복합 blockstate 미검증 |
| 검증 루프 | ★★★ | task_id→체크리스트→서브에이전트→token→declare 구조적 강제는 우수(우회 곤란). 그러나 ReportValidator는 형식만 검증(200자+8항목+좌표1개), BuildLint는 오류 감지만 가능·미학 판단 불가, 팔레트 조화 검사 없음 |
| MCP 서버 (파이썬) | ★★ | 4,008줄 단일 파일, call_tool if/elif 121개, 응답 압축 없음(lint가 부유블록 좌표 전부 반환 등). 집 1채 = 왕복 40~100회 |
| guidance.md | ★★★ | 지형보호·60/30/10·검증 흐름은 잘 가르침. **비례/실루엣/색이론(그라데이션)/랜드스케이핑 원칙 부재** |

## 2. "출력물이 뻔해지는" 구조적 이유 (코드 근거)

1. **스타일당 팔레트 1개 고정** — nordic이면 무조건 spruce_log+spruce_planks+dark_oak_stairs. 같은 스타일 집끼리 색이 완전 동일 (HouseGenerator.java:53-114)
2. **실루엣 단일** — 모든 집이 "직사각형+돌출윙" 실루엣. FootprintGenerator가 T/U/plus를 만들 수 있는데 HouseGenerator가 안 씀 (HouseGenerator.java:216-238)
3. **창문 배치 결정론 단일 규칙** — 간격·개수·높이 전부 고정 공식 (HouseGenerator.java:313-340)
4. **인테리어 = 고정 큐** — 아키타입×프로필×층 = 결정적 가구 순서, 변형은 수작업 추출 패턴 풀(N≤9)에 의존 (FurnishPass.java:122-162)
5. **모든 디자인 결정이 재컴파일 영역** — 스타일/규칙/프리팹 추가 = Java 수정+빌드+재시작

## 3. SOTA 리서치 요약 (2024-2026)

- **GDMC 2024·2025 우승 공식**: 도시계획(경로망·배치)은 알고리즘, **건물 본체는 사람이 만든 프리팹/케이스 로직**. 순수 절차생성·순수 LLM 둘 다 우승 못 함.
- **T2BM 논문**: LLM 직접 블록배치 성공률 GPT-4 기준 12% → 중간표현(IR)+정제 도입 시 38%. **LLM은 스펙 JSON을 내고 엔진이 시공**해야 함.
- **Creative Agents**: 시각 목표(이미지)를 먼저 확정 → 블루프린트 변환 → 결정론 시공. 레퍼런스 이미지가 있을 때 비평 품질 급상승.
- **비전 피드백 실효성**: 스크린샷은 "좌표 교정"엔 부적합, "미학 판정(단조/비율/팔레트 충돌)"엔 유효. 1차 검증은 구조적 관측(텍스트)으로.
- **WFC**: 통건물 적용은 실패 사례 다수. 실내 방 배치·바닥 패턴 같은 국소 2D 레이아웃 전용.
- **팔레트 이론은 이미 텍스트화돼 있음**: BlockBlend "2-of-3 규칙"(색온도·명도·재질논리 중 2개 공유), 60-30-10, ArdaCraft/Conquest 그라데이션 블록 체인표 → 그대로 JSON화 가능.

## 4. 로드맵 (효과/노력비 순)

### P1. 팔레트·그라데이션 데이터화 + 스타일 JSON 구동 — ✅ 완료 (2026-07-10, dev 배포·검증, prod 미배포)
구현 내역 (커밋 8d3bef4):
- `styles.json`(번들→`plugins/AIBuilder/styles.json`) + `StyleRegistry.java` — HouseGenerator switch 제거, 데이터 구동
- 팔레트 변형: nordic 4종(classic/falu_red/tarred/whitewash), tudor 3종(classic/aged_plaster/oak_frame), stone 3종(classic/weathered/deepslate_keep), 목재 5종 이식 + **신규 brick(european)/desert(sandstone·adobe)** — 위치 시드 해시 선택으로 같은 스타일도 집마다 팔레트 다름, `style/variant` 명시 지정 가능
- `mc_style_list` / `mc_style_reload` / `mc_style_upsert`(재료 레지스트리 검증, LLM이 런타임에 새 팔레트 정의→즉시 사용)
- `palette-meta.json`(114 블록 패밀리: 색온도warm/cool/neutral+명도 v1~5) + BuildLint `palette_harmony`(온도충돌·모노톤·액센트노이즈·명도격차) — 벽vs지붕 대비(2밴드 차)는 정상 처리, 3밴드+ 격차만 경고
- guidance.md 색이론(2-of-3)·비례·실루엣·랜드스케이핑 섹션
- dev 검증: flatroom 6채 변형 분산 확인 + harbor 커스텀 업서트→건축 + lint 오탐 튜닝. MCP 신규 툴은 MCP 재연결 후 노출

원계획:
- ArdaCraft/BlockBlend류 **블록 그라데이션 체인 + 유효 팔레트 조합표를 JSON**으로 제작 (`palettes.json`: 팔레트id → {corner/beam/plinth/infill가중치/roof가중치/색온도/명도단계})
- HouseGenerator의 switch 하드코딩 → **JSON 로드 + 런타임 리로드** (스타일 추가에 재컴파일 불필요). 같은 스타일도 팔레트 변형 N종에서 해시 선택 → 마을에서 집마다 색이 달라짐
- BuildLint에 **팔레트 위반 검사** 추가(온도 충돌, 액센트 비율 초과, 그라데이션 끊김)
- guidance.md에 비례·실루엣·색이론 섹션 신설

### P2. 프리팹 어휘 대량 확충: 148종 → 수백 종 (효과 큼, 2~4일)
- 파이프라인은 이미 존재: `import_schem.py` + `mc_pattern_extract` + 블록 바이블. 빠진 건 **수집→부품 분해→시맨틱 태깅 배치 작업**
- 라이선스 명시 자산(CC/MIT) 위주로 수집, 통짜 복사 대신 **부품 단위 분해**(현관/굴뚝/창틀 리듬/박공/발코니/부벽/지붕 모서리) — 권리·미학 양쪽에 유리
- 패턴 스키마에 **적용 제약 태그** 추가(스타일·최소 벽폭·앵커 규칙) → FurnishPass/DetailPass가 자동 선택 가능
- import_schem 리맵 테이블 확장(현재 chain 1개뿐), 회전 테스트 스위트

### P3. 생성기 자유도 해제 — ✅ 완료 (2026-07-10, dev 검증, 커밋 665867c)
- 크기 캡 28→44, 층수 3→4, 넓은 벽 창문 수 자동 상향
- `footprint` 파라미터: l/t/u/plus/offset 형태 플랜 — **팔 두께 ≥8 강제**(기존 피드백), 문 방향 자동 회전, hip 지붕(apex 굴뚝)
- `roof_type`(auto/gable/hip/flat) + `roof_max_rise`: Composer 맨사드 클램프(경사 상한+더블슬랩 데크, 자동 ≤9) — 넓은 집 성당지붕 방지
- (원계획 중 "IR JSON 스키마 승격"은 사실상 파라미터 확장으로 갈음 — 필요시 후속)

### P4. 검증 루프 재편 — ✅ 핵심 완료 (2026-07-10, 커밋 eefa973)
- /build_house 응답에 **lint 자동 포함**(hints+팔레트 온도) — 집당 후속 왕복 절감
- 검증 체크리스트 15→**20항목**: 미학 5항목 신설(실루엣 2단, 파사드 깊이, 팔레트 60/30/10+온도, 지붕 비례 1/3~1/2, 랜드스케이핑 심) + 미학은 좌표 교정 금지·스펙 수준 수정 제안 지시
- 잔여(선택): scan/inspect 응답 truncation, python 서버 모듈화(P6)

### P5. 랜드스케이핑 어휘 — ✅ 완료 (2026-07-10, 커밋 665867c)
- `mc_build_mega_tree`: 절차 대형 나무 10~48블록(테이퍼 트렁크+린+뿌리 플레어, round 가지/잎블롭·spire 침엽 티어), 잎 air-only
- `mc_scatter_boulders`: 노이즈 변형 바위 블롭, 1/3 매립, 석재 60/30/10, 물 스킵
- `mc_carve_river`: 경유점 강 — 단조 하강 수위, 포물선 단면, 자갈/모래 하상+강둑 절개, 미앤더

### (구 P3 원계획 — 참고용)
- `mc_build_house` 입력을 **명시적 스펙 JSON**으로: footprint(T/U/plus/자유 폴리곤), 크기 캡 28→64+, 층수 3→5, 지붕 타입·경사 선택, 팔레트id, 프리팹 슬롯 오버라이드
- LLM 비평도 IR 수정으로 받기: "지붕 hip으로, 팔레트 warm_stone으로" → 같은 자리 재생성(이미 클린 오버라이트 지원)
- 창문 배치를 리듬 프리셋 몇 종(균등/쌍창/중앙강조/불규칙)에서 선택

### P4. 검증 루프 재편 — 텍스트 1차, 스크린샷은 미학 전용 (1~2일)
- 1차 게이트: lint(팔레트 검사 포함)+detect_rooms+elevation 등 **구조적/수치 검증** 자동 통과 후에만 스크린샷 단계로
- 스크린샷 비평은 **레퍼런스 이미지 병치 + 미학 체크리스트**로 프롬프트 표준화 (좌표 교정에 쓰지 않음)
- 응답 압축: lint 좌표 truncation, scan top_materials 필터 → 왕복당 토큰 절감
- 목표: 집 1채 왕복 40~100회 → 15~25회

### P5. 랜드스케이핑 마감 어휘 (2~3일, 병렬 가능)
- **커스텀 대형 나무** (L-system 가지, 캐노피 — 바닐라 14종만으론 랜드마크 불가)
- **바위/암설 배치기** (boulder 프리팹 + noise 회전), **강/폭포 네트워크** (침식 골짜기 → 물 경로 추출)
- WIP 미커밋 4파일(massif·generate_cliff·topdown step) 커밋부터

### P6. (지원성) MCP 서버 모듈화 — 위 작업과 병행
- 4,008줄 단일 파일 → tools 정의/핸들러 카테고리 분리, 응답 압축 헬퍼 공통화

## 5. 하지 말 것 (리서치 근거)

- ❌ LLM이 mc_place_many로 블록 좌표를 직접 대량 산출하는 방향 강화 (성공률 <40%)
- ❌ WFC 통건물 적용 (실패 사례 다수 — 국소 레이아웃만)
- ❌ 스크린샷 루프로 블록 좌표 교정 (공간 역산 약함 — 미학 판정 전용)
- ❌ 지형 엔진 재작업 (이미 강함 — WIP 커밋만)
