# 카지노 디스플레이 리워크 — GUI 탈피, 월드 테이블 전환 (2026-07-11)

> **동기**: "게임을 UI에서만 하니까 재미없다"는 피드백 다수. 인벤토리 GUI 카지노를 **월드 테이블 + ItemDisplay 카드/칩 + 딜러 NPC** 물리 카지노로 전환한다.
> 테이블 건축은 이미 완료(홀덤 8석 십자형 / 블랙잭 / 쓰리카드 / 섯다 — 스크린샷 4장 기준).

## 0. 목표 게임 및 우선순위 (유저 확정)

| 순위 | 게임 | 인원 | 딜러 NPC | 비고 |
|------|------|------|----------|------|
| 1st | 텍사스 홀덤 | **8인 멀티** | 연출용 | ★현행은 Ultimate(vs딜러) 1인용 → **엔진 신규** |
| 1st | 섯다 | **8인 멀티** | 연출용 | ★현행은 NPC 1:1 → **엔진 신규**, 족보는 재사용 |
| 2nd | 슬롯머신 | 1인 | 불필요 | 현행 로직 100% 재사용, 렌더만 교체 |
| 3rd | 블랙잭 | 4인 | **필요** | 공유 딜러+좌석별 핸드로 리팩터 |
| 4th | 룰렛 | 4인 | **필요** | 물리 베팅 레이아웃 + 회전 휠 |
| 5th | 쓰리카드 포커 | 4인 | **필요** | 블랙잭 인프라 재사용, 최소 작업 |

**필수 조건 4가지**: ① 카드 ~0.3블록, 간략화 이미지 RP 등록 ② 베팅액 따라 칩 자동 표시 ③ 딜러 NPC + 딜링/플립 애니메이션 ④ 정확한 룰.

---

## 1. 현재 구현 감사 (2026-07-11 기준)

`com/blockship/casino/` — **전체 미커밋 WIP** (`?? casino/`, 병렬 세션 산출물), 총 ~5,600줄, `/카지노` GUI 게임 6종.

### 그대로 재사용 (검증된 자산 — 건드리지 말 것)
| 자산 | 내용 |
|------|------|
| `card/StandardCard` | 52장 값 객체 (suit/rank, deck(n)) |
| `card/PokerHandEvaluator` | **5~7장 최강 5장 족보 + 완전한 타이브레이커** → 홀덤 쇼다운에 그대로 |
| `seotda/SeotdaCard`·`SeotdaRules` | 20장 덱(1~10월×2, 광=1·3·8), 절대순위 족보(38광땡~망통, 동점=무승부) → 8인 쇼다운에 그대로 |
| `blackjack/BlackjackRound` | 히트/스탠드/더블/스플릿 상태기계 — 딜러 플레이 분리 리팩터 후 재사용 |
| `card/ThreeCardPokerRules`·`slot/SlotRules`·`roulette/RouletteRules` | 정산 규칙 그대로 |
| `CasinoLedger` | **WAL 2단계 정산 + 고아 복구** (reserve→settle, saveNowVerified 롤백, refundOrphan) — 돈 안전의 핵심. 멀티테이블 스택 모델도 이 위에 얹는다(§3) |
| 복구 철학 | "확정 결과는 환불로 덮지 않는다", 셧다운 정산, 2분 타임아웃 |

### 대체 대상
- `CasinoManager`(1,545줄)의 **GUI 렌더링/세션/입력 전부** — 인벤토리 → 월드 디스플레이.
- 홀덤: `UltimateHoldemRules`(vs딜러 Ante/Blind/Play)는 물리 홀덤에 **쓰지 않는다**. 8인 노리밋 캐시게임 엔진 신규.
- 섯다: `SeotdaRound`(1:1, NPC AI 내장)는 8인 엔진으로 대체. 단 **NPC AI(chooseBotAction)는 P7 봇 착석용으로 보존**.

### 재사용할 기존 인프라 (조사 완료, 전부 검증됨)
| 필요 기능 | 기존 패턴 | 위치 |
|-----------|-----------|------|
| 카드/칩 렌더 | ItemDisplay + `setItemModel(NamespacedKey)` + FIXED billboard | ImugiBoss.java:140~150, CropManager.java:249~283 |
| 부드러운 이동 | `setTeleportDuration(2)` + `setInterpolationDuration(n)` | ImugiBoss(2틱 검증), FishingListener |
| **개인 카드 가시성** | `hideEntity`/`showEntity` + join 시 재적용 | ImugiBoss.applyVisibility / hideFromJoiner (개인전 보스에서 검증) |
| 착석 | 투명 ArmorStand + addPassenger + **grim.exempt attachment** | ZiplineManager.java:233~301 |
| 딜러 NPC 애니 | Citizens + BetterModel steve + NpcAnimator 제스처 11종(point/wave/cheer…) | npc/NpcAnimator |
| 고아 청소 | PDC 태그 + persistent=false + 부팅 스윕 + EntitiesLoadEvent | ShipOrphanCleaner |
| RP 아이템 모델 | pack_format 46, `parent: item/generated` 2D 모델, `assets/barkan/items/*.json` (item_model 방식) | barkan-resourcepack (물고기/엠블럼 선례) |
| 커스텀 사운드 | `assets/barkan/sounds.json` + ogg | 날씨 사운드 선례 |

---

## 2. Phase 0 — 공통 기반: CasinoTable 프레임워크 (`casino/table/`)

모든 게임이 공유하는 신규 계층. **이게 전체 공수의 절반** — 여기가 튼튼해야 P1~P6이 조립이 된다.

### 2.1 테이블 등록·영속 (`CasinoTableRegistry`)
- `casino-tables.json` 영속. OP 명령 `/카지노테이블` (OP 전용 → 영타 별칭 금지):
  - `생성 <게임> <테이블id>` — 서 있는 위치 = 테이블 원점
  - `좌석 <id> [번호]` — 서 있는 위치+시선으로 좌석 등록 (건축된 의자 자리에서 실행)
  - `존 <id> <board|pot|dealer|shoe|button>` — 커뮤니티 카드 라인/팟/딜러 위치/딜링 원점 등록
  - `격자 <id> <행> <열>` — 룰렛 전용: 두 모서리 클릭으로 베팅 셀 격자 자동 분할
  - `딜러 <id> <npc이름>` — Citizens NPC 바인딩
  - `정보/목록/삭제/리로드`
- 게임별 존 스키마 검증(홀덤=좌석≥2+board+pot+shoe, 블랙잭=좌석≤4+dealer…) 후에만 활성화.

### 2.2 착석 (`TableSeatService`)
- 빈 좌석: Interaction(0.6×0.8) + TextDisplay `"[빈 자리] 우클릭 착석"` + 최소 조건(바이인/레벨) 표시.
- 착석 = zipline 패턴 그대로: 투명 ArmorStand(persistent=false, marker=false) + addPassenger + **grim.exempt 부여**, 하차(웅크림)·퇴장 시 해제.
- 게임 진행 중 착석 → 다음 핸드부터 참여. 하차 → 진행 중 핸드는 자동 폴드 후 스택 정산.

### 2.3 디스플레이 수명 관리 (`CasinoDisplayService`)
- **모든** 카지노 엔티티(카드/칩/버튼/라벨/좌석)에 PDC `blockship:casino=<테이블id>` + `persistent=false`.
- ShipOrphanCleaner 패턴 복제: 부팅 스윕 + EntitiesLoadEvent — 등록 테이블의 "현재 렌더 세트"에 없는 태그 엔티티는 즉시 제거. 테이블 비주얼은 상태로부터 언제나 재구축 가능(상태→렌더 단방향).
- 핸드 진행 중 테이블 청크에 chunk ticket (플레이어가 다 튕겨도 정산까지 언로드 방지).

### 2.4 카드 렌더 (`CardDisplayService`)
- **모델**: ItemDisplay + `meta.setItemModel("barkan:card/<이름>")`, billboard FIXED, ItemDisplayTransform FIXED. `items/cod.json` 오염 없음(카드는 인벤토리에 존재하지 않는 표시 전용).
- **규격**: 텍스처 32×32(카드 영역 22×30px, 나머지 투명), scale **0.44** → 월드에서 **0.30×0.41블록** (유저 스펙 0.3블록). 테이블 면 +0.02 y, 좌석 yaw 정렬, 카드 간 미세 y오프셋(z-fight 방지, 이무기 관절 오프셋 교훈).
- **딜링 애니**: shoe/딜러 손 원점에 뒷면 스폰 → `setTeleportDuration(7)` 텔포 보간으로 목적지 비행 + left-rotation 스핀 → 착지 시 `barkan:casino.card_deal`. 좌석 순서대로 2틱 간격 스태거(8인×2장 = 약 3.5초 리듬).
- **플립 애니**: ① 3틱 보간으로 y축 0→90°(모서리만 보임) ② 그 틱에 ItemStack 을 앞면으로 교체 + 회전 −90° 세팅 ③ 3틱 보간 −90→0° — 총 6틱, `card_flip` 사운드.
- **★개인 카드(홀덤/섯다 홀카드)**: 엔티티 2벌 전략 —
  - 앞면 엔티티: 주인만 봄 (스폰 직후 전 접속자 hideEntity, 주인 제외)
  - 뒷면 엔티티: 주인 빼고 전원 봄
  - **PlayerJoinEvent에서 hide 재적용 필수** (hideEntity는 세션 단위 — ImugiBoss.hideFromJoiner 패턴 그대로)
  - 쇼다운: 개인 벌 제거 → 공용 앞면을 플립 연출로 공개. 폴드 머크: 뒷면이 팟 쪽으로 미끄러진 뒤 소멸.
- 보조 표시: 내 홀카드 요약을 액션바에 상시 ( `♠A ♥K · 원페어` ) — 멀리서/각도 문제 대비.

### 2.5 칩 렌더 (`ChipRenderer`)
- 칩 모델: elements 원반(8×1.5×8px) — 액면 4종 색상 = 기존 BETS 사다리(1천 철/1만 금/10만 초록/100만 파랑) + 지폐(1000만, 고액 테이블용 예비).
- **금액→시각 분해**: 큰 액면부터 그리디, 스택당 최대 8장(장당 y+0.028), 최대 4스택, 넘치면 스택은 캡하고 **TextDisplay 금액 라벨은 항상 정확**: `"235,000원"` (Num.fmt — double concat 금지 규약).
- 존 3종: 좌석 앞 **베팅 존**(현재 핸드 베팅) / 중앙 **팟 존** / 좌석 옆 **스택 표시**(보유 칩 스택 + 이름 라벨).
- 연출: 베팅 확정 → 칩이 베팅 존에 착·`chip_place` / 라운드 종료 → 베팅 존 칩들이 팟으로 텔포 보간 슬라이드·`chip_slide` / 승자 지급 → 팟이 승자 스택으로 슬라이드·`chip_win`.

### 2.6 입력 (`TableInputService`) — GUI 없이, 월드 안에서
- **물리 버튼**: 내 턴에만 내 좌석 앞 테이블 모서리에 생성 — Interaction(0.35×0.35) + TextDisplay 라벨. **본인에게만 보임**(hideEntity — 남의 버튼 오클릭 원천 차단).
  - 홀덤: `[체크/콜] [레이즈] [폴드]` → 레이즈 클릭 시 `[−] [금액확정] [+]`로 전환(프리셋 순환: 최소레이즈→팟½→팟→올인, 액션바에 현재 금액 실시간).
  - 클릭 처리에 기존 관행 적용: 턴 시퀀스 토큰 검증(늦은 클릭/더블클릭 무효 — 현행 roundId+viewSeq 사상 이식) + inputLocked.
- **백업 입력**: 같은 액션을 채팅 클릭 컴포넌트로도 전송(버튼 히트박스 문제/시야 가림 대비). 봇 테스트도 이 경로 활용 가능.
- **턴 HUD**: 현재 턴 좌석 위 TextDisplay `"▶ 니크네임 · 18초"` 카운트다운 + 5초 남으면 틱 사운드, 당사자 액션바 병행.

### 2.7 딜러 NPC (`CasinoDealerService`)
- NpcDef에 `casino` role 신설 + npc.json 등록(콘솔 tmux `npc등록`, saves.yml 색규칙 **하늘색 &b** = 기능형).
- 딜링 순간: NPC를 대상 좌석으로 회전(Citizens faceLocation) + NpcAnimator `point` 제스처 → 카드가 딜러 손 원점에서 출발. 셔플 타임: `ponder`/`stretch`. 승자 정산: `cheer`.
- BetterModel 커스텀 제스처(양손 딜링)는 **후순위 폴리시** — 기존 11종으로 1차 출시.
- 스킨: 마을 내 중복 금지 규약 준수(textureRaw 해시 대조), 카지노 컨셉 정장 스킨.

### 2.8 관전
- 커뮤니티 카드/팟/칩/뒷면 카드는 전원 공개 — **구경 자체가 홍보**. 좌석 Interaction 클릭으로 즉시 합류.
- 쇼다운 결과는 테이블 중앙 TextDisplay 3초 (`"승자: 니크 · 풀하우스 · +240,000원"`) + 주변 사운드.

---

## 3. 돈 처리 — CasinoLedger 확장 (신규 코드 최소화)

### 3.1 하우스 게임 (블랙잭·쓰리카드·룰렛·슬롯) — 변경 없음
현행 그대로: 판 시작 `reserve(baseBet×maxMultiplier)` → 종료 `settle`. 좌석/렌더만 바뀌고 돈 흐름 동일. 좌석별 독립 라운드라 4인이어도 각자 자기 예치/정산.

### 3.2 테이블 게임 (홀덤·섯다) — 바이인 스택 모델 ★신규
- **착석 = `ledger.reserve(buyIn)`** — 기존 단일 예치 슬롯 그대로 사용 (1인 1테이블 자연 강제, 기존 GUI 판과 상호 배타도 공짜로 얻음).
- 핸드 중 베팅은 메모리 스택 이동만. **핸드 종료마다 WAL 스냅샷**: `SETTLEMENT_PENDING=1` + `SETTLEMENT_RETURN=현재스택` 저장 (플레이어당 saveNow 1회/핸드).
  - → 크래시 시 `refundOrphan`이 **마지막 핸드 종료 시점 스택**을 반환. 기존 복구 코드 수정 없이 의미가 정확히 맞아떨어짐. "바이인만 돌려받는 크래시 보험" 악용 봉쇄.
  - 미완료 핸드의 베팅은 핸드 무효 → 커밋액이 스냅샷에 이미 포함(스냅샷=핸드 경계에서만 찍으므로 자동).
- 하차/강퇴/서버 종료: `settle(reserved, 현재스택)` — expectedEscrow/roundId 가드 그대로.
- 접속 종료: 진행 핸드 즉시 폴드, 스택은 핸드 종료 시 오프라인 `settle(UUID, nameHint)` 경로(이미 존재).
- 스택 0 → 리바이(착석 유지 + 재예치... 단일 예치 슬롯과 충돌하므로 **정산 후 재착석**으로 단순화) 또는 자동 하차.

### 3.3 레이크 (머니 싱크, balance.md §20 신설)
- 홀덤: 팟의 **5%** (팟당 상한 = BB×5). 플랍 못 가면 무레이크.
- 섯다: 승자 순이익의 **5%** (현행 1:1 섯다 commission 개념 승계).
- 하우스 게임은 하우스엣지가 이미 싱크 → 레이크 없음.
- 수치(블라인드/바이인/테이블 티어)는 §9 결정사항 확정 후 balance.md에 기록.

---

## 4. 게임별 스펙

### P1. 텍사스 홀덤 8인 — `holdem/HoldemTable` 신규 엔진 (최대 공수)
- **노리밋 캐시게임**: SB/BB, 딜러버튼 시계방향, 데드버튼 룰은 단순화(버튼 항상 다음 생존자).
- 베팅: 체크/콜/벳/레이즈/폴드/올인, **미니멈 레이즈 = 직전 레이즈 증가분**, 언더레이즈 올인은 액션 재오픈 안 함(표준 룰).
- **★사이드팟 정확 구현** — 돈 사고 1순위 리스크. 엔진은 Bukkit 무관 순수 클래스 + 결정적 Random 주입(기존 룰 클래스 관행) + **단위테스트 필수**(사이드팟 3중첩, 스플릿 팟 홀수 칩=버튼 왼쪽부터, 헤즈업 블라인드 역전).
- 쇼다운: `PokerHandEvaluator.evaluate(홀2+보드5)` 비교. 공개 순서 = 마지막 공격자부터, 지는 패 자동 머크(옵션: 강제 공개 OFF).
- 상태기계: `WAITING(≥2인, 15초 카운트)→BLINDS→DEALING(애니 3.5초)→PREFLOP→FLOP→TURN→RIVER→SHOWDOWN→PAYOUT→INTERMISSION(8초)→…`
- 턴 타이머 **25초**: 타임아웃 = 체크 가능하면 체크, 아니면 폴드. 연속 2회 → 자동 하차 정산. (현행 2분 세션 타임아웃 사상을 턴 단위로 세분화)
- 렌더: 홀카드 2×8(개인/공용 2벌), 보드 5장 중앙 라인, 딜러버튼 ItemDisplay가 핸드마다 이동, 좌석마다 `이름+스택` TextDisplay, 팟 칩+라벨.
- 최소 2인(헤즈업: 버튼=SB 선공 프리플랍, 이후 BB 선공 — 표준).

### P2. 섯다 8인 — `seotda/SeotdaTable` 신규 엔진
- 20장 덱 → 8인×2장=16장 (덱 여유 OK). 족보/비교는 `SeotdaRules` 그대로(암행어사·땡잡이 없는 현행 초보 룰 유지 — 명시적 결정).
- 진행: 전원 앤티 → 2장 딜(1장씩 두 바퀴, 두 번째 장은 개인만 확인) → **베팅 라운드 1회**(선부터 시계방향) → 쇼다운. 동점 = 팟 분배.
- **베팅: 한국식 확정(2026-07-11)** — 액션: `체크 / 삥(앤티×1) / 콜 / 하프(현재 팟의 ½, 올림) / 따당(직전 베팅×2) / 올인(캡 한도) / 폴드`. ★1인 핸드 총 커밋 캡 = **앤티×20** (바이인·선예치 상한과 변동성 통제 — 올인=캡 잔여분). 레이즈 횟수 제한 없음(캡이 자연 종결). 하프/따당 금액은 물리 버튼 라벨에 실시간 표시.
- 렌더: 화투 카드 텍스처(월 상징 간략화 + 광 금테), 패 2장을 좌석 앞 부채꼴, 나머지는 홀덤 인프라 그대로.

### P3. 슬롯머신 1인 — 렌더 교체만
- 물리 캐비닛(별도 건축): 릴 3개 = ItemDisplay 3개, 심볼 텍스처 6종(체리/레몬/벨/BAR/다이아/7). 현행 `tickSlotAnimation`(2틱 셔플·감속 슬립·20초 자동정지·SPIN 순간 결과 고정)을 **GUI 갱신 대신 ItemStack 교체로 출력만 바꿔** 이식.
- 레버 = Interaction + 당김 연출(레버 ItemDisplay 회전 보간), 정지 버튼 3개 물리 배치, 좌석 1개.
- 돈/세션/복구 = 현행 SlotSession 그대로. 테이블 등록 시스템의 1인 테이블로 취급, 여러 대 설치 가능.

### P4. 블랙잭 4인 + 딜러 NPC — `blackjack/MultiBlackjackRound` 리팩터
- `BlackjackRound`에서 슈(6덱)·딜러 플레이·핸드 평가를 추출 → 좌석 1~4 각자 베팅·핸드(스플릿 포함) + **공유 딜러 핸드**.
- 사이클: 베팅 윈도우 15초(물리 버튼으로 액면 선택, 칩 착지) → 딜링 애니(플레이어 2장 앞면, 딜러 1업1다운) → 좌석 순서대로 턴(히트/스탠드/더블/스플릿, 좌석당 타이머 20초=자동 기본전략 아님, **자동 스탠드**… 현행 forceDefault의 "스탠드 가능하면 스탠드" 승계) → 딜러 홀카드 **플립 연출** + 17까지 드로우 → 좌석별 정산.
- 돈: 좌석별 독립 reserve/settle(§3.1). 한 좌석 오류가 다른 좌석 정산을 못 막게 좌석 단위 try-catch(현행 recoverBrokenRound 사상).

### P5. 룰렛 4인 + 딜러 NPC
- **물리 베팅 레이아웃**: 테이블 상판에 숫자 그리드(0~36+외부 12칸) — `격자` 등록으로 셀별 Interaction 자동 배치. 셀 클릭=칩 추가/웅크려 클릭=회수(현행 좌/우클릭 사상). 플레이어별 칩 색 테두리(4인 구분).
- 휠: ItemDisplay 원판 y회전 보간(감속 커브) + 공 ItemDisplay 역방향 → `targetNumber`에 정렬 정지(SPIN 순간 결과 고정 — 현행 로직 그대로), 결과 TextDisplay + 지급 슬라이드.
- 사이클(공유 턴 없음): `베팅 30초 → 노모어벳 → 스핀 6초 → 정산 5초 → 반복`. RouletteRules.Bet 재사용.
- 돈: 스핀 시점에 플레이어별 `reserve(칩합)` → 정산 `settle` (베팅 중엔 돈 안 묶음 — 현행과 동일).

### P6. 쓰리카드 포커 4인 + 딜러 NPC
- 블랙잭 테이블 인프라(베팅 윈도우+좌석 독립+공유 딜러) 재사용. Ante → 3장(내 것만 앞면 개인표시) → Play(=Ante 추가)/Fold → 딜러 공개 → `ThreeCardPokerRules` 정산 그대로. **작업량 최소 — P4 완성도에 따라 며칠 수준.**

---

## 5. 리소스팩 작업 (barkan-resourcepack)

| 자산 | 수량 | 방식 |
|------|------|------|
| 트럼프 카드 | 52 + 뒷면 1 | **64×64(카드 44×60px)** — 모바일 카드게임식 간략 디자인(픽셀에서 안 깨지게 대형 글리프만): 랭크 대형+수트 대형, 핍(숫자만큼 무늬 반복) 없음. **casino-tools/gen_cards.py로 자동 생성**(4× 슈퍼샘플→다운스케일). 스타일 A=빅인덱스(랭크 좌상+수트 우하) / B=센터 스택(랭크 상단+수트 하단, 가독성 최대) |
| 화투(섯다) | 유니크 13 + 뒷면 1 | 같은 월 2장은 텍스처 공유 → 실제 파일 = 10월 일반 + 광 3장(1·3·8월). 크림 카드 + 대형 월 숫자 + 간략 월 아이콘, **광 = 금테+금badge**. 동일 스크립트 생성 |
| 칩 | 4~5색 | elements 원반 모델 + 상면 텍스처(액면 각인) |
| 슬롯 심볼 | 6 | 현행 GUI 심볼 매핑(체리/레몬/벨/BAR/다이아/7) |
| 룰렛 | 휠 원판 + 공 + 셀 하이라이트 | 원판은 단일 텍스처 회전 |
| 딜러버튼/기타 | 2~3 | BUTTON, 노모어벳 팻말 등 |
| 사운드 | 8종 | card_deal, card_flip, chip_place, chip_slide, chip_win, roulette_spin, slot_reel, jackpot (ogg, `assets/barkan/sounds.json`) |

- 모델 정의: `assets/barkan/items/card/*.json`(item_model 방식) + `models/item/card/*.json`(parent=item/generated) + `textures/item/card/*.png`. **cod.json 케이스 추가 없음**(카드/칩은 표시 전용, 물고기 CMD 체계와 완전 분리).
- 배포: deploy-rp 절차 — **볼드(§l) 금지 규약은 TextDisplay 라벨에도 적용**.

---

## 6. 안전·운영 원칙

1. **디스플레이는 전부 소모품**: persistent=false + PDC 태그 + 부팅/청크로드 스윕. 시각은 항상 상태에서 재구축(배 시스템 교훈).
2. **재시작**: shutdown 훅 — 테이블 게임은 진행 핸드 무효(커밋 스택 복귀) 후 전원 settle, 하우스 게임은 현행 로직(확정 결과 유지·미시작 환불) 유지. ★jar 교체는 stop→교체→start 규약 그대로.
3. **가시성 상태는 세션 단위**: PlayerJoinEvent에서 개인 카드/버튼 hide 재적용 (ImugiBoss 선례).
4. **성능**: 테이블당 엔티티 ≈ 카드 37 + 칩 60 + 라벨/버튼/좌석 30 ≈ **130개**. 디스플레이 엔티티는 틱 비용 극소 — 카지노 홀 6테이블 ≈ 800개 수용 가능. 보간 갱신은 이벤트 시점에만(상시 틱 루프는 슬롯 릴·룰렛 휠·타이머뿐).
5. **엔진 순수성**: HoldemTable/SeotdaTable 코어는 Bukkit import 0 (기존 룰 클래스 관행) + 결정적 Random 주입 생성자 → **사이드팟/정산 단위테스트** 가능. 돈 계산은 전부 `Math.*Exact`.
6. **테스트**: mineflayer 봇 — GUI desync 문제가 사라지고 Interaction 클릭(entity interact 패킷)은 봇으로 가능. 채팅 클릭 백업 입력 + `/chatlog` 검증 병행. 8인 홀덤 자동 대전 시나리오 봇 스크립트.
7. **네트워크 순수성**: ProtocolLib 불필요(전부 Bukkit API) — prod 1.21.10 패킷 리스크 없음. per-player 가시성도 Bukkit `hideEntity`.

## 7. 마이그레이션

- 물리 테이블이 열리는 게임부터 **GUI 진입 차단**(로비 버튼 → "카지노 홀에서 플레이하세요" + 좌표/길찾기 연동). 카지노 공간 유도가 리워크의 목적이므로 병행 기간 최소화.
- `/카지노` = 안내(홀 위치·내 진행 판 상태·규칙 열람)로 축소, `/도박` 별칭 유지.
- 현행 casino/ WIP는 **먼저 커밋해 기준선 확보** (병렬 세션 오염 방지 — jar 통째 번들 규약) 후 리워크 브랜치 시작.

## 8. 로드맵 (공수: S < M < L)

| Phase | 내용 | 공수 | 완료 기준 |
|-------|------|------|-----------|
| **P0** | RP 자산(카드/칩/사운드) + 테이블 등록/좌석/디스플레이/칩/입력/딜러NPC 프레임워크 | **L** | 쇼케이스 테이블에서 딜링→플립→칩 슬라이드 데모, 재시작 후 고아 0 |
| **P1** | 홀덤 8인 엔진 + 통합 | **L** | 사이드팟 단위테스트 통과 + 봇 8인 100핸드 무사고(원장 잔액 대사 일치) |
| **P2** | 섯다 8인 | M | 화투 렌더 + 20핸드 봇 대전 |
| **P3** | 슬롯 캐비닛 | S~M | 현행 세션 재사용 확인, 연출만 신규 |
| **P4** | 블랙잭 4인+딜러 | M | MultiBlackjackRound 테스트(스플릿/더블 경계) |
| **P5** | 룰렛 4인+딜러 | M~L | 격자 베팅 + 휠 연출 |
| **P6** | 쓰리카드 4인 | S | P4 인프라 재사용 |
| P7(옵션) | 빈자리 NPC 봇(섯다 AI 재사용, 홀덤 AI 신규) | M | 유저 적을 때 테이블 활성화 |

각 Phase는 **dev 배포·인게임 검증 후 다음 진행**, prod는 명시 요청 시(접속자 0명 예외 규약 적용).

## 9. 결정 사항 (2026-07-11 유저 확정 — "나머지는 제안대로")

1. **홀덤 스테이크**: ✅ 노리밋 캐시게임 — 일반 테이블 SB/BB 5천/1만, 바이인 50만~200만 / 하이롤러 5만/10만, 500만~2000만.
2. **섯다 베팅**: ✅ **한국식 삥·하프·따당** + 1인 핸드 총 커밋 캡 앤티×20 (상세 §4-P2).
3. **레이크**: ✅ 홀덤 팟 5%(상한 BB×5), 섯다 승자 이익 5%.
4. **GUI 차단**: ✅ 게임별 물리 테이블 오픈 즉시 해당 게임 GUI 차단.
5. **화투 아트**: ✅ 자동 생성 미니멀 v1 (필요시 후속 폴리시).
6. 확정 수치는 P1 착수 시 **balance.md §20 (카지노)** 신설해 기록.

## 10. 진행 로그

- **2026-07-11**: 계획 수립·유저 승인(§9 전부 확정). casino/ GUI WIP 커밋(기준선). **P0 착수** — 카드 텍스처 67종(트럼프 A/B 2스타일 + 화투 13 + 뒷면 2) `casino-tools/gen_cards.py`로 생성, RP `assets/barkan/{textures,models,items}/card/` 통합. RP 배포는 아직(디스플레이 코드와 함께).
- **2026-07-11 (2)**: 트럼프 **스타일 A(빅 인덱스) 확정**, 카드 폭 0.3블록 확정. 칩 4종(천/만/십만/백만, elements 원반) `gen_chips.py` → RP 통합. **P0 자바 프레임워크 작성·컴파일 통과** (blockship 커밋: `casino/table/` 5클래스 — Registry/CardDisplayService/ChipRenderer/SeatService/Command). `/카지노테이블` OP 명령: 생성→좌석(서서)→존(상판 바라보고 shoe/board/pot)→`데모`(딜링→플립→칩→팟 슬라이드 수용 테스트)→철거. ★미배포 — 다음 dev 재시작 때 jar+RP 함께. 사운드는 바닐라 플레이스홀더(북페이지/노트블록), barkan ogg는 폴리시 단계에.
