# 7챕터 발주 — 심해 「Barkan Vs Deep Dark Sea」 (Lv50~70)

> **범위: 7챕터만.** 1~6챕터(튜토~왕도 봉인서고)는 **이미 완성돼 prod 라이브**다.
> 그쪽은 손대지 말 것 — 현재 상태는 [codex-handoff.md](codex-handoff.md) 「잔여」에 기록돼 있다.
>
> 서사 권위는 [story.md](story.md) 「7챕터 — 심해」. 이 문서는 **그 서사를 구현하는 지시서**다.
> 운영·배포 규칙은 [AGENTS.md](AGENTS.md).

---

## 0. 절대 규약

1. **작업 대상은 prod** (`ubuntu@168.107.8.107:~/mcserver/plugins/BlockShip/`).
   dev는 Citizens NPC가 적어 검증이 불가하다.
   ```bash
   scp -i ~/.ssh/oracle-mc.key ubuntu@168.107.8.107:~/mcserver/plugins/BlockShip/quests.json .
   ssh -i ~/.ssh/oracle-mc.key ubuntu@168.107.8.107 'cd ~/mcserver/plugins/BlockShip && cp quests.json quests.json.bak-ch7-$(date +%s)'
   scp -i ~/.ssh/oracle-mc.key quests.json ubuntu@168.107.8.107:~/mcserver/plugins/BlockShip/
   ```
2. **prod에서 JSON을 고치면 반드시 dev로 역동기화** — `deploy-blockship.sh`가 JSON 6종을 dev→prod로
   덮으므로, dev가 낡으면 다음 jar 배포에 수정이 조용히 되돌아간다. md5 일치 확인.
3. **리로드**
   | 명령 | 대상 |
   |---|---|
   | `/npc동기화` | `npc.json` + `dialogue.json` |
   | `/데이터리로드` | `quests.json` · `fish.json` · parts · enhance · recipes · materials · config |
   | ★`regions.json` | **콘솔 리로드 없음**(`/지역 리로드`는 Player only) → **서버 재시작으로만 반영** |

   `npc.json`을 올린 **직후** `/npc동기화` — `NpcManager.save()`가 메모리를 파일로 덮어쓴다.
4. **퀘스트 삭제 금지.** 플레이어 `questStatus`에 잔재가 남는다. 7챕터는 **신규 추가만** 한다.
5. **메인 퀘스트는 반드시 NPC `quests` 배열에 배정** — 미배정 = 영구 수락 불가 (§7).
6. `&l`·`§l` **볼드 금지**(전역 훅 차단). NPC 이름표 색: 퀘스트 주면 `&a`+`[Q]` · 기능형 `&b` · 대화만 `&f`.
7. jar 배포는 `~/deploy-blockship.sh`, **접속자 0명일 때만**. `plugman reload` 금지.
8. **인게임 텍스트는 전부 한글.** 챕터 타이틀만 영문 「Barkan Vs Deep Dark Sea」.

---

## 1. 연결 지점

```
6챕터 왕도15 「왕에게 진실을」  Lv50 · 칭호 `심연의증인` · 다음퀘스트 = (없음)
        ↓  ★여기에 심해01을 붙인다
7챕터 심해01 …
```

| 항목 | 값 |
|---|---|
| `왕도15.다음퀘스트` | 현재 `null` → **`심해01`** |
| `심해01.선행퀘스트` | **`왕도15`** |
| 퀘스트 ID 규칙 | 기존 컨벤션(지역명 접두) 따라 **`심해01`~`심해NN`** |
| 레벨 밴드 | **Lv50 → 70** |

---

## 2. 확정 서사 (변경 금지)

상세는 [story.md](story.md) 「7챕터 — 심해」. 요지만:

- **적 = 심해 교단.** 새 세력이 아니다. 교단은 **사이비**이고 잠든 심연어를 깨우는 것이 유일한 목적.
  1~6챕터의 물 오염·조각 수집·기록 검열·상단 결탁이 전부 **소환 의식의 준비**였다.
- **심해전왕 = 교단이 마침내 소환해낸 고대 심연어 중 하나.**
  6챕터에서 밝혀진 「지워진 세 이름」 중 하나가 실제로 불려나온 것.
  → 대도서관이 이름을 지운 이유가 옳았음이 증명되고, **이미 늦었다.**
  → 남은 둘의 이름은 **7챕터에서도 밝히지 않는다** (후속 챕터 씨앗).
- **소집 = 왕명 + 낚시사 길드.** 모험가길드 같은 새 조직을 만들지 않는다.
- **탐험대 = 기존 인물 재활용** — 왕립 대사서 · 사피르(사막, 심연어 연구자) ·
  첨탑의 은둔 학자 · 세르간 · 길드장 하겐.
- **칭호 2종** — `심해탐험대`(1막, 소속 증명) · `바다의영웅`(최종막, 심해전왕 격파).

### ★무기 축 전환 — 이 챕터의 정체성

```
1~6챕터  낚싯대 — 물가에서 찌를 던져 기억을 읽는다
7챕터    작살  — 물속에 들어가 사냥한다   (낚싯대 병행 가능)
```

작살은 `HarpoonManager.inWater(p)`(발밑+눈높이 **둘 다 물**)로 **완전 수중 전용**이라
심해가 유일한 제 무대다. 지금까지 메인에서 한 번도 쓰이지 않았다.
**7챕터 목표에 `harpoon` verb를 적극적으로 쓸 것** — 그게 이 챕터를 앞 6개와 구분한다.

낚싯대도 계속 쓸 수 있다(유저 지시). 스킬트리 잠영(수중호흡)이 창 유무와 무관하게 적용되므로
(`BlockShipPlugin:3046`), 심해 장비를 갖춘 뒤엔 손에 낚싯대를 들어도 심해에 머물 수 있다.

### 진행 구조 — 장비가 깊이의 통행권

7챕터 진행 = **심해 장비를 단계적으로 갖추는 과정**. 각 단계의 장비가 더 깊은 곳을 열고,
최종 전투 전에 풀세트가 완성된다 — "최종전투 준비"가 곧 챕터 진행이다.

| 막 | Lv | 무대 | 얻는 것 |
|---|---|---|---|
| 1막 출항 준비 | 50–53 | 왕도·길드 회관·은빛 갈매기호 | 탐험대 소속(`심해탐험대`는 2막 끝). 작살로 축 전환 |
| 2막 원양 | 54–58 | 열도 밖 먼 바다 | 항해(`sail`). 고래·심해어 어장 |
| 3막 검은 섬 | 58–62 | 안개 해역 · 왕도 | 안개는 날씨가 아니라 경계였다. 지워진 셋째 「레비아」 해독 |
| 4막 강하 준비 | 62–66 | **심해 협곡**(바다 한가운데) | 입구 발견 + **재료 수집·제작이 축**. 수중호흡·야간투시 풀세트 |
| 5막 강하 | 67–68 | 결계 → **심해 월드** | **심해어의 가면**으로 결계를 속이고 아래로 |
| 6막 무명의 성소 | 68–69 | **심해 교단 본부** | 사육 수조(오염의 근원) · 기록고(지워진 이름들의 원본) |
| 7막 결전 | 69–70 | 성소 아래 균열 | 볼프람 → 소환 의식 → 레비아 → `바다의영웅` |

### ★입구 설정 (2026-08-14 확정 — 이전 지시서 내용을 덮어씀)

- **폭포 뒤 동굴은 심해와 무관하다.** 3~5층을 강하 통로로 쓰라던 옛 지시(§4①)는 **폐기**.
  그 동굴은 이무기·꽃으로 가는 사이드 계단이고 메인은 지나가지 않는다.
- **입구 = 바다 한가운데의 협곡**(`심해협곡`). 해도에 없는 해구이고 배로만 닿는다.
- **협곡 아래는 별도의 심해 월드다.** 기존 `world`의 깊은 물이 아니다.
- **입구에 결계가 있다** — 심해어의 기척만 통과시킨다. **「심해어의 가면」**(사역어 비늘로 제작)을
  쓰고 넘는다. 가면은 위장구지 전투 장비가 아니다 — **스탯을 주지 않는다.**
- **심해 월드 안에 교단 본부 「무명의 성소」가 있다**(2026-08-14 추가, 건축 중).
  6·7막의 무대이자 이 챕터의 종착점 — 문지기 회랑 / **사육 수조**(열도 오염의 근원) /
  **기록고**(훔친 사본 + 지워진 세 이름의 원본) / 대사제의 방 / 소환 의식장.
  ★현재 설계는 **성소 NPC 0** 전제다(신도 NPC를 두려면 대사·스킨 비용이 붙는다).

---

## 3. 그대로 재사용할 자산 — 새로 만들지 말 것

| 자산 | 상태 | 쓰는 곳 |
|---|---|---|
| **`원양어선` 어종 181종** | `fish.json`에 **이미 정의** — E28 D20 C22 B26 A28 S24 M10 L15 G8. 시간/날씨 서브키(`낮`·`밤`·`낮비`·`밤비`·`이벤트`·`통발`)까지 갖춤 | 2·3막 심해 어장. **지역에 붙이면 공짜** |
| **`ImugiBattle` v4** | `boss/` 2,095줄. 회피+약점 낚시로 딜, 공격 4종(가시볼트·수압탄·브레스·브리칭), 페이즈, 보스바, 관전 모드, 전리품 확률 롤 | **심해전왕에 틀 복제.** dev flatroom 호수 **반경 14**에서 돌아감 = 아레나는 작은 물이면 충분 |
| **작살 52종** | `수중호흡:5~70` + `수영속도`·`공격력`·`공격속도`·`돌진쿨감` 스탯 보유. 등급 하한 `HarpoonManager.breathFloor(grade)` | 심해 통행권·전투 |
| **`harpoon` goal verb** | 2026-08-07 신설. `HarpoonListener`가 `onFish` 옆에서 `onHarpoon` 호출 | "작살로만" 목표 강제 |
| **`sail` goal verb** | 2026-08-07 신설. `ShipMover.move()` 성공 시 이동 블록 누적 | 2막 항해 목표 |
| 안개·날씨·BGM·파티클 | `WeatherManager` `setViewDistance` `bgm.json` | 심해 분위기 — 별도 구현 불필요 |

**1~3막은 건축 0이다** — 기존 건물 + 먼 바다뿐. 신규 건축은 **4막 협곡 · 5막 심해 월드 ·
6·7막 교단 본부** 셋뿐이고, 앞의 둘은 건물이 아니라 지형·어둠이다.
**실제 건물은 「무명의 성소」 하나** — 이 챕터의 건축 예산은 거기 몰아 쓰면 된다.

---

## 4. 신설해야 할 것

### ① 지역 등록 (`regions.json`)

| 지역 | 레벨 게이트 | 비고 |
|---|---|---|
| `원양` | 50 | 열도 밖 먼 바다. 2막 — **2026-08-14 등록 완료** |
| `대양` | 없음 | `dogam` 목표용 도넛 4단면 — **2026-08-14 등록 완료** |
| `안개해역` | 58 | 3막. 커스텀 날씨 `안개` 연동 |
| **`심해협곡`** | 62 | **4막 입구.** 바다 한가운데의 해구. `심해20` 목표가 참조 |
| **`심해교단본부`** | 68 | **6·7막.** 「무명의 성소」. `심해28` 목표가 참조 |
| `심해` (월드 지역) | 67 | **5막 도착점.** 별도 월드 — 월드 생성이 선행. `심해27` 목표가 참조 |
| 성소 아래 균열 | 69 | 보스 아레나 |

★**폭포 뒤 동굴 3·4·5층은 이 표에서 빠졌다** (2026-08-14). 심해와 무관해졌으므로 7챕터 작업 범위가
아니다. 1층 `requiredLevel` 0 → 30 수정은 **사이드 축 과제로 이관**한다.

★**`pos1=pos2=[0,0,0]` 스텁 금지.** 과거 통발 12개 지역 중 6개가 스텁이라 상점은 레시피를 파는데
설치가 불가능했던 사고가 있었다. 실제 좌표/폴리곤을 넣을 것.

★**`regions.json`은 콘솔 리로드가 없다** — `/지역 리로드`는 Player only. **서버 재시작으로만 반영된다.**

### ①-B 심해 월드 · 협곡 · 가면 ★2026-08-14 신설

| 항목 | 지시 |
|---|---|
| **심해 월드** | 별도 월드로 만든다. 이름 미정. `world`의 깊은 물로 대체하지 말 것 — 무대가 바뀌는 것 자체가 최종막의 신호다 |
| **cross-world TP** | `player.teleport()`는 **작동하지 않는다.** `execute in <dimension> run tp`, dimension key는 `Worlds.dimKey(worldName)` (`minecraft:<월드이름>`). 월드 이름을 그대로 쓰면 **조용히 무시**된다 |
| **협곡 → 월드 이동** | 협곡 바닥 진입 시 TP. 포탈(`portal/`) 패턴 재사용이 가장 싸다 |
| **결계** | 가면 미착용이면 진입 차단. `RegionTracker`의 진입 판정에 조건 하나를 얹는 형태 |
| **「심해어의 가면」** | 아이템 + 레시피 신설. 재료는 사역어(오염어) 비늘 계열 — `검은비늘붕어` 소재와 잇는다 |
| ★가면 스탯 | **주지 않는다.** 위장구지 장비가 아니다. 수중호흡·야간투시는 작살 담당 |

가면이 참조되는 목표는 두 곳이다 — `심해26`(`craft|심해어가면|1`) · `심해27`(`equip|심해어가면|1`).
**아이템이 없으면 이 둘이 하드블록**이 되므로 배포는 아이템 정의 이후에 한다.

### ①-C 낚시 특성 「작살 계열」과 `심해21` ★2026-08-14 (소스 실측)

> 출처: `skilltree/SkillTreeManager.java` `fishingTree()` — **플러그인 소스 직접 확인**.
> `skill-tree-dopamine-design.md` §9(2026-07-19)는 3계열 시절 문서라 **낡았다.**

**낚시 트리 = 근원 + 4계열 × 5노드.** 2026-08-03에 **작살 계열이 신설**됐고, 낚시 레벨 포인트를
공유하므로 *"낚싯대냐 작살이냐"* 라는 선택이 생긴다 — 7챕터의 무기 축 전환과 정확히 맞물린다.

| 계열 | 성격 | 노드 (→ = 선행) |
|---|---|---|
| 🎏 만선 | 물량 | 겹낚시15 → 삼중낚시12 → **풍어**(계열12) → 값어치10 → **만선**(계열30) |
| 🐋 대물 | 트로피 | 손맛 → 집중 → **거대어** → 크리배수 → **전설의 대어**(계열30) |
| 🌊 심해 | 귀한 것 | 어복15 → 승격12 → **승천**(계열12) → 제압15 → **용왕의 선물**(계열30) |
| 🔱 **작살** | **창으로 사냥한다** | **잠영15** → 찌르기12 → **급소**(계열12) → 완력10 → **떼사냥**(계열30) |

작살 계열 노드 상세 — **계열 최대 39포인트**:

| 노드 | 랭크 | 효과 |
|---|---|---|
| **잠영** | 15 | **수중호흡 +1.0초/랭크** ★창을 안 들어도 항상 적용(`WaterTeleportManager` 깊은물 타이머) |
| 찌르기 | 12 | 작살공격속도 +2.0%/랭크 |
| 급소 | 발동 (계열 12) | 낮은 확률로 남은 체력 무시하고 즉시 포획 |
| 완력 | 10 | 작살공격력 +0.5/랭크 |
| 떼사냥 | 발동 (계열 30) | 매우 낮은 확률로 한 창에 3마리, 전용 공개 연출 |

★2026-08-03 표시명 변경(노드 id는 저장키라 그대로): 미끼술→**어복** · 심안→**승천** · 끈기→**제압**.

#### `심해21` 목표 — 확정 서사와 라이브 값이 다르다

**확정 서사: 작살 계열에 20 이상 투자.** 급소(12)를 넘고 떼사냥(30) 앞이라 게이트로 알맞고,
Lv63 보유 포인트(=레벨-1=62)를 다른 계열과 나눠 쓰는 실제 선택이 된다.

그런데 **엔진이 아직 그걸 표현하지 못한다** — `QuestManager` 실측:

| | 확인된 사실 |
|---|---|
| 문법 | `skill\|<수>` — 2번째 필드가 목표치. **계열 인자가 없다** |
| 판정 | **누적 카운터다.** `onSkillInvest()`가 투자 성공 시 진행도 +1 (`QuestManager:1402`) |
| 진행 표시 | `"특성 포인트 투자: N/M회"` — 수락 이후의 투자 **횟수**를 센다 |
| 계열 구분 | 없다. **어느 트리 어느 계열에 찍어도 카운트된다** |

→ 그래서 **`skill|20`은 하드블록이다.** 포인트는 레벨당 1개(`(레벨-1) - 총투자`)라 Lv63→70 사이에
얻는 건 **7개뿐**이고, 그전에 포인트를 다 써 둔 플레이어는 20회를 채울 방법이 없다.

**필요한 확장** — 이미 조회 함수가 있어서 작다:

```java
// SkillTreeManager에 이미 존재: public int spentBranch(PlayerData d, String skillKr, String branch)
// goal: skill|<계열>|<수>  →  누적이 아니라 상태검사로 판정한다 (level|66 과 같은 성격)
case "skill" -> {                       // QuestManager.parse 쪽
    if (o.length >= 3) {                // skill|작살|20
        target = parseInt(get(o, 2));
        cur    = skillTree.spentBranch(d, "낚시", get(o, 1));
    } else target = parseInt(get(o, 1)); // 기존 skill|<수> 보존
}
```

| | 값 |
|---|---|
| 최종 목표 | `skill\|작살\|20` |
| 확장 전 라이브 | **`skill\|5`** — 7포인트 안에 들어가는 안전값. 텍스트로만 작살 계열을 가리킨다 |

★**확장 전에 `skill|작살|20`을 넣지 말 것** — 2번째 필드를 수로 읽으므로 `target = 0`,
수락 즉시 완료되는 사고가 난다(§5의 "알 수 없는 verb → 즉시완료"와 같은 계열).

### ② ★어종 풀 상속 규약 — 새 지역 만들 때 필수

`FishingListener`는 어종 풀을 이렇게 푼다:

```
getRegionFish(현재지역, "기본")  +  parentIsland 체인 상속 (최대 32단)
                                +  환경 어종(비/뇌우/커스텀날씨) + 시간대 풀
```

**자체 어종 정의가 없고 `parentIsland`도 없으면 어종 풀이 빈다.**
실제로 `상단기함`이 그 상태여서 배 위에서 낚싯대를 던져도 아무것도 안 나왔다
(2026-08-07 `parentIsland="상단마을"`로 수리).

→ 신설 지역은 **반드시** ⓐ `fish.json`에 자체 어종을 정의하거나 ⓑ 어종 있는 부모를 걸 것.
   심해 지역들은 **`원양어선` 181종을 `fish.json` 지역 키로 붙이는 것**이 가장 싸다.

### ③ 야간투시 스탯 신설

**수중호흡은 이미 구현돼 있다**(작살 52종 스탯 + `breathFloor` + 스킬트리 잠영).
**야간투시는 없다** — 심해가 어두우므로 이것만 신설이 필요하다.

- 작살(또는 심해 전용 부품)의 스탯으로 추가. 기존 스탯 파싱 경로를 따를 것
  (`이름|등급|가격|내구|스탯|레벨제한|출처`의 스탯 필드)
- ★`parts.json`은 **손편집 금지** — `fish-tools/gen_spear_builds.py` 등 **생성기를 고쳐 재생성**한다

### ④ 항해 시스템

`sail` goal verb(이동 블록 누적)는 있으나 **실제 원양 항해는 미구현**이다.
2막을 성립시키려면 배로 열도 밖으로 나가는 동선이 필요하다. `ship/`(ShipMover·ShipManager) 확장.

### ⑤ 심해전왕 보스

`ImugiBattle`을 틀로 복제. 조정 포인트:

| | 이무기 | 심해전왕 |
|---|---|---|
| 레벨 | Lv80 | **Lv70** |
| 위상 | 사이드 최종 | **메인 스토리 최종** |
| 무대 | 폭포 뒤 동굴 6층 (뭍) | **성소 아래 균열** (심해 월드) |
| 전리품 | 용비늘 · 여의주 | `심해전왕의핵` — `material` 훅으로 퀘스트 연동 |

전리품은 `crafting.questManager().onMaterial(viewer, "<재료명>")` 패턴을 그대로 쓰면
`material|<재료명>|1` 목표로 "전리품을 가져오라"가 성립한다 (이무기가 이미 그렇게 돼 있다).

#### 아레나 위치 = 성소 아래 균열 ★2026-08-14 확정

**교단 본부가 균열 위에 서 있다.** 협곡이 「심연 균열」의 입구이고, 교단은 소환 의식을 하려고
그 상처 바로 위에 성소를 지었다 — 성소가 왜 하필 거기 있는지가 이걸로 설명된다.
`심해32`(소환 의식 저지)와 `심해33`(레비아)이 같은 장소에서 이어진다.

건물 **안**이 아니라 **아래**인 이유: ①레비아는 균열에서 올라온다 ②최종 보스를 방 안에 넣으면
규모가 죽는다 ③`ImugiBattle`이 열린 물 공간을 요구한다.

| 요구 | 값 |
|---|---|
| 크기 | **반경 14 물 공간**이면 충분 (dev flatroom 호수 실측). 접촉 히트박스는 수평 3.4·수직 3.2 |
| 수중 | **완전 수중이어야 한다** — 작살이 `inWater`(발밑+눈높이 둘 다 물) 조건이라 반쯤 물이면 창을 못 쓴다 |
| 격리 | `ImugiBattle`은 **1인 인스턴스전**이다. 다른 플레이어가 끼어들지 않도록 균열 바닥을 별도 공간으로 두고 진입 시 TP |
| 소환 위치 | 유저 지시대로 **임시**(`/심해전왕 전투`). 성소 건축이 끝나면 고정 좌표로 교체 |

★★**호흡 소진 처리를 반드시 설정할 것 — 심해 월드엔 육지가 없다.**
`WaterTeleportManager`는 제한초 초과 시 ①`findNearestLand`(flood-fill, 201칸에서 중단) →
②`waterteleport.json`의 **rescue 폴백 좌표** → ③둘 다 없으면 **TP하지 않고**
`"근처 육지를 찾지 못했습니다 — 육지 쪽으로 이동하세요!"` 액션바만 반복한다(타이머 유지).

즉 육지 없는 월드에서는 **페널티가 사실상 사라지고 안내만 깜빡인다.**
심해 월드용 rescue 좌표(예: 성소 입구 또는 은빛 갈매기호 갑판)를 넣어야 "숨이 끊기면 끌려 나온다"가
성립한다. 3,000원 차감은 그대로 걸린다.

### ⑥ 퀘스트 **25~30개**

**밀도 근거**: 곡선 `Lv20→50 ramp ×8` 기준 **Lv50→70이 292시간**(중반빌드)이다.
6챕터가 10개로 Lv40~50을 커버해 **109분/퀘**가 됐던 밀도 붕괴가 여기서 더 크게 반복된다.
다른 챕터가 10~21분/퀘이므로 최소 25개, 권장 30개.

1회성 퀘스트가 Lv50 요구량의 47%만 담당하는 설계이므로 **후반은 일일 퀘스트로 메꾼다** — 의도된 것이다.

---

## 5. 목표 문법 팔레트 (28종)

**★목록에 없는 verb를 쓰면 `target = 0`이 되어 퀘스트가 즉시 완료된다.**

| verb | 문법 | 목표치 위치 |
|---|---|---|
| **`harpoon`** ★7챕터 주력 | `harpoon\|어종\|등급\|수\|최소크기` | 4번째 · **작살로만 집계** |
| **`sail`** ★2막 | `sail\|블록거리` | 2번째 |
| `fish` | `fish\|어종\|등급\|수\|최소크기` | 4번째 · 낚싯대·작살 **둘 다** 집계 |
| `fish_fresh` | `fish_fresh\|어종\|등급\|수\|최소크기\|최소신선도` | 4번째 |
| `mine` `harvest` | `<verb>\|대상\|수` | 3번째 |
| `submit` | `submit\|횟수` | 2번째 |
| `area` `visit` `action` | `<verb>\|대상` (visit은 +표시명) | 항상 1 |
| `dogam` | `dogam\|지역,지역\|종수` | 3번째 |
| `craft` `material` `eatdish` `forage` `equip` | `<verb>\|대상\|수` | 3번째 |
| `deliver` | `deliver\|부품명\|수` (인벤 즉석 판정, **무손상만**, 완료 시 회수) | 3번째 |
| `sell` `money` `earn` `login` `quest_daily` `trap` `enhance` `skill` `collectible` `level` | `<verb>\|수` | 2번째 |
| `casino` | `casino\|키\|수` | 3번째 |

**등급 표기**: `B` = "B 이상" · `C~B` = **정확히 C~B 구간**(A 이상 카운트 안 됨) · `아무` = 전부.

### 목표 다양성 하드 룰

> **"N등급 N마리"만 반복하지 않는다.**

1. 같은 verb **3연속 금지**
2. `fish` 단순형(어종 `아무` + 최소크기 0 + 단일 목표)은 **막당 최대 1회**
3. 막마다 **처음 등장하는 축**을 최소 1개
4. 최종막 앞 게이트는 **복합(AND) 3중 이상**
5. 서사에 맞는 verb — 항해 = `sail` · 심해 사냥 = `harpoon` · 표본 채집 = `dogam` · 장비 준비 = `equip`/`deliver` · 전리품 = `material`

참고: 1~6챕터 메인의 `fish` 비중은 34%다. 7챕터는 `harpoon`이 그 자리를 대신해야 한다.

---

## 6. 검증 스니펫 — 단계마다 실행

```python
import json
Q = json.load(open('quests.json'))['퀘스트']
N = json.load(open('npc.json'))['npcs']
R = json.load(open('regions.json'))
F = json.load(open('fish.json'))
OK = {'fish','fish_fresh','harpoon','area','visit','action','dogam','sell','money','earn','login',
      'quest_daily','craft','deliver','material','forage','eatdish','equip','trap','enhance',
      'skill','collectible','casino','level','mine','harvest','sail','submit'}
asg = {q for v in N.values() for q in (v.get('quests') or [])}

print('불량 verb   :', [(k,g) for k,v in Q.items() for g in v.get('목표',[]) if g.split('|')[0] not in OK])
print('메인 미배정 :', [k for k,v in Q.items() if v.get('카테고리')=='메인' and k not in asg])
print('체인 끊김   :', [k for k,v in Q.items() if v.get('다음퀘스트') not in (None,'-','') and v['다음퀘스트'] not in Q])
print('선행 끊김   :', [k for k,v in Q.items() if v.get('선행퀘스트') and v['선행퀘스트'] not in Q])

# 신설 지역이 실제 좌표를 갖는지 + 어종 풀이 확보되는지
for a in ['원양','대양','안개해역','심해협곡']:   # + 심해 월드 지역(월드 생성 후 추가)
    v = R.get(a)
    if not v: print(f'  {a}: ★regions.json 없음'); continue
    stub = v.get('pos1')==[0,0,0] and v.get('pos2')==[0,0,0] and not v.get('polygon')
    own = a in F['regions']
    c, chain = v.get('parentIsland') or '', []
    for _ in range(8):
        if not c: break
        chain.append(c); c = (R.get(c) or {}).get('parentIsland') or ''
    pool = own or any(x in F['regions'] for x in chain)
    print(f'  {a}: 좌표={"★스텁" if stub else "OK"} 어종풀={"OK" if pool else "★없음"} lv={v.get("requiredLevel")}')
```

**앞 4줄은 빈 리스트, 지역은 전부 좌표 OK · 어종풀 OK**여야 한다.
그리고 `왕도15.다음퀘스트 == '심해01'` 확인.

---

## 7. ★수락 메커니즘 — 반드시 알아야 함

- `다음퀘스트`는 다음 퀘스트를 **`대기`(해금)로만** 바꾼다. 수락이 아니다.
- `대기`→`진행` 경로는 **단 하나** — `QuestGui.clickNpc` → `QuestManager.accept()`.
  즉 **그 퀘스트를 `quests` 배열에 가진 NPC와 대화**해야 한다.
- `/퀘스트` 저널엔 수락 버튼이 없고, `VillageQuestGui`·`QuestCatalogGui`는 읽기 전용이다.

**`npc.json`의 어느 NPC에도 배정되지 않은 메인 퀘스트는 영구히 수락 불가다.**
실제로 `튜토11`→`본섬01`에서 체인이 죽어 prod 플레이어가 `본섬01 = 대기`로 막혀 있었다.

한 NPC가 여러 메인을 들 때 순서는 `선행퀘스트`가 보장한다(GUI가 선행 미완을 건너뜀).
`quests` 배열은 **메인을 사이드보다 앞에** 둘 것 — GUI가 첫 유효 항목 1개만 노출한다.

완료 방식: 타입 `물고기`류 → 목표 달성 후 **수여 NPC에게 반납**(`완료대기`→보상, 자동 네비) /
타입 `지역` → 도착 즉시 자동 완료.

---

## 8. 함정

- **`regions.json`은 서버 재시작으로만 반영된다.** 리로드 명령이 없다.
- **스텁 좌표 금지** (`pos1=pos2=[0,0,0]`). 데이터에 있어도 실제로 없는 지역이 된다.
- **어종 풀 상속** — 자체 정의 없으면 부모 필수 (§4-②).
- `earn` `login` `quest_daily`는 **주간 카운터**를 읽는다. 메인에 쓰면 주간 리셋에 진행도가 날아간다.
- `deliver`는 완료 시 아이템을 **회수**하고 **무손상 부품만** 인정한다.
- `fish`는 낚싯대·작살 둘 다 집계. **작살 전용은 `harpoon`.**
- `parts.json` 손편집 금지 — `fish-tools` 생성기가 권위.
- 작살은 **완전 수중 전용**(`inWater`: 발밑+눈높이 둘 다 물). 육상 보스는 작살로 못 잡는다.
- 파티가 없어 **1인 기준 밸런싱**만 가능하다. `ImugiBattle`도 1인 인스턴스전이다.
- 돌진·넉백에 **GrimAC 면제**가 필요하다(작살 dash가 이미 `addAttachment`로 처리 중 — 같은 패턴 따를 것).

---

## 9. 현재 수치 (참고)

| 항목 | 값 |
|---|---|
| 퀘스트 / NPC / 지역 / 어종 | 257 / 157 / 32 / 565 |
| 6챕터 종점 | `왕도15` Lv50 · 칭호 `심연의증인` |
| 경험치 곡선 | `Lv20→50 ramp ×8` (Lv50 need 29,432) |
| Lv50→70 소요 | **292시간** (중반빌드) — 7챕터 분량 근거 |
| 1회성 퀘스트 담당 | Lv50 요구량의 47% (나머지는 일일·낚시) |
| 일일 하루 상한 | 쉬움 210 / 보통 375 / 어려움 750 / 전문 1,500 (주간은 XP 0) |

## 10. 미결 (설계 판단 필요 — 구현 전 확인)

> ★아래 4건 중 3건은 §11에서 해소됐다. **남은 미결은 좌표 1건뿐**이다.

- ~~지워진 이름 셋 중 어느 것이 소환됐는지~~ → **「레비아」**(심해·압력) 확정
- ~~심해 교단 지도자 캐릭터 유무~~ → **대사제 볼프람** 확정 (NPC 미생성, 텍스트+보스전)
- ~~호흡 실패 시 처리~~ → `water/WaterTeleportManager`가 이미 처리 (육지 강제 TP + 3,000원, 사망 아님)
- **심해 협곡·심해 월드의 실제 좌표** — 건축 진행 중. 협곡은 바다 한가운데, 월드는 별도 월드로 확정됐고
  좌표만 미정이다. ★건축 중인 땅에 레벨 게이트를 먼저 씌우지 말 것(OP도 `rejectEntry`로 되밀린다)
- **볼프람의 퇴장** — 심해27에서 대면만 하고 결말이 없다. 소멸·도주·생포 중 미정 (후속 챕터 씨앗)

---

## 11. 구현 기록 (2026-08-08, Claude) — 퀘스트·인물 완료 / 시스템 잔여

§10 미결 중 **①지역 좌표는 유저가 waive**("좌표는 주지말라고 그냥 임시위치에 소환하라할거야")했고,
**④호흡 소진 처리는 이미 코드에 답이 있었다** — `water/WaterTeleportManager`가 수중호흡 초 소진 시
육지 강제 TP + 3,000원 차감(사망 아님)을 이미 한다. 그래서 「1000초 호흡 장비」가 그대로 정답이다.
②③은 아래대로 확정했다.

### 확정한 서사 값

| 항목 | 확정 |
|---|---|
| 심해전왕 정체 | 지워진 세 이름 중 **첫째 「레비아」** — 심해·압력의 심연어. 교단이 소환한 개체 |
| 남은 두 이름 | 여전히 봉인 (`심해30` 마지막 줄에 후속 훅으로 남김) |
| 교단 지도자 | **대사제 볼프람** — NPC 미생성. 무대(심해)가 없으므로 퀘스트 텍스트 + 보스전으로만 등장 |
| 교단 내부자 | 신규 NPC 없이 **6챕터에서 정체를 밝힌 차석 사서를 수감 심문**으로 재사용 |
| 무대 | **은빛 갈매기호(지역 `상단기함`, 표시명 그대로) = 전진기지.** 1~3막 신규 건축 0 |
| 심해 입구 (2026-08-14) | **바다 한가운데의 협곡 `심해협곡`.** 폭포 뒤 동굴은 무관 — 옛 강하 통로안 폐기 |
| 심해 무대 (2026-08-14) | 협곡 아래 **별도 심해 월드**. 기존 `world` 깊은 물이 아니다 |
| 통행 조건 (2026-08-14) | 입구 결계는 심해어의 기척만 통과 → **「심해어의 가면」** 제작·착용. 스탯 없는 위장구 |

★유저 확인: "은빛갈매기호 좋다 거기서 진행하는걸로 하자 상단마을과 자연스럽게 연결되고 좋네"

### 무대 — 기존 건물만 사용 (실측)

| 무대 | 좌표 | 배역 |
|---|---|---|
| 은빛 갈매기호 (`상단기함`) | 박스 x1137–1252 / y55–130 / z−35–10, 선체 가문비 5,554블록·돛 흰양털 826 | 마테오(1195.5,65,−11.5) 엔리코(1148.5,57,−11.5) 피노(1170.5,57,−10.5) 조반니(1198.5,55,−11.5) 이자벨라(1220.5,57,−10.5) 니코(1187.5,127,−10.5 망대) + **신규 2인** |
| └ 갑판 아래 통로 | x1170–1171 · z−13~−10 · 바닥 가문비 반블록 y56 | **테클라 (1170.5,57,−12.5) · 하르트무트 (1171.5,57,−13.5)** |
| 낚시사 길드 회관 | 하겐 443.5,85,817.5 (이미 회관 안 — 「잔여」 이동 항목 불필요) | 하겐 |
| 왕도 대도서관 | — | 국왕 · 대사서 · 은둔학자 · 차석사서 |
| 스폰도시 대장간 | — | 펠릭스 (**npc 키 = `대장간안내`**) |

★함정: story.md가 대장간을 "펠릭스·군터"로 적었으나 **군터는 `&b[대장간]` 기능형**이라 퀘스트를 줄 수 없다.
펠릭스의 npc 키는 이름이 아니라 `대장간안내`다.

### 신규 인물 2인

| npc 키 | 표시명 | CID | 역할 |
|---|---|---|---|
| `테클라` | `&a[Q] 심해조사관 테클라` | **174** | 낚시사 길드 파견. 7챕터 주 안내자 (6개) |
| `하르트무트` | `&a[Q] 잠수장 하르트무트` | **175** | 심해 강하·호흡 장비 (4개) |

CID 174·175는 **dev·prod 양쪽에서 비어 있음을 확인하고** 잡았다 (prod는 167~173이 이미 점유,
`last-created-npc-id` 173 → 175). `saves.yml` 직접 주입 + 양쪽 동일 번호 — `deploy-blockship.sh`가
`npc.json`을 dev→prod 단방향 동기하므로 번호가 어긋나면 조용히 다른 NPC를 가리킨다.

### 퀘스트 30개 — `심해01`~`심해30`

`왕도15`(Lv50, 칭호 `심연의증인`) `다음퀘스트` `null` → **`심해01`** 로 연결. 5막 구조.

| 막 | Lv | 퀘스트 | 요지 |
|---|---|---|---|
| 1 출항 준비 | 50–53 | 01~07 | 왕명 → 길드 총동원 → 압류된 배 인수 → 선원 재소집 → 작살 훈련 → 출항 |
| 2 원양 | 54–58 | 08~13 | 오염어 표본 → 고래의 길 → 안개해역 돌파 (칭호 `심해탐험대`) |
| 3 검은 섬 | 58–62 | 14~19 | 교단 제단 → 차석 사서 심문 → **지워진 셋째 「레비아」 해독** |
| 4 강하 준비 | 62–66 | 20~25 | **바다 한가운데의 협곡** 발견 → 압력 훈련 → 심해 작살 벼림 → 천 초의 숨 → 어둠을 보는 눈 → 강하 허가 |
| 5 강하와 결전 | 67–70 | 26~30 | **심해어의 가면** 제작 → 결계 통과·심해 월드 강하·대사제 볼프람 → **`Barkan Vs Deep Dark Sea`** (칭호 `바다의영웅`) → 개선 |

- **경험치**: Lv50→70 요구 1,228,200 중 **577,200 (47.0%)** 배분. 필요레벨의 요구량에 비례(9,100 → 42,100).
  1~6챕터와 같은 47% 원칙 유지 = 나머지 53%는 낚시·일퀘로 메워야 한다.
- **보상돈**: 60,000 → 200,000 (6챕터 종점 50,000에서 이어 올림)
- **목표 다양성**: 35개 목표에 verb 14종. **순수 낚시(`fish`/`fish_fresh`) 5개 = 14%**
  (1~6챕터 메인 평균 34%보다 낮다). `harpoon`(작살 전용) 6 · `visit` 8 · `sail` 3 · `dogam` 3 ·
  `material` 3 · `money`/`level`/`skill`/`craft`/`enhance`/`forage`/`area` 각 1
- **신규 칭호 2종**: `심해탐험대`(하트 오브 더 시) · `바다의영웅`(앵무조개 껍데기)

재현 스크립트: [fish-tools/build_ch7_quests.py](fish-tools/build_ch7_quests.py) — 곡선을
`FishingLevelManager.NEED_TABLE`에서 직접 읽어 경험치를 재배분한다. 손으로 고치지 말고 이걸 돌릴 것.

### 배포·검증 결과

dev·prod 양쪽 `quests.json` `npc.json` `dialogue.json` `fish.json` `parts.json` `recipes.json` `materials.json` `enhance.json` `titles.json` **md5 9/9 일치**.

| 검증 | 결과 |
|---|---|
| 체인 (선행/다음 30단) | ✅ 끊김 0 · `왕도15`→`심해01` 연결 |
| NPC 배정 | ✅ 30/30 배정 (미배정 = 영구 수락 불가) |
| 필요레벨 단조 | ✅ |
| 목표 문법·필드 수 | ✅ 알 수 없는 verb 0 (verb 오타 → target 0 → 즉시완료 버그 회피) |
| 참조 실재 (어종·지역·칭호·visit 대상) | ✅ |
| 볼드 `&l` | ✅ 0건 |
| 기존 257개 보존 | ✅ (삭제 0, 287개) |
| prod 로드 | ✅ `Loaded 287 quests` · `Loaded 159 npcs, 113 dialogue sets` |
| Citizens 등록 | ✅ **158 → 160명** (재시작 전후 로그 대조) |
| dev 로드 | ✅ 287 quests · 159 npcs · Citizens 160 |

### ★구현 완료 기록 (2026-08-08)

| # | 항목 | 영향 | 비고 |
|---|---|---|---|
| 1 | **목표 진행도 텍스트** | ✅ 완료 | `QuestManager.getProgressText`에 `harpoon`·`fish_fresh`·`mine`·`harvest`·`sail`·`submit`·`deliver` 표시 추가 |
| 2 | **`야간투시` 스탯** | ✅ 완료 | 작살 버프 적용/해제, 장비·도감 표시를 연결하고 심해24 진행에 사용 |
| 3 | **심해 작살 티어** | ✅ 완료 | `fish-tools/gen_spear_builds.py` 생성기에서 S급 3종(수중호흡 1000초, 야간투시) 생성; `parts.json` 손편집 없음 |
| 4 | **심해전왕 보스** | ✅ 완료 | `ImugiBattle` 프레임 재사용, `/심해전왕 전투` 임시 위치 소환, `심해전왕의핵` 보상 및 심해28 연결 |
| 5 | **신규 2인 스킨** | ✅ 완료 | Citizens 174 테클라·175 하르트무트에 MineSkin URL 적용; 재시작 후 textureRaw 해시 확인 |
| 6 | 심해 지역 | 🔄 재설계 | 2026-08-14 협곡·월드 확정으로 대상이 바뀜 — `심해협곡` + 심해 월드. 폭포 뒤 동굴 목표는 제거됨 |
| 7 | 인게임 클릭 검증 | ⏸ 대기 | 접속자 0명으로 미수행. 배 갑판 아래에서 두 NPC 우클릭·대화·퀘스트 수락 확인만 남음 |

재배포 후 prod에서 `npc동기화`와 `데이터리로드`를 실행했다. dev·prod 핵심 데이터 9종의 md5가 일치하며,
검증 스니펫의 불량 verb·메인 미배정·체인 끊김·선행 끊김은 모두 빈 리스트다.

---

## 12. 2026-08-14 개편 — 입구 재설정 + 데이터 회귀 봉합

유저 지시로 **심해 입구가 바뀌었다.** 폭포 뒤 동굴은 심해와 완전히 무관해졌고,
입구는 **바다 한가운데의 협곡**, 그 아래는 **별도의 심해 월드**, 통행 조건은 **심해어의 가면**이다.
보스 구현은 뒤로 미루고 **서사·퀘스트를 먼저 100% 확정**하는 단계다.

### 퀘스트 변경 3건 (`build_ch7_quests.py`)

| 퀘스트 | 이전 | 현재 |
|---|---|---|
| `심해20` 「바다 한가운데의 협곡」 | `area\|폭포_뒤_동굴_2층` · "문은 폭포 뒤에 있소" | `area\|심해협곡` · "동굴을 찾고 있었다면 헛수고요. 문은 뭍에 없소" |
| `심해26` 「심해어의 얼굴」 | 소환 의식 저지 (`harpoon\|S\|10`) | `harpoon\|S\|10` + **`craft\|심해어가면\|1`** — 사역어 비늘로 가면 제작 |
| `심해27` 「가면을 쓰고, 문을 넘다」 | 대사제 볼프람 (`harpoon\|M\|1`) | **`equip\|심해어가면\|1`** + `harpoon\|M\|1` — 결계 통과·월드 강하·볼프람 대면 |

`심해25`(강하 허가)는 목표 그대로, 텍스트만 "이자벨라가 협곡 위에 배를 세운다"로 무대를 맞췄다.
**퀘스트 수·레벨 밴드·경험치 배분은 불변**(30개 / Lv50~70 / 577,200 = 47%) — 목표와 텍스트만 교체했다.

### 데이터 회귀 봉합 — 생성기가 라이브를 되돌리던 문제

8/14 P0 수리는 `quests.json`을 **직접** 고쳤는데 생성기는 옛 값을 그대로 들고 있었다.
그대로 재실행했다면 두 수리가 조용히 되돌아간다:

| 항목 | 생성기 옛 동작 | 봉합 |
|---|---|---|
| `심해01.선행퀘스트` | `i > 0`일 때만 설정 → **첫 퀘스트는 선행 없음** = 5·6챕터 스킵 가능 | `i == 0`이면 `왕도15` |
| `심해17` 목표 | `dogam\|기억의연못\|6` — 연못 어종은 5종뿐 = 영구 미완 | `dogam\|기억의연못\|5` |

이제 생성기가 유일한 권위다. `quests.json` 손편집 금지.

### 배포 전 선행 조건 — 이게 없으면 새로 하드블록이 생긴다

- [ ] `심해협곡` 지역 실좌표 (`pos1=pos2=[0,0,0]` 스텁 금지) — `심해20`이 참조
- [ ] 심해 월드 생성 + cross-world TP(`Worlds.dimKey`) — `심해27` 이후 전부
- [ ] **「심해어의 가면」** 아이템 + 레시피 — `심해26`(`craft`)·`심해27`(`equip`)가 참조
- [ ] 협곡 입구 결계 판정 (가면 미착용 차단)
- [ ] `심해전왕의핵` dropTable = 보스 (기존 하드블록, 이번 범위 밖)

★위 4건이 끝나기 전에는 **`quests.json`에 올리지 말 것.** 지금 라이브는 옛 목표(`폭포_뒤_동굴_2층`)라
적어도 진행은 되지만, 새 목표를 자산 없이 올리면 `심해20`부터 즉시 멈춘다.

### ★문서 간 불일치 (미해소 — 실측 필요)

§11의 8/08 기록은 **야간투시 스탯(#2)** 과 **심해전왕 보스(#4)** 를 ✅ 완료로 적었으나,
8/14 라이브 전수조사(story.md)는 둘 다 미완으로 판정하고 `심해전왕의핵`이 **어떤 dropTable에도 없다**고
기록했다. 생성기 수준에서 확인되는 것은 **심해 작살 3종(#3)뿐**이다(`gen_spear_builds.py` DEEP_SPEARS).
플러그인 소스 또는 prod 실측으로 판정할 것 — **§11의 ✅를 근거로 완료 처리하지 말 것.**

미확인 항목 하나 더: 테클라·하르트무트의 `dialogue.json` 대화 트리 존재 여부(§11 검증표에 없음).


---

## 13. 2026-08-14 — 교단 본부 「무명의 성소」 추가 (30 → 35퀘)

유저 지시: *"심해협곡에서 들어가면 심해교단 본부가 나올 거고 그걸 지을 건데 거기에도 퀘가 좀 있어야 할 듯"*.
5막(강하와 결전) 하나였던 뒤끝을 **5막 강하 / 6막 성소 / 7막 결전**으로 쪼개고 5개를 늘렸다.

### 왜 ID를 새로 안 붙이고 28~30을 갈아끼웠나

`심해31~35`를 **뒤에 붙이고** 옛 종결부(레비아·개선·마무리)를 그대로 두면 ID 순서와 진행 순서가
어긋난다(성소가 31~35, 결말이 28~30). 그래서 **28~30의 내용을 성소 3단으로 교체**하고
옛 종결부를 31·34·35로 옮겼다. ID가 진행 순서와 일치한다.

- **삭제는 없다**(§0-4 준수). 기존 30개 ID는 전부 살아 있고 내용만 바뀐다.
- 안전한 이유: prod 최고 레벨이 Lv17대라 **7챕터 진행자가 0명**이다. 지금이 갈아끼울 수 있는 마지막 시점에 가깝다.

### 5·6·7막 구성

| ID | Lv | 수여 | 무대 | 목표 |
|---|---|---|---|---|
| `심해26` | 67 | 테클라 | 협곡 입구 | `harpoon\|A\|10` + `craft\|심해어가면\|1` |
| `심해27` | 67 | 하르트무트 | 결계 → 심해 | `equip\|심해어가면\|1` + `area\|심해` |
| `심해28` | 68 | 테클라 | **성소 문지기 회랑** | `area\|심해교단본부` |
| `심해29` | 68 | 테클라 | **사육 수조** | `harpoon\|A\|15` |
| `심해30` | 68 | 하르트무트 | **기록고** | `material\|무명의낱장\|1` |
| `심해31` | 69 | 하르트무트 | 대사제의 방 | `harpoon\|M\|1` |
| `심해32` | 69 | 테클라 | 소환 의식장 | `harpoon\|S\|12` |
| `심해33` | 69 | 테클라 | 균열 | `material\|심해전왕의핵\|1` · 칭호 `바다의영웅` |
| `심해34` | 70 | 국왕 | 왕도 | `visit\|국왕` |
| `심해35` | 70 | 하겐 | 길드 회관 | `visit\|하겐` |

- 경험치 총액은 **577,200(47%) 그대로**다 — 생성기가 필요레벨 비례로 재배분하므로 퀘당 몫만 줄어든다.
- `심해26`의 작살 등급을 S→**A**로 내렸다. `심해32`(S 12)와 겹쳐 단조로워지는 것을 피하고 난이도를 올림차순으로 뒀다.
- 볼프람이 `심해27`에서 `심해31`로 옮겨졌다 — **문턱이 아니라 성소 가장 깊은 방**에서 만난다.

### 신설 자산 (배포 전 필수)

- [ ] 「무명의 성소」 건축 + 지역 `심해교단본부` (`심해28`이 참조)
- [ ] `무명의낱장` 재료 + 획득 경로 — 기록고에서 회수 (`심해30`이 참조). `materials.json` 항목은 생성기가 넣는다
- [ ] 성소 어종 풀 — 사육 수조에서 작살을 쓰므로(`심해29`) **자체 정의 또는 `parentIsland` 필수**(§4-②)

★**성소 NPC는 0을 전제로 설계했다.** 신도 NPC를 두면 잠입 연출이 살지만 대사·스킨 비용이 붙고
NPC 이름표 색 규약(§0-6)도 따라온다. 둘지 여부는 건축 단계에서 결정할 것.
