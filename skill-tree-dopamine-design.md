# 숙련 특성 트리 기획

> 대상: 낚시, 채굴, 재배, 요리, 탐험, 수집
> 목적: 레벨을 단순 숫자가 아니라 "다음 플레이가 달라지는 선택"으로 만들고, 각 활동에 눈에 보이는 보너스 사건을 추가한다.

## 1. 결론

현재 레벨과 경험치 시스템은 유지하고, 각 레벨에 **숙련 포인트 1개**를 붙인다.
플레이어는 포인트를 연결형 특성 트리에 사용한다. 전 단계에 1포인트 이상 투자하면 다음 노드가 열리고,
깊은 노드는 해당 계열 총 투자량도 요구한다.

레벨은 콘텐츠 해금, 특성은 플레이 방식 선택을 담당한다.

- 낚시의 M/L/G 등급 해금, 채굴의 광물 티어 해금 등은 계속 레벨로 열린다.
- 특성을 잘못 찍어도 지역, 등급, 레시피 같은 핵심 콘텐츠가 막히지 않는다.
- 특성은 단순한 `수익 +N%`보다 더블, 압축, 완벽, 보급품처럼 **발동 순간이 보이는 사건**을 만든다.
- 기존 레벨은 소급 적용한다. 사용 가능 포인트는 기본적으로 `현재 레벨 - 1`이다.

## 2. 현재 구조에서 먼저 바로잡을 것

현재 코드에는 낚시 Lv.100, 나머지 숙련 Lv.50과 로드맵 GUI가 이미 있다. 다만 로드맵 문구와 실효과가 완전히 일치하지 않는다.

- 낚시 레벨 보너스와 M/L/G 해금은 실제 적용 중이다.
- 실제 낚시 등급 게이트는 `M Lv.30 / L Lv.45 / G Lv.60`인데, `RewardMath.levelBonus()`의 표시용 최대 등급만
  `M Lv.30 / L Lv.40 / G Lv.50`으로 어긋나 있다. 특성 작업 전에 실제 게이트 기준으로 통일한다.
- ~~채굴은 광물 풀, 수량, 5초 쿨타임이 실제 적용 중이다.~~ **2026-07-10 정정** — "5초 쿨타임 울타리 우클릭"
  메커니즘(SkillManager.onMine)은 잘못된 시스템이라 전면 삭제. 실제 채굴 행동은 **섬광산**(islandmine/,
  울타리·물 틈 광석 생성기 — §5.2의 광물 풀과 일치)과 **드릴**(mine 월드 광맥)이다.
  **섬광산에는 매크로 방지를 위해 경험치를 절대 연결하지 않는다**(발동/특성 효과는 가능, 경험치만 금지).
- 재배는 바닐라 완전 성장 작물에 레벨별 추가 드롭이 실제 적용 중이다.
- 요리 로드맵의 더블/트리플/완벽 조리는 아직 실제 효과가 없고, 제작 시 요리 경험치도 지급되지 않는다.
- 특수 작물은 재배 레벨/경험치와 아직 연결되지 않았다.
- 탐험과 수집은 새 지역, 수집품, 도감 최초 발견 위주라 반복 가능한 경험치 공급원이 부족하다.

따라서 특성 트리 GUI의 설명은 실제 효과 정의와 같은 데이터를 읽게 해야 한다. "보인다고 적용되는 척하는" 보상은 남기지 않는다.

## 3. 공통 규칙

### 3.1 포인트와 트리 크기

- 레벨업마다 해당 숙련 포인트 `+1`.
- Lv.1은 시작 레벨이므로 포인트 0, Lv.50은 총 49포인트, 낚시 Lv.100은 총 99포인트.
- 모든 특성을 전부 찍을 수는 없게 한다. 전체 필요 포인트는 획득 가능 포인트의 약 1.35~1.45배로 잡는다.
- 각 숙련은 3개 계열을 가진다.
- 일반 노드는 여러 랭크, 발동 노드와 핵심 특성은 1랭크다.
- 전 노드 1랭크 투자 시 다음 연결 노드가 열리지만, 중간/최종 노드는 계열 총 투자량도 요구한다.

권장 규모:

| 구분 | 계열당 비용 | 전체 비용 | 만렙 포인트 | 결과 |
|---|---:|---:|---:|---|
| 낚시 Lv.100 | 약 43 | 약 129 | 99 | 2계열 완성 + 1계열 일부 |
| 나머지 Lv.50 | 약 23 | 약 69 | 49 | 2계열 완성 + 1계열 일부 |

### 3.2 재분배

- 레벨과 경험치는 그대로 두고 투자 포인트만 반환한다.
- 출시 후 2주는 무료 재분배로 밸런스 조정 피해를 막는다.
- 안정화 뒤에는 숙련별 주 1회 무료, 추가 재분배는 레벨에 비례한 게임머니를 받는다.
- 수치 조정이나 효과 변경이 있으면 해당 숙련은 자동 무료 초기화한다.

### 3.3 기존 보상과의 관계

- 기존 장비/강화는 **수치 스펙**, 특성은 **규칙 변화와 발동 사건**을 중심으로 한다.
- 낚시 장비의 크기, 행운, 더블, 트리플을 특성에서 크게 다시 주지 않는다.
- 기존 레벨 자동 보너스는 출시 시 빼앗지 않는다. 특성은 "추가 드롭이 터지면 한 번 더", "트리플이 만선으로 승격"처럼 기존 결과를 변형한다.
- 보너스로 생긴 추가 아이템은 다시 특성 발동이나 경험치 지급을 유발하지 않는다.
- 한 번의 행동에서 대형 잭팟은 1종만 발동한다. 일반 더블과 잭팟이 무한 증폭되지 않게 최종 수량 상한을 둔다.

## 4. 도파민 리듬

특성마다 아래 3단계 보상을 섞는다.

| 층 | 체감 주기 | 역할 | 표시 |
|---|---|---|---|
| 작은 발동 | 5~15회 행동 | 빌드가 작동한다는 확인 | 짧은 효과음 + 액션바 |
| 중간 발동 | 3~10분 | 계속할 이유 | 파티클 + 결과 수량 강조 |
| 잭팟 | 30~120분 | 기억에 남는 순간 | 화면 앞 아이템 공개 + 전용 사운드 |

잭팟은 고정 독립 확률 대신 내부 PRD를 사용한다. 실패할수록 다음 확률이 조금 오르고 성공 시 초기화하되,
확률이나 실패 횟수는 화면에 표시하지 않는다. 낚시의 등급 PRD처럼 긴 무보상 구간만 줄이는 방식이다.

경제 기여와 연출 강도는 분리한다.

- 한 숙련 트리의 최종 시간당 수익 증가는 대략 15~25% 안에 둔다.
- 잭팟이 만드는 기대수익은 전체의 5~10% 정도만 차지하게 한다.
- 대신 잭팟은 이름, 소리, 파티클, 큰 결과물로 가장 강하게 보인다.
- 일반 발동은 전체 채팅을 쓰지 않는다. 매우 희귀한 레시피/유물만 전체 공지를 고려한다.

## 5. 숙련별 트리

### 5.1 낚시

낚시는 장비 스탯이 이미 많으므로 특성에서 같은 퍼센트를 크게 반복하지 않는다.

| 계열 | 안정 효과 | 중간 발동 | 핵심 잭팟 |
|---|---|---|---|
| 집중 | 크리티컬 시 추가 콤보, 일정 쿨타임마다 실패 1회 콤보 보호, 콤보 보너스 소폭 강화 | `완벽한 낚시`: 크리티컬 성공 시 낮은 확률로 품질 최소 100 | `무아지경`: 높은 콤보 중 완벽한 낚시가 발동하면 경험치와 보물 판정 1회 추가 |
| 대어 | 최종 크기 소폭 증가, 개인 기록 경신 보너스, 최대 크기 초과분 가치 강화 | `거대어`: 낮은 확률로 최종 크기 +25% | `전설의 대어`: 거대어가 개인 기록을 경신하면 지역 재료 묶음 추가 |
| 어복 | 더블 성공 시 추가 1마리 승격 확률, 트리플 승격 확률, 미끼/내구 보존 | `풍어`: 한 번의 낚시 결과가 3~4마리로 승격 | `만선`: 약 0.15~0.5%의 내부 PRD로 동일 물고기 5마리, 전용 공개 연출 |

초기 수치 원칙:

- 직접 더블/트리플 확률 증가는 계열 완성 기준 합계 5% 안쪽.
- `거대어`는 0.3%에서 시작해 계열 투자로 최대 약 1.0%.
- `만선`은 경제 기여보다 희귀 연출이 목적이며 추가 결과에 재귀 발동하지 않는다.
- 기존 M/L/G 해금, 레벨 크리티컬, 콤보 보너스, 젖은 보물상자는 그대로 유지한다.

### 5.2 채굴

> **2026-07-10 기반 정정**: 구 울타리 우클릭 채굴(5초 쿨타임)은 삭제됨. 발동/특성의 기준 행동은
> **섬광산 광석 1개 캐기**(islandmine/IslandMineManager.mine — 아래 광물 풀과 일치)이며, 드릴 광맥(mine 월드)도 대상.
> 채굴 경험치(구현 완료): **섬광산 = 일일 500회 상한**(무한 생성기 매크로 방지, 광물별 xp 돌2~다이아10,
> `SkillManager.addIslandMineExp`), **드릴 = 무제한**(고갈+재생 구조라 매크로 효율 낮음, breakTicks/2 xp).
> 특성 발동(추가 광물/응축/대광맥)은 상한과 무관하게 두 채굴 모두에서 동작하게 한다.

섬광산 광석 1개 캐기(약 1~3초 간격의 반복 행동)를 기준으로 설계한다. 사용자가 말한 "추가 광물"과 "9개 압축 광물"이 가장 잘 맞는 파일럿이다.

| 계열 | 안정 효과 | 중간 발동 | 핵심 잭팟 |
|---|---|---|---|
| 광맥 | 같은 광물 +1 확률, 기존 추가 수량 발동 강화 | `풍부한 광맥`: 추가 드롭이 한 번 더 굴러감 | `대광맥`: 3~6회의 추가 광물 판정을 한꺼번에 지급 |
| 응축 | 모래/자갈 같은 잡광물 정제, 압축 가능한 광물 판정 | `응축 성공`: 결과 대신 해당 광물 블록 1개 지급(9개 가치) | `완전 응축`: 매우 낮은 확률로 광물 블록 2개 |
| 탐광 | 채굴 쿨타임 최대 1초 감소, 희귀 광물 가중치 상승 | `정밀 탐사`: 잡광물 결과를 희귀 풀로 1회 재굴림 | `심층 광맥`: 다이아/에메랄드/네더라이트 조각 계열 보상 묶음 |

초기 수치 원칙:

- 추가 광물 확률은 계열 완성 시 최대 약 20~25%.
- 압축 성공은 0.1%에서 시작해 최대 약 0.7%. 5초 행동 기준 최대 투자 시 약 12분에 1회 기대다.
- 석탄, 철, 금, 구리, 청금석, 다이아, 에메랄드처럼 9개 압축이 자연스러운 결과에만 적용한다.
- 네더라이트 조각에는 광물 블록 판정을 적용하지 않고 별도 희귀 보상표를 쓴다.

발동 문구 예시:

`응축 성공! 철 블록 x1 (철 9개)`

### 5.3 재배

바닐라 작물과 섬의 특수 작물을 모두 재배 숙련에 연결한다. 빠른 바닐라 수확과 느린 특수 작물은 서로 다른 확률표를 쓴다.

| 계열 | 안정 효과 | 중간 발동 | 핵심 잭팟 |
|---|---|---|---|
| 풍작 | 기존 보너스 수확이 +2로 승격될 확률, 특수 작물 수량 증가 | `풍년`: 한 수확의 결과 2~3배 | `대풍년`: 결과 4배, 주변에 짧은 수확 연출 |
| 품종 | 희귀 씨앗/지역 작물 확률, 강화 농산물 판정 | `강화 수확`: 기본 작물 16개짜리 강화 농산물을 바로 획득 | `황금 수확`: 최상급 농산물 또는 요리용 희귀 재료 획득 |
| 재배기술 | 씨앗 보존, 특수 작물 성장시간 최대 10% 감소 | `되살아남`: 수확한 특수 작물이 씨앗 소모 없이 즉시 재식재 | `생명의 밭`: 재식재된 작물이 일부 성장한 상태로 시작 |

`강화 밀/당근/감자`는 현재 실제로 기본 농산물 16개 압축 재료이므로, 드롭 순간부터 가치가 명확하다.

### 5.4 요리

요리는 행동 빈도가 낮아 발동 확률을 낚시/채굴보다 높게 잡는다. 먼저 모든 실제 제작에 레시피 티어 기반 요리 경험치를 연결해야 한다.

| 계열 | 안정 효과 | 중간 발동 | 핵심 잭팟 |
|---|---|---|---|
| 대량조리 | 완성품 더블 확률 | `손이 빨랐다`: 완성품 2개 | `연회 준비`: 완성품 3개 |
| 완벽조리 | 완벽 판정 확률, 완벽 요리 판매가/버프시간 증가 | `완벽한 한 접시`: 이름과 광택이 다른 완벽 등급 요리 | `명장의 요리`: 버프시간과 제출가가 크게 오른 명품 결과 |
| 절약 | 재료 1종 일부 반환, 고가 재료 보존 가중치 | `남김없는 손질`: 무작위 재료 1종 전량 반환 | `기적의 조리`: 매우 낮은 확률로 재료를 소비하지 않고 완성 |

초기 수치 원칙:

- 더블은 최대 10%, 트리플은 최대 1% 안쪽.
- 완벽 조리는 0.5%에서 시작해 최대 3%.
- 완벽 요리는 새 레시피가 아니라 동일 요리의 품질 태그로 구현해 레시피 폭증을 막는다.

### 5.5 탐험

단순 이동 거리로 경험치를 주면 AFK 이동과 원형 달리기가 최적 행동이 되므로 금지한다. 새 지역/수집품, 탐험 의뢰,
지역 채집 포인트처럼 의미 있는 상호작용만 경험치와 발동 기회로 인정한다.

| 계열 | 안정 효과 | 중간 발동 | 핵심 잭팟 |
|---|---|---|---|
| 기동 | 야외 도보 이동속도 최대 +8%, 이동 편의 | `가벼운 발걸음`: 발견 직후 짧은 이동속도 상승 | `선구자의 질주`: 첫 신규 발견 뒤 일정 시간 빠른 탐색 |
| 채집 | 지역 채집물 더블 확률, 지역 재료 수량 증가 | `풍성한 채집`: 채집 결과 2~3배 | `지역의 선물`: 해당 지역 재료 대량 묶음 |
| 탐색 | 미발견 수집품 근처에서 낮은 빈도의 소리 힌트, 탐험 상자 확률 | `직감`: 60초 쿨타임의 방향 없는 근접 신호 | `숨겨진 보급품`: 개인 전용 상자가 근처에 나타나 지역 재료/지도/레시피를 지급 |

필수 선행 콘텐츠:

- 지역별 3~5종의 반복 가능한 `채집 포인트`를 만든다. 조개, 약초, 버섯, 광석 더미처럼 맵에 어울리는 모델을 쓴다.
- 개인별 또는 짧은 재생성 방식으로 다른 플레이어에게 뺏기는 스트레스를 줄인다.
- 이동속도는 야외 섬 도보에서만 적용하고 배, 미니게임, 파쿠르/연출 구역에서는 끈다.
- 탐색 힌트는 정확한 좌표나 화살표를 주지 않는다. 발견의 재미를 없애지 않을 정도의 소리/입자만 준다.

### 5.6 수집

현재 수집은 도감 최초 등록 위주라 콘텐츠를 다 찾으면 성장이 멈춘다. 반복 가능한 "기록 개선"을 추가해야 트리가 살아난다.

| 계열 | 안정 효과 | 중간 발동 | 핵심 잭팟 |
|---|---|---|---|
| 기록가 | 신규 도감 경험치 증가, 물고기 개인 기록 경신 시 수집 경험치 | `주목할 기록`: 기존 최고 크기/품질 갱신 보너스 | `역대급 표본`: 큰 기록 경신에 연구 보상 묶음 |
| 감정가 | 물고기/부품/재료 획득 시 희귀 표본 판정 | `희귀 표본`: 수집 NPC 제출가와 수집 경험치 증가 | `진품 감정`: 매우 희귀한 전시용 표본과 특별 보상 |
| 복원가 | 지역 도감 세트 보상 강화, 레시피 단서 확률 | `낡은 설계도`: 미해금 레시피 단서 획득 | `복원 완료`: 단서 누적으로 히든 레시피 또는 장식품 해금 |

수집은 돈을 직접 크게 뿌리기보다 도감, 전시, 히든 레시피, 장식 보상으로 연결해야 낚시/채굴과 역할이 겹치지 않는다.

## 6. 발동 연출 규칙

모든 숙련이 같은 이름의 `+보너스`만 띄우면 금방 무감각해진다. 활동별 대표 잭팟 이름과 결과물을 고정한다.

| 숙련 | 대표 잭팟 | 화면 앞 공개 아이템 |
|---|---|---|
| 낚시 | 만선 / 전설의 대어 | 실제 물고기 |
| 채굴 | 응축광맥 / 대광맥 | 광물 블록 |
| 재배 | 황금 수확 / 대풍년 | 강화 농산물 |
| 요리 | 완벽한 한 접시 | 완벽 등급 요리 |
| 탐험 | 숨겨진 보급품 | 지역 상자 또는 지도 |
| 수집 | 진품 감정 | 희귀 표본 |

- 작은 발동: 액션바 한 줄, 짧은 소리. 채팅을 쌓지 않는다.
- 중간 발동: 손 근처 파티클과 한 번의 강조음.
- 잭팟: 현재 낚시 B+ 공개처럼 실제 결과 아이템을 화면 앞에 약 0.7초 표시한 뒤 지급한다.
- 동일한 전역 `RewardRevealFx`를 만들어 낚시 전용 연출 코드를 재사용한다.
- 전용 리소스팩 모델은 시스템이 검증된 뒤 추가한다. 첫 버전은 실제 아이템 모델로 충분하다.

## 7. GUI

`/레벨`의 기존 6개 숙련 아이콘은 유지한다.

- 아이콘에 `사용 가능 포인트 N`을 표시하고 포인트가 있으면 광택을 준다.
- 숙련 클릭 시 기존 1~50/100 로드맵 대신 3계열 연결형 특성 보드를 연다.
- 상단에는 레벨, 현재/필요 경험치, 사용 가능 포인트만 표시한다.
- 각 노드는 현재 랭크, 다음 랭크의 정확한 수치, 선행 조건을 보여준다.
- 잠긴 노드는 이유를 하나만 명확히 표시한다: `광맥 계열에 7포인트 필요`.
- 1회 클릭은 1포인트 투자, 우클릭은 1포인트 회수로 한다. 무료 재분배 기간이 아니면 개별 회수 대신 전체 초기화만 허용한다.
- 뒤쪽 노드에 투자한 포인트가 있으면 선행 노드는 개별 회수할 수 없다.
- 핵심 특성 투자 시에는 짧은 타이틀과 소리를 내되, 일반 랭크 투자에는 클릭음만 쓴다.

레벨업 메시지는 다음 정도로 짧게 유지한다.

`채굴 Lv.18 달성 - 숙련 포인트 +1 (사용 가능 3)`

상시 사이드바에 포인트를 넣지는 않는다.

## 8. 밸런스 안전장치

- 발동 확률은 행동 빈도별로 따로 계산한다. 채굴 5초 행동과 요리 1회 제작에 같은 1%를 쓰지 않는다.
- 대량 제작도 레시피 클릭 1회를 발동 1회로 본다.
- 보너스 결과에는 경험치, 도감 발견, 추가 특성 판정을 재귀 호출하지 않는다.
- 자동화 가능한 바닐라 농장은 특수 작물보다 낮은 압축/잭팟 확률을 쓴다.
- 탐험 경험치는 이동 거리나 접속 시간으로 주지 않는다.
- 전체 트리 완성의 시간당 기대수익을 먼저 계산하고, 화려한 잭팟의 액면가만 보고 확률을 정하지 않는다.
- 운영 로그에 숙련별 행동 수, 발동 수, 생성 가치, 플레이어별 최장 무발동 횟수를 남긴다.

## 9. 구현 순서

### 1차: 공통 엔진 + 채굴 파일럿 — **2026-07-10 구현 (dev 미배포)**

- 포인트 소급 지급, 저장, 선행 노드, 재분배.
- 3계열 GUI.
- 내부 PRD 롤러와 공통 아이템 공개 연출.
- 채굴의 추가 광물, 압축 광물, 대광맥을 실제 적용.
- 1주일 운영 통계로 발동 주기와 경제 증가율 확인.

**구현 현황** (`com/blockship/skilltree/` — SkillTreeManager·RewardRevealFx):

- 포인트 = 레벨 소급 계산식(`레벨-1 - 총투자`), 저장 = `PlayerData.extraNums("특성채굴.<노드id>")`.
- 채굴 트리 12노드(계열당 10+8+1+1=20P, 전체 60 vs Lv.50 보유 49 → 선택 강제. 목표비 1.35보다 낮음 — 추후 노드 추가로 조정).
- 발동: 섬광산(`IslandMineManager.mine`)에 광맥(+1/연쇄/풍부/대광맥)·응축(정제/블록/강화/완전)·탐광(가중/재굴림/심층) 전부,
  드릴(`DrillManager.complete`)에 광맥+잭팟. 한 행동 잭팟 1종, 보너스에 exp/재귀 없음.
- 재분배: 무료기간(FREE_UNTIL 2026-07-24, 우클릭 개별 회수 가능) → 이후 주 1회 무료 전체 초기화. 유료 초기화는 미구현(추후).
- 통계: `통계채굴행동/발동` extraNums + 잭팟 콘솔 로그. 생성 가치/최장 무발동 로그는 미구현.
- GUI: `/레벨` → 채굴 클릭 시 로드맵 대신 특성 보드, 허브 아이콘에 포인트+광택, 레벨업 메시지에 포인트 안내.
- `RewardMath.levelBonus` maxGrade 표시를 실제 게이트(M30/L45/G60)로 통일 + 자가 테스트 갱신(49/0 통과).

채굴을 먼저 권하는 이유는 행동과 결과가 단순하고, `+1`, `9개 압축`, `3~6회 추가`의 차이가 즉시 보여서 공통 엔진 검증이 빠르기 때문이다.

### 2차: 낚시

- 현재 장비, 콤보, 크리티컬, 보물상자와 중복 계산 점검.
- 만선/거대어/완벽한 낚시 추가.
- 기존 낚시 공개 연출을 공통 연출로 이동.

### 3차: 재배 + 요리

- 특수 작물을 재배 경험치/특성과 연결.
- 요리 제작 경험치 지급.
- 강화 농산물, 완벽 요리 품질 태그 적용.

### 4차: 탐험 + 수집

- 지역 채집 포인트를 먼저 만든 뒤 탐험 더블/보급품을 적용.
- 기록 경신과 희귀 표본으로 수집의 반복 경험치 루프를 만든다.

## 10. 참고한 좋은 구조

- [Hypixel SkyBlock의 Heart of the Mountain](https://wiki.hypixel.net/Heart_Of_The_Mountain)은 레벨로 토큰을 받고 연결형 특성을 열며,
  비용을 내고 초기화할 수 있다. 구조는 참고하되 별도 강화 재화까지 복제하지 않는다.
- [Stardew Valley의 Skills](https://stardewvalleywiki.com/Skills)는 매 레벨의 작은 성장과 특정 레벨의 직업 선택을 나눈다.
  우리 서버는 매 레벨 포인트와 계열 핵심 특성을 이 역할로 사용한다.

핵심은 "확률 +1%"를 파는 것이 아니라, 그 결과로 **오늘 처음 철 블록이 튀어나오고, 완벽한 요리가 만들어지고,
숨겨진 보급품이 나타나는 순간**을 파는 것이다.
