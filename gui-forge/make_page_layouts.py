#!/usr/bin/env python3
"""전용 배경 3장 좌표 가이드 — 강화 · 주방 · 우편함.

세 화면 다 **0행을 슬롯이 쓴다**. 그래서 공용판(0행 = 제목 명판)을 못 쓰고,
아이스박스와 같은 구조 — 제목을 격자 **위쪽 띠**로 올린 전용 판 — 로 간다.

역할 표기
  홈   : 아이템이 올라가는 자리. 홈만 파고 아이콘은 우리가 올린다
  입력 : 플레이어가 아이템을 넣는 빈 칸. 다른 홈과 **구분되게** 그린다
  목록 : 내용물이 매번 바뀌는 칸. 안쪽은 조용하게
  장식 : 아이템 없음. 그림으로 채운다

산출: src/<이름>/_guide.png
"""
import os

from PIL import Image, ImageDraw, ImageFont

HERE = os.path.dirname(os.path.abspath(__file__))
RP = os.path.expanduser("~/development/barkan-resourcepack")
FONT_TTF = os.path.join(RP, "assets/barkan/font/aggro_bold.ttf")

SCALE, GRID_X, GRID_Y, CELL, COLS = 4, 7, 17, 18, 9
TITLE_Y0, TITLE_Y1 = 24, 64

ROLE_COLOR = {
    "타일": ((255, 220, 60, 255), (255, 235, 140, 255)),
    "홈":   ((255, 160, 0, 255), (255, 190, 90, 255)),
    "입력": ((80, 255, 140, 255), (150, 255, 190, 255)),
    "목록": ((120, 200, 255, 255), (170, 220, 255, 255)),
    "장식": ((160, 160, 160, 160), (200, 200, 200, 200)),
}

# 이름: (행 수, {슬롯: (역할, 라벨)}, 기본역할)
# TILES[이름] = [(라벨, 시작열, 시작행, 열수, 행수)] — 여러 칸을 한 덩어리로 그리는 큰 버튼.
# 대장간 허브가 이 방식이다(메뉴/내 정보와 같은 타일형).
# 대장간은 작업대가 넷뿐이다. 3열x2행 6칸으로 깔면 두 칸을 억지로 채워야 해서
# 2x2로 두고 **사이와 바깥에 한 칸씩 여백**을 준다 — 남은 자리는 대장간 그림 몫이다.
#   가로 1 + 3 + 1 + 3 + 1 = 9  ·  세로 여백행(0) + 2 + 여백행(3) + 2
TILES = {
    "smithy": [("조합대", 1, 1, 3, 2), ("낚싯대 강화", 5, 1, 3, 2),
               ("부품 분해", 1, 4, 3, 2), ("재료 제작소", 5, 4, 3, 2)],
    # 길드 허브 — 메뉴/내 정보와 같은 3열x2행 타일 6개(9열을 3+3+3 으로 정확히 나눈다).
    "guild": [("길드 섬", 0, 1, 3, 2), ("업그레이드", 3, 1, 3, 2), ("기부", 6, 1, 3, 2),
              ("길드원", 0, 3, 3, 2), ("랭킹", 3, 3, 3, 2), ("엠블럼", 6, 3, 3, 2)],
}

PAGES = {
    # 길드 허브(/길드) — 버튼 9개가 54칸에 흩어져 있던 것을 타일 6 + 아래 아이콘 줄로 정리한다.
    # 타일은 TILES 가 그린다(아이템을 안 올린다 — 올리면 구워 둔 그림을 가린다).
    # 0행은 길드 이름·레벨·자산을 보여줄 자리라 4번 한 칸만 쓰고 나머지는 그림 몫이다.
    "guild": (6, {
        4: ("홈", "길드 정보"),
        45: ("홈", "뒤로"), 46: ("홈", "길드 설정"), 47: ("홈", "가입 신청"),
        48: ("홈", "채팅 설정"), 50: ("홈", "둘러보기"), 52: ("홈", "해산"),
        53: ("홈", "닫기"),
    }, "장식"),
    # 대장간 허브 — 27칸이라 가운데 네 칸만 쓰던 것을 54칸 타일형으로 넓힌다.
    "smithy": (6, {8: ("홈", "닫기")}, "장식"),   # 타일은 TILES 가 그린다
    # 조합대 — 왼쪽 목록 5x5 · 오른쪽 3x3 그리드 · 43 미리보기 · 52 제작 버튼
    "crafting": (6, {
        0: ("홈", "안내"), **{1 + i: ("홈", "탭") for i in range(6)}, 8: ("홈", "닫기"),
        **{s: ("목록", "") for r in range(5) for s in range(9 + r * 9, 14 + r * 9)},
        **{s: ("홈", "그리드") for s in (15, 16, 17, 24, 25, 26, 33, 34, 35)},
        43: ("홈", "미리보기"), 52: ("홈", "제작"),
    }, "장식"),
    # 부품 분해 — 실물 부품·재료를 넣고 「갈기」 한 번. 판매창과 같은 3x7 안쪽 영역.
    # (부품은 PDC 태그가 붙은 진짜 아이템이다 — buildPartItem. 넣을 수 있다.)
    # 한 번에 21칸이 부족하면 여러 번 갈면 되므로 더 키우지 않는다.
    # ★입력 3x7 은 행2~4(19~43) 다 — 아트의 초록판이 거기다. 행1~3 으로 적어 뒀던 탓에
    #   첫 줄이 장식 위에 떠 있고 「갈기」와 사이에 빈 행이 끼었다(2026-08-11 수정).
    "disassemble": (6, {
        **{s: ("입력", "") for r in range(3) for s in range(19 + r * 9, 26 + r * 9)},
        49: ("홈", "갈기"),
    }, "장식"),
    # 재료 제작소 — 9~44 목록, 0 분해로, 8 닫기, 49 잔액
    "forge": (6, {
        0: ("홈", "분해로"), 8: ("홈", "닫기"),
        **{s: ("목록", "") for s in range(9, 45)}, 49: ("홈", "잔액"),
    }, "장식"),
    # 아이스박스 상점 — 3행 27칸. 가운데 줄(9~17)에만 티어 9개를 놓고 위아래 줄은 여백이다.
    # 2행으로 짰더니 진열이 천장에 붙어 답답했다(2026-08-08).
    # 잔액·진주 보유량은 제목 줄로 올렸고 닫기는 Esc — 그래서 홈은 9개뿐이다.
    # ★진열은 **아랫줄(18~26)** 이다 — 아트가 아랫줄로 와서 코드를 그쪽에 맞췄고
    #   (IceboxGui.SHOP_ROW=18) 이 표만 가운데 줄로 남아 있었다. 그 탓에 칸 정렬 조사가
    #   엉뚱한 줄을 재고 있었다(2026-08-10 전수조사에서 발견).
    "iceshop": (3, {0: ("홈", "뒤로"), 8: ("홈", "자동수납"),
                    **{s: ("홈", f"{s-17}티어") for s in range(18, 27)}}, "장식"),
    # 스킬 허브 — 3행 27칸. 가운데 줄 5칸(11~15)에만 스킬 메달리온. 나머지는 전부 그림.
    "skillhub": (3, {**{s: ("홈", ["낚시", "재배", "채굴", "요리", "수집"][s - 11]) for s in range(11, 16)}}, "장식"),
    # 특성 트리 — 5행 45칸. 0행에 버튼 4개, 1~4행은 노드·레일·브랜치 라벨이 빼곡히 올라간다.
    # ★1~4행에는 소켓을 파지 말 것: 노드와 레일이 oversized_in_gui 로 칸 밖까지 그려져
    #   소켓 테두리와 겹친다. 그 영역은 평평한 판이어야 한다(발주서에 명시).
    # ★왼쪽 열(9·18·27·36)에는 홈이 없다. 갈래 이름표를 그리던 자리였는데 트리 5개가
    #   전부 근원 노드를 가져 그 분기가 죽어 있었다(2026-08-08 삭제). 지금 그 열에는
    #   근원 노드와 거기서 뻗는 세로 연결선만 지나간다 — 1~4행과 같은 평평한 판이어야 한다.
    "skilltree": (5, {
        0: ("홈", "이전"), 4: ("홈", "정보"), 7: ("홈", "다음"), 8: ("홈", "초기화"),
    }, "장식"),
    # ===== 도감 4종 (2026-08-09 발주) =====
    # 낚싯대/부품/재료/지역/채집품 탭이 슬롯 배열을 공유하고(TAB_SLOTS 28칸),
    # 수집품 상세도 같은 배열(DETAIL_SLOTS)이라 판 하나가 6화면을 덮는다.
    # 분류 일곱 = 물고기·낚싯대·부품·재료·지역·수집품·채집품. 10~16 이라 가운데(13) 축으로
    # 좌우가 맞는다. (미분류 물고기는 2026-08-09 폐지 — 관리용 화면이 유저 메뉴에 섞여 있었다.)
    "dexmain": (3, {
        4: ("홈", "머리"), 8: ("홈", "닫기"),
        **{s: ("홈", "분류") for s in range(10, 17)},
    }, "장식"),
    "dextab": (6, {
        0: ("홈", "뒤로"), 8: ("홈", "닫기"),
        **{s: ("목록", "") for r in range(4) for s in range(10 + r * 9, 17 + r * 9)},
        48: ("홈", "이전"), 49: ("홈", "페이지"), 50: ("홈", "다음"),
    }, "장식"),
    # 물고기 도감 — 0행이 등급 필터(E~G)까지 쓰고, 아래 한 줄이 통째로 내비게이션이다.
    "dexfish": (6, {
        0: ("홈", "뒤로"), 8: ("홈", "닫기"),
        **{s: ("홈", "등급") for s in range(1, 8)},
        **{s: ("목록", "") for s in range(9, 45)},
        45: ("홈", "이전"), 46: ("홈", "지역◀"), 49: ("홈", "페이지"),
        51: ("홈", "지역▶"), 52: ("홈", "전환"), 53: ("홈", "다음"),
    }, "장식"),
    "dexisland": (4, {
        0: ("홈", "뒤로"), 4: ("홈", "머리"), 8: ("홈", "닫기"),
        **{s: ("목록", "") for r in range(2) for s in range(10 + r * 9, 17 + r * 9)},
    }, "장식"),
    # NPC 대화 창(4행 36칸) — 말 걸어 대사가 끝나면 열린다.
    #   0~17  대화 기록(최근 18줄, NPC=종이 / 나=책)
    #   18~26 구분 줄 + 22 = 진행중 퀘스트 정보
    #   27~35 선택지. 개수에 따라 가운데 정렬 — 1개면 31, 4개면 29·30·32·33.
    #         그래서 홈은 29~33 다섯 자리에만 필요하다(NpcDialogueManager.centerSlots).
    "npcdialog": (4, {
        **{s: ("목록", "") for s in range(18)},
        22: ("홈", "퀘스트"),
        **{s: ("홈", "선택지") for s in (29, 30, 31, 32, 33)},
    }, "장식"),
    # 장비 작업대(3행 27칸) — /장비. economy/WorkbenchGui.
    #   4=장비 요약(낚싯대 본체+강화+5부위 합산 스탯)
    #   11~15=장착 슬롯. 왼쪽부터 릴·줄·바늘·미끼·찌 순서로 고정이다.
    #   ★이 다섯은 홈이 아니라 **입력**이다 — 플레이어가 인벤토리에서 부품을 끌어다 놓는다.
    #   ★닫기 버튼이 없는 화면이다(Esc 로 닫는다). 8은 장식.
    "workbench": (3, {
        4: ("홈", "장비 요약"),
        11: ("입력", "릴"), 12: ("입력", "줄"), 13: ("입력", "바늘"),
        14: ("입력", "미끼"), 15: ("입력", "찌"),
    }, "장식"),
    # 퀘스트 목록(3행 27칸) — 쉬프트+Q. QuestGui.renderJournal.
    #   4=제목 명판 · 8=닫기 · 13=진행 중인 메인 퀘스트(가운데 하나) · 18="오늘의 일일" 라벨
    #   19~25=수락한 일일 퀘스트 최대 7개
    "questlist": (3, {
        4: ("홈", "제목"), 8: ("홈", "닫기"),
        13: ("홈", "메인 퀘스트"),
        18: ("홈", "일일 라벨"),
        **{s: ("목록", "") for s in range(19, 26)},
    }, "장식"),
    # NPC 퀘스트 수락창(2행 18칸) — 대화 끝 또는 [Q] NPC 우클릭. QuestGui.openNpc.
    #   ★한 번에 퀘스트 한 장만 보여준다(수락 가능한 첫 항목). 13 이 그 자리다.
    #   그래서 나머지는 전부 그림이고, 13 을 주인공으로 크게 잡아야 한다.
    "questnpc": (2, {
        4: ("홈", "제목"), 8: ("홈", "닫기"),
        13: ("홈", "퀘스트"),
    }, "장식"),
    "enhance": (5, {
        4: ("홈", "낚싯대 정보"),
        10: ("홈", "↓라벨"), 12: ("홈", "↓라벨"), 13: ("홈", "↓라벨"), 14: ("홈", "↓라벨"),
        19: ("입력", "낚싯대"), 21: ("입력", "상승권"), 22: ("입력", "방지권"), 23: ("입력", "감소권"),
        16: ("홈", "강화 전"), 25: ("홈", "▼"), 34: ("홈", "강화 후"),
        31: ("홈", "성공률"), 40: ("홈", "강화 버튼"),
    }, "장식"),
    "cooking": (6, {
        **{s: ("홈", "탭") for s in (0, 1, 2)}, 8: ("홈", "안내"),
        **{s: ("목록", "") for s in
           list(range(10, 17)) + list(range(19, 26)) + list(range(28, 35))},
        45: ("홈", "이전"), 47: ("홈", "대기열"), 49: ("홈", "페이지"),
        50: ("홈", "다음"), 53: ("홈", "닫기"),
    }, "장식"),
    "mailbox": (6, {
        **{s: ("목록", "") for s in range(45)},
        45: ("홈", "이전"), 48: ("홈", "안내"), 49: ("홈", "모두 수령"),
        53: ("홈", "다음"),
    }, "장식"),
}


def font(px):
    try:
        return ImageFont.truetype(FONT_TTF, px)
    except Exception:
        return ImageFont.load_default()


def geom(rows):
    gh = 114 + rows * CELL
    return 176 * SCALE, gh * SCALE, 31 + rows * CELL


def guide(name, rows, roles, default):
    w, h, inv_y = geom(rows)
    im = Image.new("RGBA", (w, h), (34, 34, 42, 255))
    d = ImageDraw.Draw(im, "RGBA")
    fs = font(15)
    for r in range(rows):
        for c in range(COLS):
            s = r * COLS + c
            role, label = roles.get(s, (default, ""))
            outline, text = ROLE_COLOR[role]
            x0, y0 = (GRID_X + CELL * c) * SCALE, (GRID_Y + CELL * r) * SCALE
            box = [x0, y0, x0 + CELL * SCALE - 1, y0 + CELL * SCALE - 1]
            d.rectangle(box, outline=outline, width=5 if role != "장식" else 2)
            d.text((x0 + 4, y0 + 4), str(s), font=fs, fill=(255, 255, 255, 180))
            if label:
                d.text((x0 + 4, y0 + 44), label, font=fs, fill=text)
            elif role == "장식":
                d.text((x0 + 4, y0 + 44), "장식", font=fs, fill=text)

    for label, col, row, wc, hr in TILES.get(name, []):
        x0, y0 = (GRID_X + CELL * col) * SCALE, (GRID_Y + CELL * row) * SCALE
        box = [x0, y0, x0 + CELL * wc * SCALE - 1, y0 + CELL * hr * SCALE - 1]
        d.rectangle(box, outline=ROLE_COLOR["타일"][0], width=7)
        d.text((x0 + 10, y0 + 10), label, font=font(24), fill=ROLE_COLOR["타일"][1])

    d.rectangle([GRID_X * SCALE, TITLE_Y0, (GRID_X + CELL * COLS) * SCALE - 1, TITLE_Y1],
                outline=(150, 150, 255, 255), width=4)
    d.text((GRID_X * SCALE + 8, TITLE_Y0 + 8), "제목 띠: 글자는 코드가 찍는다. 판만 그릴 것",
           font=fs, fill=(190, 190, 255, 255))
    d.rectangle([GRID_X * SCALE, inv_y * SCALE, (GRID_X + CELL * COLS) * SCALE - 1,
                 (inv_y + 76) * SCALE], outline=(255, 90, 90, 220), width=5)
    d.text((GRID_X * SCALE + 8, (inv_y - 13) * SCALE), "PLAYER INVENTORY: 격자 그리지 말 것",
           font=fs, fill=(255, 120, 120, 255))
    d.text((12, h - 30), f"CANVAS {w} x {h}", font=font(22), fill=(255, 255, 0, 255))
    return im


def main():
    for name, (rows, roles, default) in PAGES.items():
        w, h, inv_y = geom(rows)
        out = os.path.join(HERE, "src", name)
        os.makedirs(out, exist_ok=True)
        guide(name, rows, roles, default).save(os.path.join(out, "_guide.png"))
        ry = [((GRID_Y + CELL * r) * SCALE, (GRID_Y + CELL * (r + 1)) * SCALE - 1) for r in range(rows)]
        print(f"  {name:8} {w}x{h} · {rows}행 · 인벤 y {inv_y * SCALE}~{(inv_y + 76) * SCALE}")
        print(f"           행 y {' · '.join(f'r{i} {a}~{b}' for i, (a, b) in enumerate(ry))}")


if __name__ == "__main__":
    main()
