# Install as /etc/systemd/system/mcserver.service.d/velocity-backend.conf.
# The marker path is read by BlockShip without changing start.sh or its JVM flags.
[Service]
Environment=BLOCKSHIP_SHIP_MARKER_DIR=/home/ubuntu/mc-network/shared/ship-entities
