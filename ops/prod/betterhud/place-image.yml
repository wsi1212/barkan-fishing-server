# ★★이 파일은 gen_hud_sizes.py 가 생성한다. 손으로 고치지 말 것 —
#   크기 단계가 4 벌이라 손으로 고치면 한 벌만 바뀌고 나머지가 어긋난다.
#   좌표를 바꾸려면 gen_hud_sizes.py 의 STATUS/PLACE 를 고치고 다시 돌린다.
place_plate_sm:
  type: single
  file: status/place-plate.png
  setting:
    scale: 0.5

place_plate_md:
  type: single
  file: status/place-plate.png
  setting:
    scale: 0.66

place_plate_lg:
  type: single
  file: status/place-plate.png
  setting:
    scale: 0.85

place_plate_xl:
  type: single
  file: status/place-plate.png
  setting:
    scale: 1.0
