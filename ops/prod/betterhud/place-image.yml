# ★2026-08-09: 화면 대비 너무 커서 전부 0.66 배로 줄였다. 아트를 다시 굽지 않고
#   setting.scale 로 줄인다 — 어차피 0.66 은 비정수라 어느 쪽이든 리샘플이고,
#   숫자 하나만 되돌리면 원래 크기로 돌아온다.
#   ★scale 을 바꾸면 max(가장 넓은 이미지 폭)도 같이 바뀌어 레이아웃 x 가 전부 틀어진다.
# 왼쪽 위 위치·환경 HUD 그림. 아트는 gui-forge/build_status_hud.py 가 굽는다.
# 판이 150 인 이유: "바르칸 > 폭포_뒤_동굴_2층" 같은 지역명이 110 에는 안 들어간다.
# ★160 을 넘기면 글리프 아틀라스에 못 들어가 에러 없이 통째로 사라진다. 150 이 상한.
place_plate:
  type: single
  file: status/place-plate.png
  setting:
    scale: 0.66
