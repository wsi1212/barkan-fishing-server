# Every mcserver startup validates the actual public resource-pack bytes before
# Paper reads server.properties. --repair only updates the configured SHA1 when
# GitHub's mutable `latest` asset has changed; download failures abort startup
# rather than admitting players who are guaranteed to fail the mandatory pack.
[Service]
ExecStartPre=/home/ubuntu/mcserver/scripts/resourcepack-guard.sh --repair
