#!/usr/bin/env python3
# -*- coding: utf-8 -*-
"""7챕터 사이드 — 성소 밖 「도망친 심해어」 (2026-08-15 신설).

■ 왜 필요했나
  사이드가 마을별로 44 · 28 · 22 · 11 · 카지노 5 인데 **심해는 0개**다.
  Lv55 이상 사이드는 `왕사이드_견습생04`(왕립 어보) 하나뿐이라, Lv67에 심해로
  내려가면 **메인 10개 말고는 할 게 아무것도 없다.** 엔드게임이 통째로 비어 있다.

■ 무대 — 성소 **밖** 건물들
  교단이 기른 사역어 중 **탈주한 것들**이 성소 밖에 모여 산다. 지상으로는 못 나간다
  (물 밖에서 못 산다). 교단은 굳이 쫓지 않는다 — 어차피 갈 데가 없으니까.

  ★첫 만남이 곧 최고의 훅이다. 플레이어는 `심해26`에서 **동족의 비늘로 만든 가면**을
    만들었고 `심해27`에서 그걸 쓰고 넘어왔다. 그 얼굴을 하고 그들의 집에 도착한다.

      "…그 얼굴. 어디서 났나."

■ 이름 — 6챕터 주제의 뒤집기
  교단은 이들에게 **번호만** 붙였다. 6챕터에서 배운 것이 「봉인 = 이름을 아는 자가
  세상에 없게 만드는 것」이고 견습생 니나의 답이 「전부 적어 두면 아무도 못 지워요」였다.
  심해어는 **이름을 빼앗긴 채 살아 있는 존재**다 — `심사이드_일곱셋01`이 그 마지막 못이다.

■ ★사이드는 선택이다 — 메인의 무게를 사이드에 맡기지 않는다
  「수조의 것들은 되돌릴 수 없다」는 사실은 **메인 `심해28`에 직접 박았다**
  (`build_ch7_quests.py`). 사이드를 하나도 안 해도 `심해29`(수조 비우기)의 의미는 선다.
  사이드는 **확장만** 한다 — 그 사실을 당사자 입으로 다시 듣게 할 뿐이다.

■ 말투 — 심해어는 존댓말을 안 쓴다
  배운 적이 없어서다. 무례한 게 아니라 **그냥 다르다.** 문장이 짧고 주어를 자주 생략한다.
    모르       촌장 격. 오래 살아 말이 트였다 — 짧지만 문장이 완결된다
    일곱-셋    어리다. 단어를 띄엄띄엄 놓는다 ("나. 이름. 없다.")
    가라앉은 하 옛 파수. **교단 격식체가 배어 있는 유일한 개체** ("…하였다")
    비늘 짜는 이 손일하는 자. 짧고 부드럽다
  ★`fix_dialogue_tone.py`의 VOICE 맵에 넣지 말 것 — 5갈래 어디에도 안 맞는 별종이다.

■ 미완 — 「심해 어보」는 아직 못 만든다
  다른 네 마을엔 어보 캡스톤이 있는데(`add_village_capstones.py`) 심해엔 못 넣었다.
  **`fish.json`에 `심해` 지역이 없기 때문**이다(지금 있는 건 강·대양·오아시스 등 15곳).
  심해 어종 데이터가 생기면 `add_village_capstones.py`에 다섯 번째 항목으로 추가할 것.

사용법 — quests.json·npc.json·dialogue.json이 있는 디렉터리에서
         (★`build_ch7_quests.py` **뒤에**, `add_quest_difficulty.py` **앞에**):
    python3 build_ch7_side.py
    python3 build_ch7_side.py --dry
"""
import json, os, shutil, sys

sys.path.insert(0, os.path.dirname(os.path.abspath(__file__)))

QP, NP, DP = "quests.json", "npc.json", "dialogue.json"
DRY = "--dry" in sys.argv
Q = json.load(open(QP, encoding="utf-8"))
N = json.load(open(NP, encoding="utf-8"))
D = json.load(open(DP, encoding="utf-8"))
QUESTS, NPCS = Q["퀘스트"], N["npcs"]

# ══ NPC 5인 ═══════════════════════════════════════════════════════════════
# 색 규칙(CLAUDE.md): 퀘스트 주는 NPC = 초록 &a + [Q] / 대화만 = 하얀 &f
# ★citizensId는 비워 둔다 — 건물이 서고 Citizens로 소환한 뒤 채울 것(아래 TODO 출력).
NPC_DEFS = {
    "모르":       ("&a[Q] 모르", "탈주자들의 촌장 격. 옛 번호 사-일. 스스로 붙인 첫 이름"),
    "일곱셋":     ("&a[Q] 일곱-셋", "아직 번호로만 불리는 어린 개체"),
    "가라앉은하": ("&a[Q] 가라앉은 하", "옛 파수. 성소 내부를 안다"),
    "비늘짜는이": ("&a[Q] 비늘 짜는 이", "장비·가면 제작. 죽은 동족의 비늘만 쓴다"),
    "울지않는것": ("&f울지 않는 것", "그릇으로 개조되다 만 개체. 말을 못 한다"),
}
NPC_FLAGS = ["ferry", "shop", "inn", "smithy", "islandShop", "scrollShop", "guild",
             "submit", "cooking", "drillShop", "heal", "horseRental", "market",
             "quest", "casino", "appraisal", "villageQuest", "ranking"]

# ══ 사이드 7개 ════════════════════════════════════════════════════════════
# (id, giver, lv, 타입, 이름, 설명[], 목표[], 선행, 돈, exp)
# 보상은 같은 구간 메인의 40~55%로 잡는다(심해26 = 179,000원 / 22,300exp).
SIDE = [
    ("심사이드_모르01", "모르", 67, "이동", "&b그 얼굴, 어디서 났나",
     ["&7성소에서 떨어진 곳, 어둠 속에 불빛이 몇 점 있습니다.",
      "&7눈이 퇴화하지 않은 심해어들이 거기 삽니다 — &f도망친 것들&7입니다.",
      "&7그들이 당신을 봅니다. 정확히는 &f당신이 쓴 얼굴&7을 봅니다.",
      "&8당신은 그 가면을 동족의 비늘로 만들었습니다.",
      "&f모르&7를 찾아가세요."],
     ["visit|모르|모르"], "심해27", 70000, 9000),

    ("심사이드_모르02", "모르", 67, "재료", "&b번호를 지우다",
     ["&7모르: \"우리에겐 이름이 없었다. 번호가 있었다.\"",
      "&7\"등에 새긴다. 비늘에. 지우려면 비늘째 뜯어야 한다.\"",
      "&7뜯어 낸 비늘은 성소 밖 바닥에 흩어져 있습니다.",
      "&7&f재료 30개&7를 모아 오세요 — 그들의 번호를 거두는 일입니다.",
      "&8의뢰: &7모르"],
     ["material|아무|30"], "심사이드_모르01", 76000, 9600),

    ("심사이드_비늘01", "비늘짜는이", 68, "재료", "&b부화장의 유해",
     ["&7성소가 쓰다 버린 옛 부화장. 수조는 깨졌고 물만 고여 있습니다.",
      "&7비늘 짜는 이: \"산 것에서는 안 뜯는다. 죽은 것에서만 받는다.\"",
      "&7&f재료 40개&7 · &f작살로 A등급 이상 10마리&7.",
      "&8버려진 것들을 수습하는 일입니다. 아무도 안 하던 일입니다.",
      "&8의뢰: &7비늘 짜는 이"],
     ["material|아무|40", "harpoon|아무|A|10|0"], "심사이드_모르01", 84000, 10500),

    ("심사이드_일곱셋01", "일곱셋", 68, "복합", "&b이름 없는 자에게 이름을",
     ["&7어린 개체가 당신을 따라다닙니다. 아직 번호로만 불립니다.",
      "&7일곱-셋: \"나. 이름. 없다. 위에는— 이름. 많다?\"",
      "&7위의 물에서 본 것들을 보여 주세요. 그 애가 고를 겁니다.",
      "&7&f강·협곡·항구 도감 40종&7.",
      "&8왕도에서 배운 것의 반대편입니다. 지우는 대신, 붙여 주는 일.",
      "&8의뢰: &7일곱-셋"],
     ["dogam|강,협곡,항구|40"], "심사이드_모르01", 92000, 11400),

    ("심사이드_하01", "가라앉은하", 68, "물고기", "&b가라앉은 하의 기억",
     ["&7옛 파수 하나가 홀로 앉아 있습니다. 성소 안을 아는 유일한 자입니다.",
      "&7가라앉은 하: \"…말하여 주겠다. 다만 값이 있다.\"",
      "&7\"수조에 있을 적, 위에서 내려오던 것이 있었다. 그것을 다시 보고 싶다.\"",
      "&7&fS등급 이상 5마리&7를 그 앞에 놓으세요.",
      "&8그가 성소 안쪽 구조를 일러 줍니다.",
      "&8의뢰: &7가라앉은 하"],
     ["fish|아무|S|5|0"], "심사이드_모르01", 96000, 12000),

    ("심사이드_모르03", "모르", 69, "재료", "&b침몰선의 이름표",
     ["&7본거지 변두리에는 가라앉은 배들이 겹겹이 쌓여 있습니다.",
      "&7모르: \"저건 너희 것이다. 우리는 손대지 않았다.\"",
      "&7선원들의 이름표가 아직 붙어 있습니다. 위로 가져갈 수 있는 것들입니다.",
      "&7&f재료 50개&7 · &f작살로 A등급 이상 15마리&7.",
      "&8언젠가 이 이름들을 찾는 사람이 지상에 있을 겁니다.",
      "&8의뢰: &7모르"],
     ["material|아무|50", "harpoon|아무|A|15|0"], "심사이드_모르02", 104000, 13000),

    ("심사이드_비늘02", "비늘짜는이", 69, "복합", "&b고래의 길 끝",
     ["&7사면에 거대한 고래 사체가 걸려 있습니다. 온 생태가 여기 붙어 삽니다.",
      "&7비늘 짜는 이: \"저것 하나로 백 해를 산다. 죽어서도 먹인다.\"",
      "&7&f작살로 A등급 이상 20마리&7 · &f재료 40개&7.",
      "&8이자벨라가 말한 &f「고래가 가는 곳에 심해가 있다」&7의 끝자리입니다.",
      "&8의뢰: &7비늘 짜는 이"],
     ["harpoon|아무|A|20|0", "material|아무|40"], "심사이드_비늘01", 110000, 13800),
]

# ══ 대화 — 퀘스트마다 인사/진행중/퀘스트완료 3노드 (story.md 「대화 노드 규약」) ══
DLG = {
    "심사이드_모르01": (
        ["…멈춰라.",
         "그 얼굴. 어디서 났나.",
         "…대답 안 해도 된다. 나는 안다. 저 아래 수조에서 났다.",
         "벗어라. 우리 앞에서는 벗어라. 그러면 말하겠다."],
        ["아직 서 있나. 벗으라 했다."],
        ["…좋다. 얼굴 밑에 사람이 있었군.",
         "나는 모르다. 전에는 사-일이었다. 그건 이름이 아니다.",
         "여기 있는 것들은 전부 도망친 것들이다. 위로는 못 간다. 물 밖에서는 못 산다.",
         "그래도 여기 산다. 그게 다다."]),
    "심사이드_모르02": (
        ["우리에겐 이름이 없었다. 번호가 있었다.",
         "등에 새긴다. 비늘에. 지우려면 비늘째 뜯어야 한다.",
         "뜯은 것들은 바닥에 버렸다. 서른쯤 거두어 오라.",
         "…버려 둔 게 마음에 걸린다. 그것도 우리였다."],
        ["급할 것 없다. 그것들은 도망 안 간다."],
        ["…다 거두었군.",
         "이제 저것들에게도 이름을 붙일 수 있겠다. 죽은 뒤에라도.",
         "고맙다. 이 말은 위에서 배웠다."]),
    "심사이드_비늘01": (
        ["산 것에서는 안 뜯는다. 죽은 것에서만 받는다.",
         "옛 부화장. 저기 버려진 것들이 있다. 수습해야 한다.",
         "재료 마흔. 그리고 지키는 것들이 붙어 있으니 작살로 열."],
        ["서두르지 마라. 손이 떨리면 비늘이 상한다."],
        ["…수습했군.",
         "너는 우리 얼굴을 쓰고 왔다. 그런데 우리 시체를 거두어 주었다.",
         "둘 다 사실이다. 나는 둘 다 기억하겠다."]),
    "심사이드_일곱셋01": (
        ["…너. 위에서. 왔다?",
         "나. 이름. 없다. 일곱-셋. 그건 자리다. 이름 아니다.",
         "위에는— 이름. 많다? 보여줘. 나. 고른다."],
        ["아직? 나. 기다린다. 오래 기다렸다."],
        ["…많다. 이렇게 많다.",
         "이거. 이 이름. 나 가진다.",
         "…이제 나. 일곱-셋 아니다."]),
    "심사이드_하01": (
        ["…내려온 자여. 가까이 오라.",
         "나는 파수였다. 수조 곁에서 백 해를 서 있었다.",
         "성소 안이 어떻게 생겼는지 말하여 주겠다. 다만 값이 있다.",
         "수조에 있을 적, 위에서 가끔 내려오던 것이 있었다. 큰 것. 빛나는 것.",
         "다섯이면 족하다. 다시 한 번 보고 싶을 뿐이다."],
        ["…기다림은 익숙하다. 백 해를 서 있었다 하지 않았나."],
        ["…아아. 이것이었다. 이것이 내려오곤 했다.",
         "약속대로 말하겠다. 안쪽 수조는 셋이고, 기록고는 그 너머다.",
         "그리고 하나 더 — 수조의 것들은 되돌릴 수 없다. 이미 그릇이다.",
         "네가 그것들을 어찌하든, 그건 죽이는 게 아니다. 이미 죽어 있다."]),
    "심사이드_모르03": (
        ["저건 너희 것이다. 우리는 손대지 않았다.",
         "가라앉은 배. 겹겹이 쌓였다. 이름표가 아직 붙어 있다.",
         "쉰 개쯤. 지키는 것들이 사나우니 작살도 챙겨라.",
         "…언젠가 저 이름을 찾는 자가 위에 있을 것이다."],
        ["바닥은 깊다. 서두르면 길을 잃는다."],
        ["…가져가라. 전부.",
         "우리는 이름을 빼앗겼고, 저들은 이름만 남았다.",
         "이상하지. 결국 같은 것을 잃은 셈이다."]),
    "심사이드_비늘02": (
        ["저 고래. 저것 하나로 백 해를 산다.",
         "죽어서도 먹인다. 우리를, 저기 붙은 것들 전부를.",
         "붙은 것들이 사나워졌다. 스물쯤 걷어 내라. 재료도 마흔."],
        ["고래는 안 썩는다. 천천히 해도 된다."],
        ["…끝냈군.",
         "위의 사람들은 고래를 잡는다더군. 우리는 고래를 기다린다.",
         "어느 쪽이 옳은지는 모르겠다. 그저 다르다."]),
}
ASK = [{"id": "c1", "text": "들어볼게요", "action": "퀘스트목록", "next": "x"},
       {"id": "c2", "text": "나중에 오겠습니다", "action": "닫기", "next": "x"}]
TAKE = [{"id": "c1", "text": "보상 받기", "action": "퀘스트목록", "next": "x"}]

# 말 못 하는 개체 — 퀘스트 없이 존재만으로 `심해29`의 무게를 받친다.
MUTE_DLG = {
    "인사": {"lines": ["…",
                     "&8(입을 벌리지만 소리가 나지 않습니다. 아가미가 잘못 자라 있습니다.)",
                     "&8(수조에서 그릇으로 개조되다 만 개체라고, 모르가 말해 주었습니다.)"],
             "choices": []},
    "첫만남": {"lines": ["…",
                      "&8(그것이 당신을 오래 봅니다. 정확히는 당신이 벗어 둔 가면을 봅니다.)"],
              "choices": []},
}

# ══ 적용 ═══════════════════════════════════════════════════════════════════
for key, (name, _desc) in NPC_DEFS.items():
    if key in NPCS:
        continue
    e = {"citizensId": "", "name": name}
    for f in NPC_FLAGS:
        e[f] = False
    e["quests"] = []
    NPCS[key] = e

for qid, giver, lv, typ, name, desc, goals, prereq, money, exp in SIDE:
    if qid in QUESTS:
        sys.exit(f"✗ {qid} 이 이미 있다 — 두 번 적용됐다")
    if prereq not in QUESTS:
        sys.exit(f"✗ 선행 {prereq} 이 없다 — build_ch7_quests.py 를 먼저 돌릴 것")
    QUESTS[qid] = {
        "id": qid, "이름": name, "설명": desc, "목표": goals, "타입": typ,
        "카테고리": "사이드", "필요레벨": lv, "선행퀘스트": prereq,
        "보상돈": money, "보상경험치": exp,
    }
    qs = NPCS[giver].setdefault("quests", [])
    if qid not in qs:
        qs.append(qid)
    g, pr, dn = DLG[qid]
    d = D.setdefault(giver, {})
    d[f"인사/{qid}"] = {"lines": g, "choices": ASK}
    d[f"진행중/{qid}"] = {"lines": pr, "choices": []}
    d[f"퀘스트완료/{qid}"] = {"lines": dn, "choices": TAKE}

D.setdefault("울지않는것", {}).update(MUTE_DLG)

if not DRY:
    for path, obj in [(QP, Q), (NP, N), (DP, D)]:
        shutil.copy(path, path + ".pre-ch7side")
        with open(path, "w", encoding="utf-8") as f:
            json.dump(obj, f, ensure_ascii=False, indent=2)

# ══ 리포트 ═════════════════════════════════════════════════════════════════
print("\n" + "=" * 78)
print(f"심해 사이드 {len(SIDE)}개 · NPC {len(NPC_DEFS)}명 · 대화 {len(SIDE) * 3 + 2}노드\n")
for qid, giver, lv, _t, name, _d, goals, prereq, money, exp in SIDE:
    print(f"  Lv{lv} {qid:18} {NPC_DEFS[giver][0][6:]:12} ← {prereq:16} {' + '.join(goals)[:40]}")

print("\n★배포 전 필수 — Citizens NPC 5명을 세우고 citizensId를 채울 것:")
for key, (name, role) in NPC_DEFS.items():
    print(f"  {key:12} {name:16} {role}")
print("  (citizensId가 비면 길안내 화살표가 실시간 위치를 못 찾아 안내좌표로만 떨어진다)")
print("★건물 좌표가 정해지면 각 퀘스트에 `안내좌표`(월드,x,y,z)도 넣을 것.")
print("★지역 등록(인게임 `/지역 생성`):")
print("  심해협곡      world 월드의 협곡 **입구 부분만** (`심해20`)")
print("  심해교단본부   deep_sea 월드 **전체**. `심해27`의 area 목표가 이걸 본다")
print("  무명의성소    성소 **건물 내부** (`심해28`). 심해교단본부의 하위")
print("  이름있는자리   도망친 심해어 마을. 역시 심해교단본부의 하위")
print("                → 하위로 걸면 dogam|심해교단본부|N 하나로 전부 포함된다")
print("★「심해 어보」는 아직 못 만든다 — fish.json에 `심해` 지역이 없다.")
print("  심해 어종이 생기면 add_village_capstones.py 에 다섯 번째 항목으로 추가할 것.")

# ── 검증 ───────────────────────────────────────────────────────────────────
bad = []
owned = {q for v in NPCS.values() for q in (v.get("quests") or [])}
for qid, giver, *_ in [(s[0], s[1]) for s in SIDE]:
    if qid not in owned:
        bad.append(f"{qid} NPC 미배정")
    if f"인사/{qid}" not in D.get(giver, {}):
        bad.append(f"{qid} 대화 노드 없음")
mix = [s[0] for s in SIDE
       if any(g.startswith("visit|") for g in s[6]) and len(s[6]) > 1]
if mix:
    bad.append(f"visit 혼합: {mix}")     # onVisit이 다른 목표를 통째로 무시한다
print("\n검증:", bad if bad else "이상 없음")
if bad:
    sys.exit("✗ 검증 실패")
print(f"\n{'(드라이런 — 저장 안 함)' if DRY else '✓ 완료.'} ★다음: add_quest_difficulty.py")
