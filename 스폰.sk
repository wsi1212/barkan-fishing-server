# 스폰 명령어

command /스폰:
    aliases: /넴주, /tmvhs, /spawn
    trigger:
        make player execute command "execute in minecraft:overworld run tp @s 0 100 0"
        # 개인 월드보더 초기화 (길드 섬에서 나올 때)
        send "&a스폰으로 이동했습니다." to player
