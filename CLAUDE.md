# Fish - Minecraft Skript 낚시 플러그인

## 프로젝트 개요
마인크래프트 서버용 Skript 기반 낚시 시스템. 지역별 물고기, 등급 시스템, 레벨링, 재화, 낚시대회, NPC 퀘스트, 장비 조립, 아이스박스, 미니게임, 페리 등을 포함한 종합 낚시 컨텐츠.

## 스토리 & 세계관
메인 스토리 설정은 [story.md](story.md) 참고. 바르칸 열도 세계관, 세력 구성, 섬 구성(10개), 메인 퀘스트 흐름, 장기 업데이트 구조 등 포함.

## 기술 스택
- **언어**: Skript (Minecraft 서버 스크립팅 언어)
- **플랫폼**: Minecraft Java Edition 서버 (Feather Player)
- **Skript**: 2.13.2

### 설치된 플러그인
| 플러그인 | 버전 | 역할 |
|----------|------|------|
| Skript | 2.13.2 | 코어 스크립팅 엔진 |
| SkBee | 3.15.0 | Skript 에드온 (보스바, NBT, 구조물 등) |
| SkQuery | - | Skript 에드온 (추가 표현식/이벤트) |
| skRayFall | 1.9.29 | Skript 에드온 (파티클, 홀로그램, 지역 이벤트 등) |
| MarSkRebirth | 1.0.3 | Skript 에드온 — **지역(area) 관리**, 커맨드: `/ska`. `enter area at` / `exit area at` 이벤트, 지역 생성/삭제, 플레이어 지역 감지 등 제공 |
| WorldEdit | 7.4.0-beta | 월드 편집 도구 |
| Multiverse-Core | 4.3.11 | 멀티 월드 관리 |
| SuperVanish | 6.2.21 | 투명화 플러그인 |

### 지역(Area) 시스템 — MarSkRebirth
- WorldGuard 대신 **MarSkRebirth**가 지역 관리 담당
- 지역 데이터: `plugins/MarSkRebirth/area.sk` (YAML 형식)
- 지역 관리 명령어: `/ska` (지역 생성/삭제/목록 등)
- Skript 이벤트: `enter area at "이름"` / `exit area at "이름"`
- Skript 조건: `IsInArea` (플레이어가 지역 안에 있는지)
- 현재 정의된 지역:
  - `바르칸물길` — flatroom 월드, -121,-61,17 ~ 15,2,41
  - `바르칸동굴` — flatroom 월드, -50,-61,-21 ~ 15,2,15
  - `lavatest` — flatroom 월드, 5,-61,-84 ~ 36,-44,-56

## 파일 구조 및 역할

### 핵심 시스템
| 파일 | 역할 |
|------|------|
| `낚시.sk` | **핵심 낚시 로직** - 물고기 잡기, 등급 확률, 크리티컬, 도주, 경험치, 도감, 낚시대회 |
| `지역별물고기.sk` | **물고기 DB** - 전체 물고기 데이터 정의 + 지역별 물고기 풀 (`get_fish` 함수) |
| `낚시레벨.sk` | **레벨 시스템** - 레벨업, 보너스 확인, 레벨 랭킹 |
| `낚시미니게임.sk` | **낚시 미니게임** - 등급별 타이밍 미니게임, 난이도/도주 파라미터 |
| `날씨시간.sk` | **시간/날씨 보너스** - 시간대(낮/저녁/밤/새벽)와 날씨(맑음/비/뇌우)별 낚시 보너스 |
| `돈.sk` | **재화 시스템** - 잔액 확인, 출금(종이 돈), 송금, 관리자 설정 |
| `판매.sk` | **판매 시스템** - 물고기 판매 (명령어 + 상점 GUI), 가격 = 등급배율 × 크기 |

### NPC & 퀘스트 시스템
| 파일 | 역할 |
|------|------|
| `NPC.sk` | **NPC 대화 엔진** - NPC 우클릭 상호작용, 대화 시퀀스, 퀘스트 수락/완료 대화 |
| `퀘스트.sk` | **퀘스트 코어** - 수락, 진행체크, 보상, 포기, 일일/주간 퀘스트 배정, GUI |
| `퀘스트데이터.sk` | **퀘스트 DB** - 메인/일일/주간 퀘스트 데이터 정의 (on load) |
| `퀘스트훅.sk` | **퀘스트 브릿지** - 낚시/판매/레벨업/지역입장 등에서 퀘스트 진행 업데이트 |

### 장비 & 아이템 시스템
| 파일 | 역할 |
|------|------|
| `장비.sk` | **장비** - 장비 상자, 초음파 탐지기 (현재 물고기 목록 조회) |
| `장비시스템.sk` | **장비 작업대** - 낚싯대+부품 조립/분해, 스탯 표시 GUI (`/장비`) |
| `부품데이터.sk` | **부품 DB** - 낚싯대 15종 + 릴/줄/바늘/미끼/찌 데이터 정의 |
| `부품상점.sk` | **부품 상점** - 카테고리별 부품 구매 GUI (`/부품상점`) |
| `아이스박스.sk` | **아이스박스** - 물고기 보관함 (9단계 업그레이드, 신선도 보존 배율) |

### 월드 & UI
| 파일 | 역할 |
|------|------|
| `공간.sk` | **지역 감지** - MarSkRebirth 지역 입장/퇴장 처리, 용암 낚시 상태 관리, 방문 지역 기록 |
| `보트.sk` | **보트 시스템** - 연료제 보트 소환, 3단 속도 조절, 속도 이펙트, 인양 시스템 |
| `메뉴.sk` | **메뉴 GUI** - `/메뉴` 명령어, 지역별 물고기도감 GUI |
| `gui.sk` | **GUI 빌더** - 범용 GUI 생성/수정/삭제 (관리자용) |
| `페리.sk` | **페리 시스템** - 지역간 배 이동, 노선 관리, 요금, 보스바 카운트다운 |
| `팁.sk` | **팁 시스템** - 3분마다 랜덤 팁 채팅 표시, `/팁` 즉시 전송 |

### 유틸리티
| 파일 | 역할 |
|------|------|
| `변수관리.sk` | **변수 초기화** - 첫 접속 시 돈 0 설정 |
| `물빠짐.sk` | **물 텔레포트** - 물속 N초 이상 체류 시 강제 이동 |
| `랜덤블럭.sk` | **관리자 도구** - 10x10 범위 랜덤 블럭 배치 |
| `name.sk` | `/name` - 아이템 이름 변경 |
| `명령어.sk` | `/아이템확인` - 손에 든 아이템 정보 출력 |
| `test.sk` | 빈 테스트 파일 |

## 핵심 시스템

### 등급 시스템
E < D < C < B < A < S < S+ < M < L < G (총 10등급)
- 확률 기반 등급 결정 (E가 가장 높은 확률, G가 0.01%)
- 레벨에 따라 상위 등급 물고기 해금 (Lv20: S+, Lv30: M, Lv40: L, Lv50: G)

### 물고기 데이터 포맷
```
"이름|최소크기|최대크기|등급" (기본, 도주확률 없음)
"이름|최소크기|최대크기|등급|도주확률" (S+ 이상)
```

### 지역 (현재 3개)
- `바르칸물길` - 한국 민물고기 (E~S등급, 24종)
- `바르칸동굴` - 동굴 물고기 (E~S등급, 26종)
- `lavatest` - 용암 물고기 (C~M등급, 14종, 용암 낚싯대 필요)

### 퀘스트 시스템
- **카테고리**: 메인, 일일, 주간, 반복
- **일일 퀘스트**: 매일 00:00 리셋, 난이도별(쉬움/보통/어려움/전문) 4개 배정
- **주간 퀘스트**: 매주 월요일 리셋, 주간 풀에서 랜덤 배정 (39종)
- **목표 타입**: `fish|이름|등급|수량|최소크기`, `level|레벨`, `area|지역`, `sell|횟수`, `money|금액`, `login|일수`, `quest_daily|횟수`
- **보상 사운드**: 일반 완료 → `entity.player.levelup`, 올클/어려운 메인(Lv20+) → `ui.toast.challenge_complete`
- **GUI**: `/q`로 접근, 일일/주간/메인 탭, 페이지네이션 지원

### NPC 대화 시스템
- NPC 우클릭으로 대화 시작 → 퀘스트 수락/완료 처리
- 대화 데이터는 `on load:`에서 초기화
- 변수: `{대화진행::%player%}`, `{대화NPC::%player%}`, `{대화세션::%player%}`

### 장비 부품 시스템
- **부품 유형**: 낚싯대, 릴, 줄, 바늘, 미끼, 찌 (6종)
- **스탯**: 도망감소, 크리배율, 등급업, 경험치, 크기, 크리확률, 난이도, 더블찬스, 내구보존, 판매보너스
- **작업대** (`/장비`): 낚싯대 + 5부품 슬롯 조립/분해
- **상점** (`/부품상점`): 카테고리별 부품 구매 GUI

### 아이스박스 시스템
- 9단계 업그레이드 (3슬롯 → 45슬롯)
- 신선도 보존 배율 (1.5x → 12x)

### 낚시 미니게임
- 등급별 파라미터: 존 너비(E:10→G:1), 퍼펙트 윈도우, 틱 속도, 도주 확률
- 도주 실패 시 확률 점증

### 페리 시스템
- 노선별 출발지/도착지, 간격, 소요시간, 요금 설정
- NPC 대화로 탑승 (페리 NPC 우클릭 → GUI → 노선 선택)
- 방문한 지역만 이용 가능 (`{방문지역::%player%::*}`)
- 명령어: `/페리설정`, `/페리시작`, `/페리정지`, `/페리삭제`, `/페리탑승`(op), `/페리`, `/페리취소`

### 보트 시스템
- `/보트`로 소환 (연료비 100원), 티어에 따라 연료 120~600초
- 9단계 티어 업그레이드 (`/보트상점`): 나무→강화→철제→금→다이아→에메랄드→흑요석→네더라이트→전설
- 연료 소진 시 인양 (안전위치로 이동, 인양비 최대 500원 차감)
- 보스바로 연료 잔량 표시 (색상 변화: 초록→노랑→빨강)

### 팁 시스템
- 3분마다 랜덤 팁 회색 채팅 표시 (24종: 조작, 낚시, 지역, 퀘스트, 도감)
- `/팁` (op) - 즉시 랜덤 팁 전체 전송

## 주요 변수 (글로벌)

### 기본
- `{돈::%player%}` - 소지금
- `{낚시레벨::%player%}` - 낚시 레벨
- `{낚시레벨현재경험치::%player%}` / `{낚시레벨필요경험치::%player%}` - 경험치
- `{area::%player%}` - 현재 위치한 낚시 지역
- `{도감최대::%물고기%::%player%}` - 도감 최대 크기 기록
- `{용암낚시::%player%}` - 용암 낚시 상태 (1/0)

### 퀘스트
- `{활성퀘스트::%player%::*}` - 활성 퀘스트 목록
- `{퀘스트상태::%questId%::%player%}` - 퀘스트 상태 (대기/진행/완료대기/완료)
- `{퀘스트진행::%questId%::%player%::%objIdx%}` - 목표 진행도
- `{일일퀘스트::%player%::*}` / `{주간퀘스트::%player%::*}` - 일일/주간 퀘스트 풀
- `{일일퀘스트날짜::%player%}` / `{주간퀘스트주차::%player%}` - 리셋 추적
- `{주간접속일수::%player%}` - 이번주 접속 일수
- `{주간일퀘완료수::%player%}` - 이번주 일퀘 완료 수

### NPC
- `{NPC목록::*}` - NPC 목록
- `{NPC::%npcId%::이름}` / `{NPC::%npcId%::퀘스트::*}` - NPC 데이터
- `{대화진행::%player%}` / `{대화NPC::%player%}` / `{대화세션::%player%}` - 대화 상태

### 장비
- `{낚싯대부품::%슬롯%::%player%}` - 장착된 부품 (릴/줄/바늘/미끼/찌)
- `{부품::%유형%::%이름%}` - 부품 데이터 ("표시명|등급|가격|내구도|스탯키:스탯값")
- `{아이스박스::%player%}` - 아이스박스 단계 (1~9)

### 페리
- `{페리노선목록::*}` - 노선 목록
- `{페리::%노선%::출발지}` / `{페리::%노선%::도착지}` / `{페리::%노선%::요금}` 등

### 보트
- `{보트등급::%player%}` - 보트 티어 (1~9, 기본 1)
- `{플레이어보트::%player%}` - 보트 엔티티
- `{소환보트::%uuid%}` - 소환 보트 플래그
- `{보트탑승중::%player%}` - 탑승 중 여부 (true/false)
- `{보트연료::%player%}` - 남은 연료 (초)
- `{보트최대연료::%player%}` - 최대 연료 (초, 티어 기반)

### 기타
- `{낚시대회::*}` / `{낚시대회물고기::*}` - 낚시대회 데이터
- `{gui::이름::*}` - GUI 데이터
- `{지역리스트::*}` - 낚시 지역 목록
- `{waterTP::*}` - 물 텔레포트 설정
- `{팁목록::*}` / `{팁총수}` - 팁 데이터

## 주요 함수

### 낚시 핵심
- `get_fish(지역명) :: texts` - 해당 지역 물고기 리스트 반환 (`지역별물고기.sk`)
- `rankchange(등급) :: number` - 등급→숫자 변환 (`낚시.sk`)
- `isBetter(a, b) :: boolean` - 두 낚시 기록 비교 (`낚시.sk`)
- `fishprice(등급, 크기) :: number` - 판매 가격 계산 (`판매.sk`)
- `parsegrade(item) :: text` / `parsesize(item) :: number` - 아이템 파싱 (`판매.sk`)
- `isfish(item) :: boolean` - 물고기 아이템 여부 (`판매.sk`)

### 퀘스트
- `퀘스트수락(p, questId)` - 퀘스트 수락 (`퀘스트.sk`)
- `퀘스트즉시체크(p, questId)` - 즉시 완료 가능 조건 체크 (`퀘스트.sk`)
- `퀘스트진행체크(p, questId)` - 목표 달성 여부 체크 (`퀘스트.sk`)
- `퀘스트보상(p, questId)` - 보상 지급 (`퀘스트.sk`)
- `일일퀘스트리셋(p)` / `일일퀘스트수락(p, 난이도)` - 일일 퀘스트 (`퀘스트.sk`)
- `주간퀘스트체크(p)` - 주간 퀘스트 배정/리셋 (`퀘스트.sk`)

### 퀘스트 훅 (브릿지)
- `퀘스트_물고기(p, fishName, grade, size)` - 물고기 낚았을 때 (`퀘스트훅.sk`)
- `퀘스트_레벨업(p, lv)` - 레벨업 시 (`퀘스트훅.sk`)
- `퀘스트_지역입장(p, area)` - 지역 입장 시 (`퀘스트훅.sk`)
- `퀘스트_판매(p, count, price)` - 판매 시 (`퀘스트훅.sk`)
- `퀘스트_접속(p)` - 접속 시 (주간 로그인 추적) (`퀘스트훅.sk`)
- `퀘스트_일퀘완료(p)` - 일퀘 완료 시 (주간 카운터) (`퀘스트훅.sk`)

### NPC
- `npc대화시작(p, npcId)` - NPC 대화 시작 (`NPC.sk`)
- `npc진행중대화(p, npcId, questId)` - 진행중 퀘스트 대화 (`NPC.sk`)

### 장비
- `openWorkbench(p)` - 장비 작업대 열기 (`장비시스템.sk`)
- `openPartShop(p, category)` - 부품 상점 열기 (`부품상점.sk`)

### 미니게임
- `미니게임파라미터(grade) :: text` - 등급별 미니게임 파라미터 (`낚시미니게임.sk`)
- `물고기난이도(grade) :: number` - 등급→난이도 숫자 (`낚시미니게임.sk`)

### 시간/날씨
- `현재시간대(p) :: text` - 시간대 반환 (`날씨시간.sk`)
- `현재날씨(p) :: text` - 날씨 반환 (`날씨시간.sk`)

### 아이스박스
- `iceboxName(id) :: text` / `iceboxSlots(id) :: number` / `iceboxMultiplier(id) :: number` (`아이스박스.sk`)

### 기타
- `enterlava(p)` / `exitlava(p)` - 용암 낚시 토글 (`공간.sk`)
- `regiondictionary(p, 지역)` / `regiondictionarygui(p)` - 도감 GUI (`메뉴.sk`)
- `fishtimer(분)` - 낚시대회 타이머 (`낚시.sk`)

## 주요 명령어
- `/메뉴` - 메인 메뉴 열기
- `/q` (또는 `/퀘스트`) - 퀘스트 GUI
- `/돈 [출금|송금|설정]` - 재화 관리
- `/낚시레벨` - 레벨 확인
- `/낚시레벨보너스` - 현재 레벨 보너스 확인
- `/낚시레벨랭킹 [수]` - 레벨 랭킹
- `/낚시대회 [시작|순위|강제종료]` - 낚시대회
- `/판매 [전부]` - 물고기 판매
- `/상점` - 물고기 상점 GUI
- `/장비` - 장비 작업대
- `/부품상점 [카테고리]` - 부품 상점
- `/보트` - 연료제 보트 소환 (100원)
- `/보트상점` - 보트 업그레이드 상점 (9단계)
- `/페리` - 페리 이용 안내
- `/페리취소` - 페리 탑승 취소
- `/페리설정`, `/페리시작`, `/페리정지`, `/페리삭제`, `/페리탑승` - 페리 관리 (op)
- `/팁` - 랜덤 팁 즉시 전송 (op)
- `/팁목록` - 전체 팁 목록 확인 (op)
- `/name <텍스트>` - 아이템 이름 변경
- `/아이템확인` - 손에 든 아이템 정보
- `/watertp [setdest|setseconds]` - 물 텔레포트 설정 (권한: watertp.admin)
- `/랜덤블럭10` - 10x10 랜덤 블럭 배치 (권한: op)
- `gui [생성|수정|삭제] <이름> [rows]` - GUI 관리 (관리자용)

## 코드 컨벤션
- 변수명/함수명은 한글 사용 (일부 영문: `waterTP`, `get_fish`, `gui`, `iceboxName` 등)
- 로컬 변수: `{_변수명}`, 글로벌 변수: `{변수명::키}`
- 주석은 `#`으로 작성
- 색상 코드: `&f`(흰색), `&a`(초록), `&c`(빨강), `&e`(노랑), `&7`(회색), `&b`(하늘), `&0`(검정), `&8`(짙은회)
- GUI 클릭 핸들러에서 슬롯 번호는 반드시 `index of clicked slot` 사용 (`clicked slot`은 Slot 타입이라 변수 경로에 사용 불가)
- `continue` 키워드가 중첩 loop > if에서 동작하지 않음 → boolean 플래그 패턴 사용
- **한글 명령어 영타 별칭 필수**: 한글 명령어를 추가할 때 반드시 영문 키보드(QWERTY) 상태에서 같은 키를 눌렀을 때의 문자열을 `aliases`로 등록해야 함. 예: `/섬` → `/tja`, `/스폰` → `/vmfhd`, `/넴주` → `/spaWn`(X) → 실제 키매핑 기준. 한글→영타 매핑: ㅂ=q ㅈ=w ㄷ=e ㄱ=r ㅅ=t ㅛ=y ㅕ=j ㅑ=i ㅐ=o ㅔ=p ㅁ=a ㄴ=s ㅇ=d ㄹ=f ㅎ=g ㅗ=h ㅓ=j ㅏ=k ㅣ=l ㅋ=z ㅌ=x ㅊ=c ㅍ=v ㅠ=b ㅜ=n ㅡ=m

## Skript 코딩 주의사항 (해결 완료, 참고용)
- **`clicked slot` vs `index of clicked slot`**: GUI 클릭 핸들러에서 변수 키로 사용할 때 반드시 `index of clicked slot`으로 정수 추출 필요 (Slot 타입은 변수 경로에서 정수로 변환 안 됨)
- **`continue` in nested loop**: Skript 2.13.2에서 `loop > if > continue` 조합이 정상 작동하지 않음 → `{_skip}` boolean 플래그 패턴으로 우회
