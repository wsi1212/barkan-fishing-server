# 튜토리얼 도입 컷씬 — 「바르칸으로 가는 배」 설계

작성 2026-08-05. 신규 플레이어 첫 접속 서사(어부가 되겠다고 2년간 갑판 선원으로 일한 끝에 바르칸에 도착)를 컷씬으로 만든다.
★2026-08-20 도입부 개편 — 옛 「나는 고아다」 전제는 폐기했다. 이유는 3개: ①플레이어가 왜 배에 타고 있는지를
설명하지 못했다(밀항인지 손님인지 불명) ②플레이어 캐릭터에 「부모 없음」을 강제로 박았다 ③2년 허드렛일 =
이미 지불한 시간이라 튜토리얼의 존재 이유(배는 탔지만 낚시는 못 배웠다)까지 서사가 만들어 준다.
선원 바스티아노의 「뱃삯 대신 갑판 닦은 값은 했으니 빚은 없다」 대사가 이미 이 전제를 반쯤 깔고 있었다.
1막(망망대해 대사)·선원 NPC·채팅 억제는 **이미 구현 완료**(`TutorialIntroManager`), 이 문서는 **3막 입항 컷씬**의 설계다.

관련: [design.md](design.md) · 기존 튜토 트레일은 memory `project_tutorial_npc_trail`

---

## 1. 실측으로 확정된 하드 제약

컷씬 설계를 좌우하는 두 제약. **둘 다 prod 실측**(mc_heightmap / mc_inspect_volume).

### 1-1. 배에서 항구까지 항해는 물리적으로 불가
- 1막 배 위치 `2908, -3144` → 항구 부두 `400, 941` = **직선 4,840블록**
- 범선 최고속(1.2블록/틱 = 24블록/초)으로도 3분 24초
- 경로 대부분이 **미생성 대양 청크 ~300개** → 청크 GEN 병목(memory `project_prod_load_test_and_chunky`: 100명 51초 프리즈)을 신규 유저 첫 접속에 정면으로 밟는다
- 게다가 `ShipCollider.canMoveTo`는 미로드 청크로의 이동을 **거부** → 배가 그냥 멈춘다

**결론: 순간이동 구간이 반드시 필요하다.** → 2막 암전이 이 4,840블록을 "몇 시간의 항해"로 서술적으로 흡수한다.

### 1-2. 항구 입구는 아치 수문 하나뿐이고 높이 제한이 있다
- 항구는 방파제로 완전히 둘러싸인 內港. 방파제는 `x300 → x487` **연속, 틈 없음**
- 유일 입구 = **아치 수문** `x369~389, z1131~1141` (통항폭 21, 터널길이 11)
- **수면 y = 60** (전 수역 공통)
- **개구부 높이 — 2026-08-06 `mc_inspect_volume`으로 직접 정밀 재측정** (중심선 x379, 최협층은 z1136):

| 절대 y | 개구부(z1136) | 비고 |
|---|---|---|
| ~y75 | `x370~388` 전폭 | 선체(폭 9 = x375~383) 여유롭게 통과 |
| y78 | `x374~383` | 돛대(폭 3 = x378~380) 여유 4/3 |
| **y79** | **`x376~382`** | **★진짜 병목. 여유 2/2** |
| y80 | `x373~385` **막힘** | 배는 여기까지 올라가지 않음 |
| y81~82 | 완전 폐쇄 | 아치 키스톤 |

  ★초기 조사(서브에이전트)의 "y78→x374~384, y80→x379만"은 **z1134 기준**이었고 실제 병목은
  **z1136의 y79층**이다. 또한 선체 ItemDisplay는 `pivot_y 9.5`에 놓이고 모델이 ±9.5로 뻗으므로
  **시각적 최상단은 블루프린트 y18(=절대 y78)이 아니라 절대 y79**다 — 돛대 끝이 키스톤 1블록 아래를
  스치며 지나간다(좌우 여유 2블록). 통과는 확실하고, 연출로는 아슬아슬해서 오히려 좋다.
  ※시각적 상단 y79는 pivot 계산에 근거한 추정 — **스크린샷 1장으로 육안 확인 필요**(미완).
  더 여유가 필요하면 `BoatArrivalCutscene.SEA_Y`를 59로 내리면 된다(흘수 1블록 = 현실적).

| 배 | 폭 | 돛대 최고(수면기준) | 수문 통과 |
|---|---|---|---|
| 1막 블록 배(유저 빌드, `2908,-3144`) | 14 | **y97 = 수면+37** | ❌ 불가 (3배 초과) |
| `범선` 프리셋(`ships/범선.json`, 구운 모델) | 9 | **y78 = 수면+18**, 단 y75~78 구간은 폭 3(x-1..+1) | ✅ 양옆 3.5블록 여유 |

**결론: 컷씬 배는 `범선` 프리셋이어야 한다.** 유저 빌드 배는 수문을 못 지나므로 1막 전용으로 남고,
작은 `범선`만 입항할 수 있다 — 제약이 설계와 맞물린다.
(1막 배와 3막 배의 외형이 다른 건 알려진 비용. 암전이 사이에 있어 대부분 눈치채지 못한다.)

---

## 2. 막 구성

### 1막 — 망망대해 (구현 완료)
갑판 기상(`384, 65, 1293` — 방파제에서 150블록. 옛 `2908,-3144`은 낡음) → 독백 11줄, 줄 길이에 비례한
2.1~4.1초 간격(`TutorialIntroManager.lineDelay`), 그 동안 전체채팅/등급공지/날씨공지/길드홍보/서버팁 전부 억제 →
문 밖 선원(`&a[Q] 선원 바스티아노`, CID 160)에게 말 걸기 → `[뱃머리로 나간다]`(dialogue action `항해`).

**선원 대화는 분기 3갈래**(2026-08-20) — `인사` → `바르칸`(열도 소개) → `길드`(요한·회관 위치) / `2년`(동승 회상).
모든 갈래에 `[뱃머리로 나간다]`가 있어 어디서 끊어도 컷씬으로 수렴한다. 정보(길드가 목적지)와 정(2년)을
선택으로 분리해서, 서사에 관심 없는 유저는 첫 화면에서 바로 출항할 수 있다.

★**함정 — 방문 퀘스트가 대화를 가로챈다** (2026-08-20 발견·수정). `튜토_선원`의 목표가 `visit|선원`인데
`QuestManager.onVisit`이 방문을 자동완료하고 `true`를 돌려주면, `NpcInteractListener`가 **대화를 열기 전에
early return**한다(`dialogue.cleanup` 후 수락창만 띄우는 설계). 즉 `항해` 선택지에 도달할 방법이 없어
**입항 컷씬 전체가 실행되지 않는다.** 2026-08-06 `caee350`(인트로 목표를 선원으로)부터 이 상태였다.
- 수정 ① `onVisit` 맨 앞에 `if ("선원".equals(npcId) && d.getIntroStage() == 1) return false;`
- 수정 ② `completeArrival`이 `introStage`를 2로 올린 뒤 `onVisit(p,"선원")`을 **다시 호출**해 정식 완료
  (보상·경험치·`튜토_길드` 대기 전환을 기존 방문 완료 경로에 그대로 위임 — 완료 로직 복제 금지)
- 수정 ③ `npc.json`의 `선원.quests`를 `[]`로 — 등록돼 있으면 `beginByQuestState`가 「진행중」 분기로 빠지고,
  진행중 노드의 선택지는 **동적(포기/닫기) 고정**이라 거기서도 `항해`에 도달할 수 없다.
  (`quests`는 「이 NPC가 주는 퀘스트」 목록이고 `튜토_선원`은 인트로가 직접 부여하므로 원래 등록이 잘못이었다.)

추가할 것: 선택 직후 뱃고동·파도 사운드 1회 → 암전 진입.

### 2막 — 암전 3.5초
- v1: `BLINDNESS` 포션 (RP 작업 0)
- v2(선택): 완전 검은 화면은 오로라에 쓴 **RP 글리프 커튼** 방식으로 격상 (memory `project_fog_viewdistance_mechanism`)
- 사운드: 파도 → 갈매기 → 종
- 이 뒤에서 조용히 처리: 3막 지점으로 텔포 · 배 스폰 · 승선 · 경로 청크 티켓 · 타 플레이어 가시성 차단

### 3막 — 입항 (핵심)
개방 대양에서 시작해 **방파제 수문을 통과**하고 만에 진입해 부두에 접안한다.

**항로** (전 구간 수심·장애물 실측 검증 완료, 흘수 여유 7블록 이상)

| # | 좌표 | 비고 |
|---|---|---|
| W0 | `379, 61, 1160` | 시작. 방파제 밖 개방 대양(해저 y43~45) |
| W1 | `379, 61, 1136` | **아치 수문 통과** (개구 x369~389 중심선) |
| W2 | `383, 61, 1090` | 만 진입 |
| W3 | `388, 61, 1040` | 좌현에 정박 범선 A(`x345~357, z1040~1085`) |
| W4 | `393, 61, 1000` | 우현에 정박 범선 B(`x411~424, z955~1000`) |
| W5 | `398, 61, 965` | |
| W6 | `400, 61, 941` | **접안**. 선체 x393~407, 선수 z925 / 선미 z957 |

- 총 **220블록 / 15초** (유저 지정 길이). 수문(z1136)→부두(z941) 195블록이 지형으로 고정이라 총 길이는 줄일 수 없다 → **속도로 맞춘다**
- 평균 14.7블록/초. `범선` 자체 최고속이 `speed 10 × 0.12 = 1.2블록/틱 = 24블록/초`이므로 **최고속의 61%** — 과속 아님
- ★**등속 금지, 구간별 속도**로 짜야 빠른 게 의도적으로 느껴진다:

| 구간 | 거리 | 속도 | 설계 | **실측** | 의도 |
|---|---|---|---|---|---|
| A: W0→W1 (접근+아치) | 24 | 8블록/초 | 3.0s | 2.5s | 아치 아래를 **느리게** 지나 위압감 |
| B: W1→W5 (만 순항) | 172 | 17.5블록/초 | 9.8s | 10.6s | 항구를 시원하게 가로지름 |
| C: W5→W6 (접안) | 24 | 17.5→6 선형감속 | 2.2s | 2.2s | ease-out으로 부드럽게 정지 |
| **총** | **220** | 평균 14.7 | 15.0s | **15.3s** | |

★**ease-out은 반드시 거리 선형**으로 할 것. 처음에 거리 2차식(`t*t`)+바닥값 0.06블록/틱으로
짰다가 총 항해가 **20.1초**로 5초 초과했다(2026-08-06 봇 실측). 소요시간은 `∫ds/v(s)`라
바닥값이 작으면 마지막 몇 블록이 1.2블록/초로 기어가며 시간을 다 먹는다 —
2차식+0.06 = 140틱(7.0s) vs 선형+0.30 = 45틱(2.2s). 선형은 소요시간이
`(L/(V−vmin))·ln(V/vmin)` 닫힌 형태라 튜닝이 예측 가능하다.

- 접안 시 좌현(x393)이 부선 난간(x392)에 정확히 붙는다
- 정박선 회피: 중심선 x379→400은 범선 A(서)와 B(동) 사이를 통과. 최소 이격 4블록

**카메라 연출** (`p.setRotation(yaw, pitch)` — 텔포 없이 룩패킷만, `TableSeatService:88` 선례)

yaw 180 = 정북(선수 방향), pitch 음수 = 위

15초 타임라인. 암전이 걷히는 순간 이미 아치가 눈앞에 있어 **첫 프레임부터 시각적 후크**가 걸린다.

| 구간 | yaw | pitch | 의도 |
|---|---|---|---|
| 0~1.2s | 180 | 0 → −15 | 암전이 걷히며 아치가 눈앞에 |
| 1.2~3.0s | 180 | −15 → −35 | **아치 아래를 지나며 올려다본다** (구간 A 저속과 맞물림) |
| 3.0~5.5s | 180 | −35 → −5 | 만 내부가 펼쳐진다 |
| 5.5~9.0s | 180 → 125 | −5 | 좌현으로 팬 — 정박 범선 A(z1040~1085) 통과 시점과 일치 |
| 9.0~11.5s | 125 → 180 | −5 → −10 | 정면 복귀. 언덕 위 스폰마을이 보인다 |
| 11.5~12.8s | 180 | −10 → 0 | 부두 접근 |
| 12.8~15.0s | 180 → 155 | 0 → +15 | 접안하는 부선을 내려다본다 |
| 종료 | — | — | 시야 해제(자유) |

★카메라 페이즈 경계는 **시간이 아니라 항로 진행률(z좌표)에 걸어야** 한다 — 구간별 속도가 다르므로
시간 기준으로 짜면 속도를 조정할 때마다 연출이 어긋난다.

### 4막 — 하선 → 길드 → 할아버지 (2026-08-06 구현·dev+prod 배포 완료)

**엔진 수정 0** — `onVisit`이 역할 dispatch 전에 실행되므로(NpcInteractListener) 접수원 클릭 한 번에
① visit 완료 ② 인사 대사 ③ 퀘스트 GUI에서 튜토00 수락까지 이어진다. 배선은 데이터뿐:
`튜토_길드`의 `다음퀘스트`=튜토00 + 요한의 `quests`에 튜토00 등록.

★`튜토00`에 `선행퀘스트`를 달지 않았다 — `normalizeAbandonedIntro`가 인트로 이탈자에게 튜토00을
직접 수락시키는 폴백 경로가 있어 선행을 걸면 그 경로가 막힌다.

신규 NPC **요한**(`&a[Q] 요한`, npc id `길드접수원`, **CID 162 dev/prod 공통**, `414.5/85/818.5`
= 잉그리드 서쪽 2블록 실내). 잉그리드는 `guild=true` 역할 NPC라 우클릭 시 GUI를 열고 return해서
대사가 안 뜬다 → 기존 튜토 트레일과 같은 「시설 옆 전용 대화 NPC」 패턴.


전 구간 **도보 가능**(실측: 경사로 연속 상승 확인).

1. 부선 갑판 `392, 61, ~920` 하선 → 스타터 키트 지급 + 억제 해제
2. 목재 안벽 `400, 62, 900` (1블록 계단)
3. 석조 경사로 `x396~407` — z903(y61) → z858(y76) 2블록당 1단
4. 마을 앞 `400, 77, 856`
5. **신규 visit 퀘 「길드를 찾아가자」** → 안내좌표 = 길드 `잉그리드`(`416, 85, 818`, CID 17)
6. **신규 「길드 접수원」 대화 NPC**(잉그리드 옆) → "신참이야? 길잡이 할아버지를 찾아가봐"
   → ★잉그리드는 `guild=true` **역할 NPC라 우클릭 시 GUI를 열고 return** → 대사가 안 뜬다.
   기존 튜토 트레일이 쓰는 것과 같은 「시설 옆 전용 대화 NPC」 패턴(대장간안내/시장안내/요리안내)으로 해결
7. → 기존 `튜토00`(할아버지 찾아가기) 자동 수락 → 기존 14퀘 트레일로 합류

---

## 3. 기술 설계

### 3-1. 기존 배 시스템은 재사용하지 않는다 (조사 결과)
- **`ShipMover`는 죽은 코드** — 호출자 0건(`BlockShipPlugin:138` 생성, `:3728` getter뿐). 게다가 인자에 `Player` 필수 + 블록 단위 순간이동
- 실제 이동은 `task/ShipTickTask`인데 **`pilot.getCurrentInput()`에 하드결합** — 파일럿이 없으면 마찰로 감속 후 `startDocking`으로 강제 정지(`:77-85`)
- `ShipFactory.createFromBlueprint`로 만들면 선체 ItemDisplay 1 + 돛 BlockDisplay 84 + 좌석/아바타/셜커 = **85+ 엔티티**. 유저별 가시성 차단 대상이 그만큼 늘고, `Ship`은 전체 엔티티 목록을 노출하지 않는다

**→ 컷씬은 완전 독립 구현**. `ShipManager`/`ShipTickTask`/`ShipOrphanCleaner`와 아예 무관하게 격리한다.

### 3-2. 컷씬 배 = 2엔티티
| 엔티티 | 역할 |
|---|---|
| `ItemDisplay` | 구운 선체 모델. `ship-models.json` 값 그대로: `item_model=barkan:ship/beomseon`, `scale=10.83871`, `pivot_y=9.5` |
| 투명 `ArmorStand` | 갑판 좌석. `addPassenger(player)` → 플레이어가 배와 함께 이동 |

- ★`Ship.camSeat`은 쓰지 않는다 — 상공 25블록에 있고, 주석에 "**라이더 시점이 고정 안 되는 정석 트릭이라 자유 조망에 맞음**"이라 명시돼 있다. 컷씬은 반대로 시점을 잡아야 한다
- 돛(84 BlockDisplay) 없이 선체 모델만으로 외형이 어떤지 **dev에서 스크린샷 육안 검증 후** 필요하면 추가

### 3-3. 유저별 가시성 (유저 요구)
`hideEntity` 선례 다수: `HarpoonManager:868`, `ForageManager:263`, `RewardRevealFx:96`
- 컷씬 시작 시 **다른 모든 온라인 플레이어**에게 선체·좌석 `hideEntity`
- **플레이어 본인도** 타 플레이어에게 `hideEntity` — 안 하면 남들 눈에 사람이 항구 위를 6블록/초로 날아간다
- 컷씬 중 접속하는 플레이어에게도 `PlayerJoinEvent`에서 동일 처리

### 3-4. 반드시 신규로 넣어야 하는 것
| 항목 | 이유 |
|---|---|
| `grantShipGrimExempt(p)` 직접 호출 | 기존 grant는 **"클릭 탑승" 경로에만** 걸려 있다(`PlayerInteractListener:81,99`). 프로그램 승선은 매 틱 텔포 → Grim 오탐으로 러버밴딩/킥 위험 실재 |
| `EntityDismountEvent` 취소 | 지금은 Shift 하차가 **그냥 허용**된다(`ShipEntityListener:51-77`은 허용+정리만) → 컷씬 중 아무 때나 탈출 가능 |
| `addPluginChunkTicket` | 배 코드에 청크 확보가 **전무**. 225블록 항로 ≈ 40~50청크. 항구는 이미 생성돼 있어 GEN 부담은 없다 |

### 3-5. 고장 안전장치 (필수)
`PlayerData.introStage` 저장: `0` 미시작 / `1` 배 위 / `2` 항해 중 / `3` 완료
- 항해 중 접속이 끊기면 → 재접속 시 **컷씬을 재생하지 않고** 곧바로 부두 하선 지점 + 스타터 키트 지급
- 1막에서 끊기면 → 배 위에서 대사 재생
- 컷씬이 깨져도 **게임 진행이 막히지 않아야 한다**
- `onDisable`/`PlayerQuitEvent`에서 배 despawn · 청크 티켓 해제 · 억제 해제 · 가시성 복원

### 3-6. 신규/수정 파일
| 파일 | 내용 |
|---|---|
| `cutscene/BoatArrivalCutscene.java` (신규) | 웨이포인트 lerp 이동 + 카메라 키프레임 + 가시성/청크/Grim/하차봉쇄 + 정리 |
| `npc/TutorialIntroManager.java` | `completeArrival()`의 즉시 워프를 컷씬 시작으로 교체 |
| `playerdata/PlayerData.java` | `introShown`(있음) → `introStage` 확장 |
| `BlockShipPlugin.java` | 컷씬 매니저 생성·와이어링, `/튜토리얼 컷씬` 테스트 서브커맨드 |
| `quests.json`·`dialogue.json`·`npc.json` | 「길드를 찾아가자」 visit 퀘 + 「길드 접수원」 NPC (dev+prod 양쪽, **항목만 추가 — 파일 덮어쓰기 금지**) |

---

## 3-7. 검증 현황 (2026-08-06 dev)

**✅ 서버측 실측 완료** — dev 테스트 봇(`~/dev-test-bot`, HTTP:8765)으로 `/튜토리얼 컷씬` 재생:
- 승선 → 항로 추종(x 379.5→397.5 부드럽게 드리프트, z 1160→941) → 부두 하선 `(390.5, 61, 934.5)` 정상
- 총 항해 **15.30초** (목표 15초)
- 선체 ItemDisplay 정리 확인 — 항로 전체에 y69.5 잔존 엔티티 없음
- 예외/에러 0, 부팅 클린(138 NPC)

★봇 사용에 필요했던 우회: GrimAC가 mineflayer를 TickTimer로 8초 만에 킥한다(위치 무관, 알려진
봇 한계). dev `punishments.yml`의 MovementKick 임계를 150→999999로 임시 상향 후 테스트,
**끝나고 원복 완료**. 다음에 봇으로 탑승 계열을 테스트할 땐 같은 우회가 필요하다.

**❌ 미검증(육안 필요)** — 봇으로는 원리적으로 불가:
- 돛 없는 선체 모델 외형(돛 84 BlockDisplay를 뺐다) — 돛대만 있는 배로 보일 수 있음
- 시각적 최상단 y79 추정이 맞는지 = 아치 통과 시 돛대가 키스톤을 뚫지 않는지
- 카메라 지터/멀미 — 매 틱 `setRotation`이 마우스 입력과 싸운다
- 4,840블록 장거리 TP 시 클라의 "지형 로딩" 검은 화면과 암전이 겹치는 모양

## 4. 남은 확인/리스크
- **1막 배가 어느 지역에도 속하지 않는다** — `바르칸` 지역 AABB는 `x -1367~1340`인데 배는 x2908. 사이드바 지역 표시/날씨/물고기 상속이 어떻게 되는지 확인 필요(30초짜리 도입부라 치명적이진 않음)
- 돛 없는 선체 모델 외형 — dev 스크린샷 육안 검증
- 카메라를 매 틱 `setRotation`하면 마우스 입력과 싸워 미세한 지터가 날 수 있다 → 2틱 간격/보간으로 완화, dev에서 체감 확인
- 항구 서안 상공 `x322~337, z1000~1014, y196~219`에 **유령 테라코타 돔**이 떠 있다(Y좌표 실수 복사로 추정). 항해엔 무관하나 카메라가 위를 향하는 6~9초 구간에서 화면에 들어올 수 있다
- 배포는 유저 요청 시 **다른 작업과 묶어 한 번에** (jar 변경 = 서버 풀 재시작)
