# 인수인계 — 무인운영 배포 파이프라인 (2026-08-13)

> 모바일 세션(정보 제한적)에서 작업한 것을 다음 세션에 넘긴다.
> **이 문서 하나로 상황 파악 + 다음 행동이 되도록** 정리했다.
>
> 읽을 순서: **이 문서 → [CLAUDE.md](CLAUDE.md) → `ops/*/README.md`**

---

## ⛔ 2026-08-14 후속 세션 — 이미 낡은 항목 (읽기 전에 이것부터)

이 문서는 08-13 모바일 세션 시점의 사실이다. 하루 뒤 실측으로 **아래는 무효**다.
나머지(§0 절대규약, §2 목표, §5-A·§6·§7)는 여전히 유효하다.

| 항목 | 문서 서술 | 2026-08-14 실측 |
|---|---|---|
| §3·§4-3·§5-D **버전 드리프트** | prod 1.21.11 vs 빌드 1.21.4 = **7패치 드리프트** | **해소됨.** `paperDevBundle`·`paper-api` 1.21.11 + paperweight 2.0.0-beta.21 + Gradle 9.0. 부산물로 `Material.CHAIN`(1.21.9에서 `iron_chain` 개명) 지뢰 4곳도 제거 |
| §5-B **blockship-plugin push** | "GitHub에 push 해야 함" | 저장소 **이미 있음**(`wsi1212/blockship-plugin`, private). 남은 건 미push 커밋 반영 + `ci/`·`.github/workflows/` 배치 |
| §1 **저장소 상태** | 양쪽 main이 `fab379e`에 정지, 브랜치 9커밋만 미반영 | 원격 정지는 맞지만 **로컬 main이 103커밋 미push**였다(2.15GB). 그쪽이 훨씬 큰 공백이었고 조각 push로 해소 |
| §1-3 **이 브랜치 처리** | (a) 레거시 main 머지 or (b) cherry-pick | **(b)로 처리 완료.** `24bb196`의 CLAUDE.md만 선별 병합(드리프트 서술 폐기), `869976b`의 docs-index는 생성물이라 재생성 |
| §7 **barkan.kro.kr** | "이미 해석된다" (CLAUDE.md의 '예정'은 낡음) | 맞음 — `dig` 실측 `168.107.8.107`. CLAUDE.md 수정 완료 |

### §6 미검증 항목 — 4건 해소, 2건 남음 (2026-08-14 prod 실측)

- ✅ **월드 크기**: `world` 2.2G + guild 88M + island 78M + end 34M + nether 23M = **약 2.5GB**. 여유 23GB 안에 2층 dev 가 넉넉히 들어간다.
- ✅ **staging jar 파일명 패턴**: `nightly-restart.sh` 는 `"$STAGING"/*.jar` 로 전부 훑고, Actions 가 내보내는 자산명은 `BlockShip-1.0.0-SNAPSHOT.jar`(현행 배포 jar 와 동일) → `ASSET_GLOB=BlockShip-*.jar` 와 일치. **불일치 없음, 조정 불필요.**
- ✅ **`MCDEV_WHITELIST`**: 기본값 `wsi1212` = 본인 계정(git user, prod 접속 계정과 동일) → 그대로 두면 된다.
- ✅ **🐛 `fetch-staging.sh` 의 `REPO` 기본값이 틀렸다** — `mlid-P/blockship-plugin` 은 존재하지 않는다. 실제는 **`wsi1212/blockship-plugin`**(private). 그대로 cron 에 걸면 Release 조회가 매번 실패해 **배포가 영원히 안 온다.** 수정 완료.
- ⬜ 남음: `mcdev-sync.sh` 의 rsync 구간 · `fetch-staging.sh` 의 GitHub API 구간 — 둘 다 오라클 첫 실행을 `--dry-run` 으로.

---

## 0. 절대 규약 — 이것만은 어기지 말 것

1. **🚨 prod 인스턴스를 `terminate` 하지 마라.** 오라클이 춘천에서 A1 생성을
   막았고(공식 문서), Always Free 한도가 2 OCPU/12GB 로 줄어 현재 4/24 는 한도의
   2배다. 한도 초과 shape 는 재생성이 안 된다. **날리면 영구히 잃는다.**
   `stop`(정지)은 되돌릴 수 있다. `terminate`만 금지.
2. **`plugman reload` 금지.** 클래스로더가 손상돼 증상이 더 헷갈려진다.
   jar 교체는 `systemctl restart mcserver`.
3. **jar 배포는 접속자 0명일 때만.**
4. **prod JSON 을 고치면 dev 로 역동기화.** 안 하면 다음 jar 배포에 조용히 되돌아간다.
5. **`pgrep -f` / `pkill -f` 로 프로세스를 판정·종료하지 마라.** 그 문자열을 명령줄에
   언급한 무관한 프로세스(로그 tail 하는 셸, ssh 명령)까지 잡는다. 실측으로 물렸고
   `ops/mcdev/` 는 PID 파일 + `/proc/<pid>/comm` 검증으로 교체했다.

---

## 1. ⚠️ 저장소가 두 개다 — 먼저 이것부터 정리

| | |
|---|---|
| **새 저장소(허브)** | `wsi1212/barkan-fishing-server` |
| **구 저장소(레거시)** | `mlid-P/fish` ← **이 세션이 작업한 곳** |

`.github/workflows/cross-repo-main-sync.yml` 이 5분마다 양쪽 `main` 을 병합·동기화한다.
**단 `main` 만 동기화한다 — 브랜치는 안 넘어간다.**

### 그래서 지금 상태

- 이 세션의 결과물 **9 커밋**이 `mlid-P/fish` 의 브랜치
  **`claude/gsd-skill-review-3hn548`** 에만 있다.
- 가만두면 새 저장소에 **영원히 안 간다.**
- `main` 은 양쪽 다 `fab379e`(2026-08-08) 에서 안 움직였다 → 다른 세션도
  브랜치에서 작업 중이고 아직 main 에 안 넣었다는 뜻이다.

### ★다음 세션이 할 첫 번째 일

1. **새 저장소(`wsi1212/barkan-fishing-server`)로 세션을 열 것.** 레거시로 열면
   계속 엉뚱한 곳에 쌓인다.
2. 두 저장소의 `CLAUDE.md` 를 **먼저 대조**할 것. 이 세션이 CLAUDE.md 를 크게 고쳤다
   (§3 참조). 다른 세션도 고쳤다면 머지 시 충돌한다. 그리고 sync 워크플로는
   *"충돌은 절대 강제 해결하지 않는다 — Git 이 안전하게 병합할 수 없으면 실패한다"* 라
   조용히 멈춘다.
3. 이 브랜치의 9 커밋을 어디로 보낼지 결정 — 아래 둘 중 하나
   - (a) 레거시 `main` 에 머지 → 5분 cron 이 새 저장소로 옮겨준다 (그들 설계 그대로)
   - (b) 새 저장소에 직접 push (cherry-pick)

---

## 2. 이 프로젝트가 지금 향하는 목표

**군 복무로 사람이 장기간 없어도 서버가 살아 있고, 폰만으로 개발이 되게 만드는 것.**

기존에 이미 갖춰진 것: 프리즈 워치독 · 디스크 가드 · 하트비트 · 오프사이트 백업 ·
데일리 재시작 · 크래시 감지. **없던 것**은 "배포가 안전한가"와 "폰에서 개발이 되는가"였다.

이번 세션이 그 두 개를 3층 구조로 채웠다.

```
1층  CI 부팅 스모크      깨진 jar 를 사람 없이 걸러낸다           ops/ci/
2층  오라클 dev 인스턴스  검증 환경 (맥 dev 대체)                ops/mcdev/
3층  당겨오는 배포        Release → staging → 06:00 자동 적용     ops/oracle/
```

전제: **맥은 곧 못 쓴다.** 그래서 모든 것이 "폰 + 클라우드 세션 + 오라클" 로 돌아가야 한다.
폰에서 자바 마크 클라 접속이 되므로 **인게임 검증도 폰에서 가능하다**(픽셀 단위 GUI
작업만 맥이 편하다).

---

## 3. 이 세션이 한 것 — 전부 실측 검증됨

브랜치 `claude/gsd-skill-review-3hn548`, 커밋 9개.

### 1층 — `ops/ci/`

| 파일 | 용도 |
|---|---|
| `paper-smoke-test.sh` | Paper 를 실제로 띄워 jar 를 검증 |
| `blockship-smoke.yml` | GitHub Actions (빌드→스모크→승격 시에만 Release) |
| `README.md` | 설계·검증 결과 |

**검증**: Paper 1.21.11 build 132 부팅 31초 / 정상 jar → 종료코드 0 /
**Helper 클래스를 뺀 jar(CNFE 재현) → 종료코드 1** = 2026-08-03 사고 유형을 잡는다.

**돌려보니 나온 버그 3개를 고쳤다**: 상대경로 유실, fifo 홀더가 stdout 을 붙잡아
호출측 파이프가 안 닫힘, Paper 가 `onEnable` **전에** "Enabling" 을 찍어 enable 실패를
통과로 오판.

### 2층 — `ops/mcdev/`

`mcdev-sync.sh`(prod→dev 반영) · `mcdev-up.sh`(켜기+자동종료) · `mcdev-down.sh` ·
`mcdev-reaper.sh`(cron 안전망) · `mcdev-lib.sh`

**검증**: 자동 종료 / 수동 종료(7초, 강제종료 0회) / 리퍼(타이머를 kill -9 하고 시한을
과거로 돌린 상태에서 발동) 전부 정상 저장 후 종료·정리·멱등.

**돌려보니 나온 버그 4개를 고쳤다** — 그중 하나는 위험했다: `pkill -f` 가 무관한
프로세스를 죽일 수 있었다(§0-5).

**prod 보호**: MemAvailable<3500MB 면 시작 거부 · 디스크 80/88% 거부 ·
`~/mcserver/` 밖에 배치(jar-guard·백업 glob 회피) · 포트 분리 · **systemd 유닛 없음** ·
`online-mode=true` 유지(offline 이면 UUID 가 달라져 playerdata 가 안 붙고 검증이 거짓말이 됨).

### 3층 — `ops/oracle/`

`fetch-staging.sh`(Release→staging, cron */15) · `MIGRATION.md`(🚨전제 붕괴 표시됨) ·
`HEAP-DOWNSIZE.md` · `launch-retry.sh` · `README.md`

**검증**: jar 무결성 검사를 6개 케이스로 확인(정상 / paper-plugin.yml / 하위폴더 위조 /
yml 없음 / 손상 zip / 빈 파일). **버그**: `unzip -l` 표 형식 때문에 경계 매칭이 어긋나
**정상 jar 를 전부 거부**하고 있었다 → `unzip -Z1` 로 교체.

### 문서 갱신 (CLAUDE.md)

- prod = **Paper 1.21.11** (1.21.10 아님). 빌드 타겟 1.21.4 와 **7패치 드리프트**
- `plugin.yml` 실측: **hard `depend` 없음**, softdepend 5개.
  softdepend 를 가드 없이 참조하면 CNFE 로 죽는다는 규약 추가
- 🚨 인스턴스 재발급 불가 경고 + terminate 금지
- 디스크 48GB/여유 23GB

---

## 4. 현재 상황 체크법 — 복붙용

> zsh 에서 `#` 주석이 명령으로 읽히니 **`bash <<'EOF'` 로 감쌀 것.**

### 4-1. prod 헬스 (제일 자주 쓸 것)

```bash
bash <<'EOF'
KEY=~/.ssh/oracle-mc.key; PROD=ubuntu@168.107.8.107
ssh -i "$KEY" "$PROD" 'bash -s' <<'REMOTE'
echo "── 서버 상태"; systemctl is-active mcserver; ~/mcserver/scripts/rcon.py list
echo "── 치명 예외 (0이어야 정상)"
grep -cE 'NoClassDefFoundError|Error occurred while enabling|NoSuchMethodError' ~/mcserver/logs/latest.log
echo "── 자원"; free -m | head -2; df -h / | tail -1
echo "── staging 대기중인 jar"; ls -l ~/mcserver/staging/ 2>/dev/null
echo "── 최근 배포 백업"; ls -lt ~/mcserver/backups/deployed-jars/ 2>/dev/null | head -3
echo "── cron"; crontab -l | grep -vE '^#' | head -20
REMOTE
EOF
```

### 4-2. 저장소 상태 (repo 이원화 확인)

```bash
git fetch --all --prune
git log origin/main -1 --format='%h %ci %s'
git ls-remote --heads origin
git rev-list --left-right --count origin/main...HEAD
```

### 4-3. 버전 드리프트 확인

```bash
ssh -i ~/.ssh/oracle-mc.key ubuntu@168.107.8.107 \
  'grep -o "1\.21\.[0-9]*" ~/mcserver/version_history.json | tail -1'
grep -n 'paperDevBundle' <blockship>/build.gradle.kts
```
구동과 빌드 타겟이 벌어질수록 `NoSuchMethodError` 위험이 커진다. 현재 **1.21.4 vs 1.21.11**.

### 4-4. 오라클 한도 위험

```bash
export OCI_CLI_CONFIG_FILE=~/.oci-family/config
oci compute instance list --compartment-id <테넌시OCID> \
  --query 'data[].{name:"display-name",ocpu:"shape-config".ocpus,mem:"shape-config"."memory-in-gbs",state:"lifecycle-state"}'
```
그리고 **콘솔 → Announcements** 를 눈으로 볼 것. 한도 통지가 거기 뜬다.

### 4-5. 복구 (깨진 jar 가 적용됐을 때)

`ops/oracle/README.md` 와 아래 순서:
```bash
ls -lt ~/mcserver/backups/deployed-jars/ | head
cp ~/mcserver/plugins/BlockShip-1.0.0-SNAPSHOT.jar /tmp/broken-$(date +%s).jar
cp ~/mcserver/backups/deployed-jars/<직전> ~/mcserver/plugins/BlockShip-1.0.0-SNAPSHOT.jar
rm -f ~/mcserver/staging/BlockShip-*.jar          # ★안 하면 다음날 06:00 에 또 적용
sudo systemctl restart mcserver
```

---

## 5. 내가 해야 할 일 — 우선순위 순

### A. 지금 당장 (위험 차단)

- [ ] **`oracle-mc.key` → Termius 등록** — 폰에서 prod 에 붙는 유일한 복구 해치.
      이거 없으면 자동화가 잘못돼도 손을 못 쓴다. `cat ~/.ssh/oracle-mc.key`
- [ ] **오라클 콘솔 Announcements 확인** — 한도 통지 왔는지 (권한 있으니 직접 가능)
- [ ] **예산 알림 $1 설정** — 미터가 돌기 시작하면 즉시 감지
- [ ] **OCI Events → Discord 웹훅** — 인스턴스 정지/종료 알림이 친구 메일이 아니라
      나에게 오게. 이건 IAM 권한으로 안 메워지는 구멍이다

### B. 폰 독립 (이거 되면 맥 없이 개발 가능)

- [ ] **`blockship-plugin` 을 GitHub 에 push** (프라이빗)
      - `ops/blockship/gitignore-template` → `.gitignore`
      - `ops/ci/paper-smoke-test.sh` → `ci/`
      - `ops/ci/blockship-smoke.yml` → `.github/workflows/`
      - ★push 전 `git status` 를 눈으로 확인 (jar·키·토큰 섞였는지)
      - push 하면 **첫 Actions 실행에서 7패치 드리프트가 실제로 터지는지 바로 안다**
- [ ] **`plugins/BlockShip/*.json` 도 git 에** (playerdata 제외) — 퀘스트·어종·NPC
      데이터를 폰에서 작업하려면 필요
- [ ] **스크립트를 오라클에 배치** — `mcdev-*.sh`, `fetch-staging.sh` →
      `~/mcserver/scripts/`, 첫 실행은 전부 `--dry-run`
- [ ] cron 등록: 리퍼 `*/5`, fetch-staging `*/15`
- [ ] iptables + OCI Security List 에 **25566** (dev 접속용)

### C. 저장소 정리

- [ ] 새 저장소로 세션 열기 (§1)
- [ ] 두 CLAUDE.md 대조 → 충돌 없으면 이 브랜치를 main 에 머지

### D. 여유 될 때

- [ ] 새 이메일로 오라클 재가입 (기존 이메일은 탈퇴 이력으로 막힘) → 오사카 → 2/12
      → dev 박스용. **실패해도 계정 삭제 금지**(그 삭제가 지금 막힌 원인)
- [ ] `build.gradle.kts` 의 `paperDevBundle` 을 1.21.11 로 올릴지 검토

---

## 6. 아직 검증 못 한 것 (다음 세션이 확인할 것)

- [ ] **`mcdev-sync.sh` 의 rsync 구간** — 컨테이너에 rsync 가 없어 미검증.
      오라클 첫 실행은 반드시 `--dry-run`
- [ ] **`fetch-staging.sh` 의 GitHub API 구간** — 실제 private repo·토큰이 없어 미검증.
      첫 실행 `--dry-run`
- [ ] **월드 크기** — 디스크 여유 23GB 안에 2층 dev 가 들어가는지
      `du -sh ~/mcserver/world*`
- [ ] **`nightly-restart.sh` 의 staging jar 파일명 패턴**이 `BlockShip-*.jar` 와 맞는지
      → 다르면 `fetch-staging.sh` 의 `ASSET_GLOB` 조정
- [ ] **`MCDEV_WHITELIST`** 가 본인 마크 계정인지 (기본값 `wsi1212`)
- [ ] **7G 힙 실플레이** — GC 압박은 미리 볼 수 있지만 12GB 조건은 재현 불가

---

## 7. 배경 사실 (조사해서 확정한 것 — 다시 조사하지 말 것)

- **Always Free A1 = 2 OCPU / 12 GB** (1,500 OCPU-h / 9,000 GB-h). 4/24 에서 반토막.
- **춘천은 A1 생성 불가** — 오라클 공식 문서에 명시적 예외로 적혀 있다.
  그래서 무료 가입 드롭다운에 한국이 없다. 새 계정을 만들어도 해결 안 된다.
- 시행 시점은 **불명확하다**. 공식 이메일 공지가 없었고 콘솔과 문서가 어긋난다는 보도.
  "8월 18일" 이 검색 요약에 있었으나 **1차 출처 확인 실패 — 사실로 취급하지 말 것.**
- prod 실측: 힙 16G, `used 18523MB` → **힙 밖 2,139MB**(플러그인 20개).
  12GB 로 강제되면 **7G 힙** 권장, Aikar 플래그 5개도 소용량 변형으로 되돌려야 한다.
- **Paper API v2 는 sunset.** v3(`fill.papermc.io`)를 쓸 것.
- prod 플러그인 20개: AIBuilder · Axiom · BarkanChess · BarkanWorldWarp · BetterHud ·
  BetterModel · BlockShip · Chunky · Citizens · CoreProtect · CraftEngine · Geyser ·
  floodgate · GrimAC · ProtocolLib · Via(Version/Backwards) · VotifierPlus ·
  multiverse-core · worldedit
- `barkan.kro.kr` → `168.107.8.107` **이미 해석된다**(CLAUDE.md 의 "예정" 은 낡음).
  ★무료 도메인이라 **갱신 주기 확인 필요** — 놓치면 서버는 멀쩡한데 이름이 안 풀린다.

---

## 8. 다음 세션 첫 턴에 시킬 말 (복붙용)

```
새 저장소(wsi1212/barkan-fishing-server)에서 작업한다.
HANDOFF-2026-08-13.md 를 먼저 읽고, §4-1 prod 헬스체크와 §4-2 저장소 상태를
확인한 뒤 현재 상황을 요약해줘. 그다음 §5-A 부터 순서대로 진행하자.
레거시 저장소(mlid-P/fish)의 claude/gsd-skill-review-3hn548 브랜치에 커밋 9개가
있으니 CLAUDE.md 충돌 여부부터 대조해줘.
```
