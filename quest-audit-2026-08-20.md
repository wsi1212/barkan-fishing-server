# 퀘스트 심층 감사 — 2026-08-20

## 감사 범위

권위 데이터인 `plugins/BlockShip/quests.json`, `dialogue.json`, `npc.json`, `regions.json`과 `QuestManager.java`의 목표 처리 코드를 대조했다. 총 334개 퀘스트(메인 120, 사이드 125, 튜토 30, 일일 20, 주간 36, 히든 3), NPC 165명, 대사 세트 127개, 대사 노드 1,166개, 지역 35개를 확인했다. 초기 대사 감사에서 잡힌 기계식 문장 461건은 수정 후 0건이다.

## 반영한 수정

- 오스발트의 `“물고기는 잡으면 없어져. 밭은 안 그래.”`를 `“물고기는 건져 올린 만큼만 남지만, 밭은 자리를 내어주면 계절마다 다시 돌려준다네.”`로 교체했다. 퀘스트 설명과 NPC 첫 만남 대사를 함께 맞췄다.
- `본섬08`의 설명을 실제 목표인 `강·강_상류·항구 도감 18종 + 판매 12회`에 맞췄다. 기존의 “물고기 50마리”는 목표와 전혀 달랐다.
- `본섬11`·`영주05`의 사막 방향을 동쪽에서 서쪽으로 고쳤다. 실측 좌표상 사막은 스폰도시의 서쪽이다.
- 반복된 `이번에 맡길 일은`, `해야 할 일은`, `이 정도면 충분해` 류의 자동생성 대사를 목표·NPC 말투에 맞는 문장으로 교체했다.
- 물건을 `1마리`라고 세던 대사, `D~C등급 이상`, `레벨 12을`, `당신` 번역투, `서버에서 가장 긴 일` 같은 문서/AI 냄새를 정리했다.
- 신선도 하한이 실제 목표에 없는 `알비스01`, `심해10`, `상사이드_레일라04`, `왕도11b`는 설명에서 신선도 주장을 제거했다. 반대로 `본섬05`는 실제 목표의 신선도 80을 설명에 명시했다.
- 범위·크기 조건을 `40cm 이상인 B등급 물고기`처럼 읽히는 문장으로 통일했다.

## 2차 대사 보강

- 새 퀘스트나 새 NPC를 만들지 않고, 실제 대화 엔진이 표시하는 짧은 기존 노드 40곳에 후속 문장 1개씩을 추가했다.
- 퀘스트를 모두 끝낸 뒤 `...`로 끝나던 `모르`, `비늘짜는이`, `일곱셋`, `가라앉은 하`, `오스발트`에 `후일담` 노드를 추가했다. 새 의뢰를 만들지 않고 각 인물의 마지막 상태만 마무리한다.
- 왕도 거리의 위병·시민은 봉인서고와 종탑 소문을, 상단마을·항구 주민은 장부·물때·그물·화물·선원 생활을, 사막 주민과 배의 선원은 해당 지역의 생활감을 말하도록 보강했다.
- `일곱셋`과 `가라앉은 하`의 진행 대사도 각 인물의 끊긴 말투를 유지한 채 한 줄씩 보탰다. `울지 않는 것`은 말하지 못하는 존재라는 설정을 보존해 건드리지 않았다.
- 감사의 `대사가 아예 없는 NPC` 39명은 모두 페리·상점·길드·카지노·대장간·요리·말 대여·랭킹 등 기능형 NPC다. `NpcInteractListener`가 대화 엔진보다 먼저 해당 GUI를 열기 때문에, 표시되지 않을 대사를 만들지 않고 의도된 무대사로 남겼다.

## 지도와 실제 지역 대조 결과

정상으로 확인한 지역은 `늪지대`와 `대수림`이다. 둘 다 `pos1/pos2`가 0이어도 실제 폴리곤이 있으므로 스텁으로 판정하면 안 된다. `강`, `강_상류`(표시명 `협곡`), `항구`, `폭포_뒤_동굴_1층`도 실제 경계/점 데이터를 가진다.

다음은 데이터와 맵이 아직 맞지 않는 항목이다.

| 우선순위 | 항목 | 상태 |
|---|---|---|
| P0 | `심해20` → `area\|심해협곡` | `regions.json`에 지역 ID가 없음. 퀘스트 설명은 실제 지역처럼 안내하지만 엔진이 완료시킬 수 없다. |
| P0 | `심해26` → `craft\|심해어가면\|1` | 현재 레시피/재료 데이터에 제작 결과가 없다. 후속 `심해27`의 `submitmat`도 같은 아이템을 요구한다. |
| P0 | `심해27` → `area\|심해교단본부` | `regions.json`에 지역 ID가 없음. |
| P0 | `심해28` → `area\|무명의성소` | `regions.json`에 지역 ID가 없음. |
| P1 | `사사이드_사피르05` → `오아시스, 붉은사막, 붉은호수` | `붉은호수`는 좌표·폴리곤이 0인 지역 데이터다. 실제 장소를 만들지 않고 목표만 바꾸지는 않았다. |
| P1 | `영주성` | 건물·NPC는 존재하지만 별도 지역 ID가 없다. 현재 영주 퀘스트는 NPC 방문으로 처리되어 즉시 블록되지는 않는다. |
| 참고 | `사막_채집01`, `본섬_채집01` | 메인 카테고리지만 `다음퀘스트` 체인에는 연결되지 않은 선행 퀘스트다. NPC 목록에서 수동 수락은 가능하다. 체인에 넣을지는 서사 분기 결정이 필요하다. |

심해 지역 세 곳은 좌표를 날조해 고치지 않았다. 실제 건축/지역 경계가 확정되면 `regions.json`과 퀘스트 목표를 한 번에 연결해야 한다. `심해전왕의핵`은 `ImugiBattle`의 레비아 보상 코드에서 직접 지급되는 경로를 확인했으므로 미구현 항목으로 잘못 분류하지 않았다.

## 검증 결과

- JSON 파싱: 통과
- `ops/audit-dialogue.py --full`: ERROR 0
- 퀘스트 선행/다음 참조: 끊어진 참조 0
- 메인/튜토 NPC 배정: 튜토 강제 지급 2개를 제외하고 배정 확인
- 자동생성 대사 패턴: 461 → 0
- 짧은 표시형 대화 보강: 40개 노드에 40문장 추가
- 빈 후일담 보강: 5개 노드 추가
- 기능형 무대사 NPC: 39명 유지(실행 경로상 대화 미사용)
- 라이브 파일 백업: `dialogue.json.bak-quest-audit`, `quests.json.bak-quest-audit` 생성

이번 작업에서는 지역 좌표를 만들거나 서버를 재시작하지 않았다. 따라서 P0 네 건은 텍스트 문제가 아니라 실제 맵/제작 데이터 작업으로 남아 있다.
