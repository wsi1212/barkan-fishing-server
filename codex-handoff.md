# Codex 인수인계 — 퀘스트 전면 개편 (2026-08-07)

> **✅ 작업 완료 (2026-08-07).** Codex가 §3의 ①~⑧을 수행하고 prod에 반영했다.
> 최종 상태: **퀘스트 257 · NPC 157 · 지역 32 · 어종 565 · 메인 미배정 0 · 검증 5종 전부 통과**.
> 이 문서는 이제 **작업 지시서가 아니라 그 작업의 기록**이다. 남은 항목은 맨 아래 「잔여」 참조.

이 문서 하나로 작업이 가능하도록 정리했다. **설계 판단은 이미 끝났으니 추가 설계 없이 스펙대로 구현할 것.**

읽을 순서: **이 문서 → [story.md](story.md)(서사 권위) → [quest-rework.md](quest-rework.md)(목표 설계) → [AGENTS.md](AGENTS.md)(운영·배포)**

⚠️ [quest-rework.md](quest-rework.md)의 난이도 곡선·시간 수치는 **낡았다**(경험치 개편 전 계산).
곡선·경험치 관련 최신 확정치는 이 문서와
[감사 리포트](.claude/skills/balance-audit/audits/2026-08-07-level-pacing.md)를 따를 것.

---

## 0. 절대 규약

1. **작업 대상은 prod다** (`ubuntu@168.107.8.107:~/mcserver/plugins/BlockShip/`).
   dev는 Citizens NPC가 12명 적어(식당 주인 없음) 튜토 검증이 불가하다.
   ```bash
   scp -i ~/.ssh/oracle-mc.key ubuntu@168.107.8.107:~/mcserver/plugins/BlockShip/quests.json .
   # 편집
   ssh -i ~/.ssh/oracle-mc.key ubuntu@168.107.8.107 'cd ~/mcserver/plugins/BlockShip && cp quests.json quests.json.bak-<작업명>-$(date +%s)'
   scp -i ~/.ssh/oracle-mc.key quests.json ubuntu@168.107.8.107:~/mcserver/plugins/BlockShip/
   ```
2. **prod에서 JSON을 고치면 반드시 dev로 역동기화.** `deploy-blockship.sh`가 JSON 6종을 dev→prod로
   덮으므로, dev가 낡으면 다음 jar 배포에 수정이 조용히 되돌아간다. 작업 후 md5 일치 확인.
3. **리로드 범위**
   | 명령 | 적용 대상 |
   |---|---|
   | `/npc동기화` | `npc.json` + `dialogue.json` |
   | `/데이터리로드` | `quests.json` · fish · parts · enhance · recipes · materials · config |
   ```bash
   ssh -i ~/.ssh/oracle-mc.key ubuntu@168.107.8.107 '~/mcserver/scripts/rcon.py 데이터리로드'
   ```
   ★`npc.json`을 올린 **직후** `/npc동기화`를 돌릴 것 — `NpcManager.save()`가 메모리를 파일로
   덮어써서, 리로드 전에 save가 돌면 방금 올린 파일이 날아간다.
4. **퀘스트를 삭제하지 말 것.** 플레이어 `questStatus`에 잔재가 남는다. 이름·목표·수여자·레벨만 바꾼다.
5. **메인 퀘스트는 반드시 NPC `quests` 배열에 배정.** 미배정 = 영구 수락 불가 (§4 참조).
6. `&l`·`§l` **볼드 전면 금지** (전역 훅이 차단). 색코드는 `&`.
7. **NPC 이름표 색** — 퀘스트 주는 NPC `&a`+`[Q]` · 기능형 `&b` · 대화만 `&f`.
8. jar 배포는 `~/deploy-blockship.sh` (빌드→scp→prod 재시작). **접속자 0명일 때만.**
   `plugman reload` 절대 금지.

---

## 1. 이미 완료된 것 — 다시 하지 말 것

### 플러그인 (커밋 `534fe2b`, `f1e0173` · prod 배포 완료)

- **비낚시 활동 훅 5종 신설**: `harpoon`(작살 전용) · `mine`(드릴 채굴) · `harvest`(작물 수확) ·
  `sail`(항해 거리) · `submit`(제출소). 문법은 §5 팔레트 참조.
- **`RewardMath.totalExp` 합연산 버그 수정** — 장비 보너스를 곱한 뒤 그 결과에 환경 보너스를
  다시 곱하고 있었다. 이제 둘을 더한 뒤 한 번만 적용.
- **`RewardMath.baseExp` 개편**: E5→2 D6→3 C8→4 B10→6 A13→12 S17→30 M30→200 L40→800 G55→3000
- **`FishingLevelManager.NEED_TABLE`**: Lv20→50 ramp ×8 (Lv1~19 무변경, Lv50 need 3,679→29,432)

### JSON (prod 반영 + dev 역동기화 완료)

- **`quests.json`**
  - 2챕터 NPC 배정: `본섬01·02·08·10b`→하겐 / `본섬03·04`→어부 노인 / `본섬05·06·07·09·10`→세르간
  - 설명 정정 3건(`본섬03` D등급 명시 · `본섬05` "세르간의 오두막" 삭제 · `본섬10` "은둔처에서 기다린다"→"찾아가라")
  - 전 11개에 `&8의뢰: <NPC>` 줄 추가
  - **보상경험치**: 1회성(메인·튜토·사이드·히든) = 원본 ×1.75 → **Lv50 요구량의 47%** /
    일일 ×3 / **주간 전부 0**
  - 일일 레벨제한 상향: 쉬움 Lv1 · 보통 Lv10 · 어려움 Lv22 · 전문 Lv35
  - 일일 목표 난이도 상향 (물고기 3→5, C등급 2→3, B등급 1→2, 월척 75→85cm, 대물 120→140cm 등)
- **`dialogue.json`**: 2챕터 `진행중/<qid>`·`퀘스트완료/<qid>` 19노드 + 할아버지 `퀘스트완료/튜토11`에
  길드장 하겐 안내 1줄
- **`env-bonuses.json`**: 환경 경험치 ×0.5 (눈보라 30→15 · 낮 20→10 · 안개 15→8 · 열대야 10→5 · 밤 10→5)

### 문서 (커밋 `c143d44`, `04d27df`)

[story.md](story.md) 전면 재작성 · [quest-rework.md](quest-rework.md) 신설 ·
[감사 리포트](.claude/skills/balance-audit/audits/2026-08-07-level-pacing.md) · `pull.py` 레벨 곡선 파서 수정

---

## 2. 확정된 설계 (변경 금지)

### 챕터 구조 — 베타 = 1~6챕터

| 챕터 | 무대 | 요지 |
|---|---|---|
| 1 | 스폰마을 | 튜토 28단 |
| 2 | 스폰마을 (길드 → **영주성**) | 어부 자격 2단계, 첫 조각 |
| 3 | 사막·붉은사막 | 영주의 명. 교단 인장, 두 번째 조각 |
| 4 | 왕도 | 사막 증거가 중앙을 움직인다 → 소환·알현·첫 왕명 |
| 5 | 상단마을·은빛갈매기호 | 상단↔교단 결탁 → 안개 속 검은 섬 목격 |
| 6 | 왕도 봉인서고 | 궁정의 그림자. 지워진 세 이름 |
| **7** ★신설 | **심해** (Lv50~70) | 심해 교단 **본체**와의 결전. 무기 축이 낚싯대→**작살**로 전환. 최종막 = 심해전왕 |

7챕터는 **이번 작업 범위가 아니다**(무대 미구현). 설계만 [story.md](story.md) 「7챕터」에 확정돼 있다.
단 ①의 필요레벨 재분배 시 6챕터가 Lv40~50에서 끝나고 **7챕터가 Lv50~70을 이어받는다**는 것만 전제할 것.

**왕도는 사막 뒤에 온다.** (기존 라이브는 사막 앞) 상세: [story.md](story.md) 「챕터 구조」·「체인 배선」

### 권위 계단 — 높은 사람은 그 라인 마지막에

| 무대 | 최고 권위 | 첫 대면 | 앞을 채우는 사람 |
|---|---|---|---|
| 스폰 길드 | 길드장 하겐 | `본섬10b` | **요한(접수원)** → 어부 노인 → 세르간 |
| 영주성 | 영주 발데마르 | `영주05` | 근위병 라이너 → 사관 게르하르트 |
| 왕도 | 국왕 | `왕도05` | 성문 위병 로타르 → 대사서 → 위병 쿠르트 |
| 사막 길드 | 지부장 유세프 | `사막12b` | 유누스·파티마 → 사피르·카림·할릴 |
| 사막 마을 | 촌장 나디아 | `사막13~14` | 사막 노파 → 레일라 |
| 상단 | 선장 이자벨라 | `배상단07` | 마테오 → 엔리코 → 피노 → 조반니 |

### 목표 다양성 — 하드 룰

> **"N등급 N마리"만 반복하지 않는다.**

1. 한 챕터에서 같은 verb **3연속 금지**
2. `fish` 단순형(어종 `아무` + 최소크기 0 + 단일 목표)은 **챕터당 최대 2회**
3. 챕터마다 **그 챕터에서 처음 등장하는 축**을 최소 1개
4. 졸업 게이트는 항상 **복합(AND) 3중 이상**
5. 서사 비트에 맞는 verb — 폐사당 조사 = `area`+`dogam` · 수비대 식량 = `fish_fresh` · 길드 등록 = `equip` · 광맥 조사 = `mine` · 항해 = `sail`

---

## 3. 남은 작업 (이 순서대로)

### ① 퀘스트 필요레벨 재분배 ★최우선

곡선이 ×8로 바뀌어 기존 필요레벨이 전부 어긋났다. 목표 배분:

| 챕터 | 퀘 | 현재 필요레벨 | 목표 |
|---|---|---|---|
| 1 튜토 | 28 | 1 | 1~5 |
| 2 스폰마을 (길드+영주성) | 16 | 1~7 | **1~15** |
| 3 사막·붉은사막 | 23 | 12~20 | **15~28** |
| 4 왕도 입성 | 5 | 12~16 | **28~32** |
| 5 상단·기함 | 13 | 20~25 | **32~40** |
| 6 봉인서고 | 10 | 26~40 | **40~50** |

원칙: **마을 안에서는 1~2퀘마다 1레벨, 챕터가 바뀔 때 +2~4 점프**(그 점프가 사이드·일퀘 유도 게이트).
1회성 퀘스트가 요구량의 47%만 담당하므로 후반은 일일 퀘스트로 메꿔야 한다 — 그게 설계 의도다.

### ② NPC 미배정 메인 40개 배정

현재 메인 63개 중 40개가 미배정 = **수락 불가**. 권위 계단(§2)에 맞춰 배정.

| 구간 | 미배정 |
|---|---|
| 사막마을 | 18 |
| 왕도 | 12 |
| 상단마을 | 6 |
| 스폰도시 | 2 (`본섬11` · `본섬_채집01`) |

같이 처리할 것:
- 하겐 사이드 3개 선행 `본섬01` → **`본섬10b`** (안 하면 첫 퀘만 깨고 길드장과 대화 가능 = 권위 계단 붕괴)
- 하겐을 길드 회관 안쪽으로 이동 + 이름표 `&a[Q] 길드장 하겐`
- 위병1·2 이름표 `&f` → `&a[Q]`
- `사막02` 이름이 "촌장 나디아"인데 나디아가 뒤로 가므로 개명
- `왕도04 근위대장의 시험`이 국왕에게 배정돼 있다(근위대장 NPC 없음) → 위병으로 이동

### ③ 체인 재배선 + 왕도 명분·레벨

| 대상 | 현재 | 목표 |
|---|---|---|
| `본섬10b.다음퀘스트` | `왕도01` | `영주01` |
| `영주05.다음퀘스트` | — | `본섬11` |
| `사막14.다음퀘스트` | `본상단01` | `왕도01` |
| `왕도05.다음퀘스트` | `본섬11` | `본상단01` |

`선행퀘스트`도 같은 4곳을 역방향으로 정합시킬 것.

명분 수정 2건:
- `왕도01` — "왕실 문장이 찍힌 소환장이 도착했습니다"(발신 계기 없음) → **사막에서 찾은 교단 인장 + 영주 발데마르의 보고가 왕도에 닿아 소환**
- `왕도05` 첫 왕명 — "동쪽 사막과 상단"(사막은 이미 끝남) → **상단 조사만**

이름 변경 3건: `왕도02` 국왕 알현 → **성문의 검문**(visit 위병) / `왕도04` → 근위대의 시험(위병 수여) /
`왕도05` → **옥좌 앞에서**(국왕 첫 대면)

### ④ 영주성 NPC 3인 + `영주01~05` 신설

| NPC | 이름표 | 좌표 | 역할 |
|---|---|---|---|
| 영주 발데마르 | `&a[Q] 영주 발데마르` | 365,99,710 (대전) | 2챕터 축. **냉담한 토착 영주** |
| 근위병 라이너 | `&a[Q] 근위병 라이너` | 365,99,774 (성문) | 진입 게이트 |
| 사관 게르하르트 | `&a[Q] 사관 게르하르트` | 367,102,740 (서고) | 기록·6챕터 복선 |

퀘스트 초안 (목표는 다양성 룰에 맞춰 조정 가능):

| ID | 이름 | 목표(안) | 수여 |
|---|---|---|---|
| `영주01` | 닫힌 성문 | `visit\|근위병` | 라이너 |
| `영주02` | 수비대의 저녁 | `fish_fresh\|아무\|C\|6\|0\|70` | 라이너 |
| `영주03` | 서고로 | `visit\|사관` | 게르하르트 |
| `영주04` | 사관의 청 | `fish\|아무\|아무\|1\|55` + `dogam\|늪지대,정상\|14` | 게르하르트 |
| `영주05` | 영주 알현 | `visit\|영주` (+칭호) | **발데마르 (첫 대면)** |

캐릭터: 왕실보다 오래 이 강을 지켜왔다는 자부심. 중앙을 신뢰하지 않아 보고를 미루고 있다.
시험을 통과해도 칭찬하지 않고 *"이 정도면 왕도가 보낸 조사관보다 낫겠군"* 이라며 사막 조사를 맡긴다.
상세: [story.md](story.md) 「2챕터 — 2-B 영주성 라인」

NPC 스킨은 `npc-skin-forge` 스킬 사용 (스폰도시 기존 스킨과 **textureRaw 해시 무중복**).

### ⑤ 상단 해역 어종 신설 — 5챕터의 선행 조건

`상단마을` 어장이 **E등급 9종뿐**이다. `배상단07`(A등급)·`배상단08`(S등급)을 그 무대에서 채울 수 없다.
`fish.json`에 **E~S 20~30종 신설**. 참고: 2챕터 무대 89종 / 3챕터 61종.

### ⑥ 사이드 결함 수리

클리어 불가 4건 — 존재하지 않는 지역 참조:

| 퀘스트 | 목표 | 문제 |
|---|---|---|
| `본사이드_세르간03` | `area\|물보라동굴` | 지역 없음 |
| `사사이드_사피르02` | `area\|붉은호수` | 지역 없음 |
| `상사이드_알도01` | `dogam\|상단마을,대양\|16` | `대양` 없음 |
| `상사이드_파비오01` | `area\|대양` | `대양` 없음 |

그 외:
- `본섬_채집01`(Lv3) — 선행·다음·NPC 전부 없음. **획득 경로 없음**
- `튜토_선원` — 체인 미연결 고립
- `튜토_작살2` 목표 `fish|아무||1|0` — 등급 빈 문자열이라 **작살 안 써도 깨짐** → `harpoon|아무|아무|1|0`으로 교체
- `낚시꾼할아버지→메인01`, `동굴탐험가→메인02` — 없는 퀘스트 참조(flatroom 테스트 NPC)
- 사이드 3건이 `earn`을 씀(`본사이드_도란01`·`상사이드_마르코01`·`왕사이드_상인02`) — **주간 카운터**라 리셋에 진행도 날아감 → `sell`/`money`로 교체

### ⑦ 목표 다양화 — 비낚시 verb 활용

메인 149개 목표 중 `fish`가 대부분이다. 신설된 5종 + 기존 미사용 축을 섞을 것.

### ⑧ 장비 경험치 스탯 ×0.5

C등급 풀세팅 +154%, A등급 +206%가 곡선 조정을 무력화한다. 최적화/초보 격차가 아직 **5배**(목표 2.5배).
★`parts.json`은 손편집 금지 — **fish-tools 생성기를 고쳐서 재생성**할 것.

### ⑨ 문서 갱신

[story.md](story.md)·[quest-rework.md](quest-rework.md) 체크리스트와 감사 리포트를 실제 반영 상태로.
`gen_docs_index.py` 재실행.

---

## 4. ★수락 메커니즘 — 반드시 알아야 함

- `다음퀘스트`는 다음 퀘스트 상태를 **`대기`(해금)로만** 바꾼다. 수락이 아니다.
- `대기`→`진행` 경로는 **단 하나** — `QuestGui.clickNpc` → `QuestManager.accept()`.
  즉 **그 퀘스트를 `quests` 배열에 가진 NPC와 대화**해야 한다.
- `/퀘스트` 저널엔 수락 버튼이 없다. `VillageQuestGui`·`QuestCatalogGui`는 읽기 전용,
  `QuestCommand`에도 수락 서브커맨드가 없다.

**결론: `npc.json`의 어느 NPC에도 배정되지 않은 메인 퀘스트는 영구히 수락 불가다.**
실제로 `튜토11`→`본섬01`에서 체인이 죽어 있었고, prod 플레이어 1명이 `본섬01 = 대기`로 남아 있었다.

한 NPC가 여러 메인을 들 때 순서는 `선행퀘스트`가 보장한다(GUI가 선행 미완을 건너뜀).
`quests` 배열은 **메인을 사이드보다 앞에** 둘 것 — GUI가 첫 유효 항목 1개만 노출한다.

완료 방식:
- 타입 `물고기`류 → 목표 달성 후 **수여 NPC에게 반납**(`완료대기` → 보상). 자동 네비 있음
- 타입 `지역` → 도착 즉시 자동 완료(반납 없음)

---

## 5. 목표 문법 팔레트 (28종)

**★목록에 없는 verb를 쓰면 `target = 0`이 되어 퀘스트가 즉시 완료된다.**

| verb | 문법 | 목표치 위치 |
|---|---|---|
| `fish` | `fish\|어종\|등급\|수\|최소크기` | 4번째 |
| `fish_fresh` | `fish_fresh\|어종\|등급\|수\|최소크기\|최소신선도` | 4번째 |
| `harpoon` ★ | `harpoon\|어종\|등급\|수\|최소크기` | 4번째 · **작살 전용** |
| `mine` ★ | `mine\|블록id\|수` (`iron_ore` 형태) | 3번째 |
| `harvest` ★ | `harvest\|작물id\|수` | 3번째 |
| `sail` ★ | `sail\|블록거리` | 2번째 |
| `submit` ★ | `submit\|횟수` | 2번째 |
| `area` `visit` `action` | `<verb>\|대상` (visit은 +표시명) | 항상 1 |
| `dogam` | `dogam\|지역,지역\|종수` | 3번째 |
| `craft` `material` `eatdish` `forage` `equip` | `<verb>\|대상\|수` | 3번째 |
| `deliver` | `deliver\|부품명\|수` (인벤 즉석 판정, **무손상만**, 완료 시 회수) | 3번째 |
| `sell` `money` `earn` `login` `quest_daily` `trap` `enhance` `skill` `collectible` `level` | `<verb>\|수` | 2번째 |
| `casino` | `casino\|키\|수` | 3번째 |

**등급 표기**: `B` = "B 이상"(현재 모든 퀘스트가 이 형태) · `C~B` = **정확히 C~B 구간**(A 이상 카운트 안 됨) · `아무` = 전부.
구간 상한을 걸면 "고등급으로 뭉개기"를 막을 수 있다 — 초중반에 유용, 아직 미활용.

---

## 6. 검증 스니펫 — 작업 단계마다 실행

```python
import json
Q = json.load(open('quests.json'))['퀘스트']
N = json.load(open('npc.json'))['npcs']
OK = {'fish','fish_fresh','harpoon','area','visit','action','dogam','sell','money','earn','login',
      'quest_daily','craft','deliver','material','forage','eatdish','equip','trap','enhance',
      'skill','collectible','casino','level','mine','harvest','sail','submit'}
assigned = {q for v in N.values() for q in (v.get('quests') or [])}

print('불량 verb :', [(k,g) for k,v in Q.items() for g in v.get('목표',[]) if g.split('|')[0] not in OK])
print('메인 미배정:', [k for k,v in Q.items() if v.get('카테고리')=='메인' and k not in assigned])
print('체인 끊김 :', [k for k,v in Q.items()
                   if v.get('다음퀘스트') not in (None,'-','') and v['다음퀘스트'] not in Q])
print('선행 끊김 :', [k for k,v in Q.items()
                   if v.get('선행퀘스트') and v['선행퀘스트'] not in Q])
print('퀘스트 수 :', len(Q), '(252여야 함 — 삭제 금지)')
```
**전부 빈 리스트여야 한다.** npc.json을 고쳤으면 NPC 154명 · dialogue 108세트도 유지 확인.

---

## 7. 함정

- `earn` `login` `quest_daily`는 **주간 카운터**를 읽는다. 메인에 쓰면 주간 리셋에 진행도가 날아간다.
- `deliver`는 완료 시 아이템을 **회수**하고 **무손상 부품만** 인정한다.
- `fish`는 낚싯대·작살 둘 다 집계. 작살 전용은 `harpoon`.
- `mine`은 연쇄 채굴로 같이 부수는 옆 광석을 세지 않는다. `harvest`는 풍년 수량 배수를 세지 않는다.
- **지역을 참조하는 목표는 `regions.json`에 좌표가 실제로 있는지 먼저 확인**할 것
  (`대양`·`물보라동굴`·`깊은호수`·`붉은호수`는 미등록. `원양어선`은 어종만 있고 시스템 미구현).
- `상단마을` 어장이 E등급 9종뿐이다 — ⑤가 5챕터 목표의 선행 조건.
- 퀘스트 개수 252를 유지할 것. 신설(`영주01~05`)은 +5 → 257.

---

## 8. 지금 수치 (참고)

| 항목 | 값 |
|---|---|
| Lv50 요구 누적 XP | 243,423 |
| 1회성 퀘스트 총합 | 115,305 (**47%**) |
| 일일 하루 상한 | 쉬움 210 / 보통 375 / 어려움 750 / **전문 1,500** |
| 주간 | **XP 0** |
| 낚시 XP/포획 (중반빌드) | 4.10 × 1.89 ≈ 7.7 → 1,705 XP/h |
| Lv50 도달 | 1회성 전부 + 일일 전문 85일 / 낚시만 75h / 병행 40일 |

미해결(유저 판단 대기): `applySizeBonus` 순차곱 버그 · 캐시샵 경험치 2배 버프 ·
일일 어려움(Lv22)~전문(Lv35) 사이 티어 공백 · 낚시 우회(75h)가 일일보다 편한 문제


---

## 잔여 (2026-08-07 기준)

### 완료 확인된 것

| # | 항목 | 결과 |
|---|---|---|
| ① | 필요레벨 재분배 | 2챕터 1~15 · 3챕터 15~28 · 4챕터 28~32 · 5챕터 32~40 · 6챕터 40~50 |
| ② | NPC 미배정 40개 | **0개** — 메인 68개 전부 배정 |
| ③ | 체인 재배선 + 왕도 명분·이름 | 4곳 재배선 + `왕도02` 성문의 검문 · `왕도04` 근위대의 시험 · `왕도05` 옥좌 앞에서 |
| ④ | 영주성 NPC 3인 + `영주01~05` | Citizens 스폰 + npc.json + dialogue + 스킨까지 |
| ⑤ | 상단 해역 어종 | 9종 → **34종** (E11 D5 C5 B4 A3 S3) |
| ⑥ | 사이드 결함 | 클리어 불가 4건 + `튜토_작살2` → `harpoon` |
| ⑦ | 목표 다양화 | 메인 `fish` 비중 44% → **34%**, verb 15종 사용 |
| ⑧ | 장비 경험치 스탯 ×0.5 | C 154→**77%** · A 206→**103%** (격차 5배 → 2.5배) |

### 추가로 수리한 것 (Claude, 2026-08-07)

- **`상단기함` 어종 상속** — 어종 정의도 부모도 없어 배 위에서 어종 풀이 비어 있었다.
  `parentIsland = "상단마을"` 설정으로 34종 상속. 배상단 목표가 그 무대에서 충족된다.

### 남은 작업

| 항목 | 비고 |
|---|---|
| 하겐을 길드 회관 안으로 이동 + `&a[Q] 길드장 하겐` | 권위 계단 연출 |
| 지역 `영주성` 폴리곤 신설 | BGM·사이드바 표기용 (기능상 필수 아님) |
| `폭포_뒤_동굴_1층` `requiredLevel` 0 → 30 | |
| 폭포 뒤 동굴 3~6층 지역 생성 (50·60·70·80) + 어종 | 7챕터 강하 통로 겸용 |
| 6층 이무기 배치 + 「꽃」 정의 + 사이드 라인 | |
| `왕도10` 금서고 잠긴문 + 「금서고 열쇠」 | `door/LockedDoorManager` `보상열쇠` |
| 고립 퀘스트 — `본섬_채집01` · `튜토_선원` | 획득 경로 없음 |
| `낚시꾼할아버지→메인01`, `동굴탐험가→메인02` | 없는 퀘스트 참조 (flatroom 테스트 NPC) |
| 사이드 3건 `earn` → `sell`/`money` | 주간 카운터라 리셋에 진행도 날아감 |
| **7챕터 심해** (Lv50~70) | 설계 확정([story.md](story.md) 「7챕터」), 무대·시스템 미구현 |

### 밸런스 미결 (유저 판단 대기)

- `applySizeBonus` 순차곱 버그 — 경험치와 동일 패턴. 크기 → 판매가·도감 기록에 파급
- 캐시샵 **경험치 2배 버프** — 모든 곡선 조정을 반토막낸다
- 일일 **어려움(Lv22)~전문(Lv35) 사이 13레벨** 공백 — 그 구간 750/일뿐
- 낚시 우회 — 남은 XP를 낚시 75h로 메울 수 있어 일일보다 편할 수 있다
- `price_ladder.py` MC에 **장비 경험치 보너스 없음** → XP/h 과소 산출. `metrics.md`에 명기 필요
