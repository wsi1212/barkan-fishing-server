# 명령어 전수조사 (2026-07-25)

BlockShipPlugin.java의 `cmdMap.register` 189건 + `extends Command` 37개 클래스를 7개 구간으로 나눠 서브에이전트가 각각 grep(다른 코드/GUI/NPC json 참조 여부) + git log(도입 배경) + CLAUDE.md/design.md/balance.md/docs-index.md 대조로 분류.

## 왜 이렇게 쌓였는가 (근본 원인)

거의 모든 데드 커맨드가 **같은 패턴**: Skript(`.sk`)에서 Java로 시스템을 이관할 때 "Skript가 콘솔로 이 명령을 호출한다"는 브릿지 커맨드를 만들어뒀는데, 정작 그 `.sk` 파일 자체는 이미 전량 삭제됨(`scripts/` 폴더에 `.sk` 파일 0개 확인). 즉 다리는 놓았는데 다리 건너편이 없어진 채 방치된 것. 나머지 소수는 "강화=축복 시스템 폐지"(2026-06-13), "/도핑상점 폐지"처럼 CLAUDE.md에 이미 기록된 폐지 이벤트의 마이그레이션 잔재.

## ✅ 2026-07-25 삭제 완료 (blockship-plugin 커밋 `a12d2c4`)

아래 32개 명령 + `.orig` 파일 + 클래스 4개(`PlayerDataBridgeCommand`/`RegionQueryCommand`/`RegionResyncCommand`/`FishingShadowCommand`)를 실제로 제거하고 빌드 검증 완료. `fix`도 이 32개에 포함돼 함께 삭제됨(권한 미설정 버그였던 항목이라 삭제로 문제도 같이 해소).

## 제거 후보였던 목록 (SUSPECT_DEAD, 32개 명령 + 파일 1개, 기록용)

다른 코드/문서에서 참조 0건, 목적이 이미 소멸된 것으로 두 번 이상 교차검증됨.

### 칭호 콘솔 브릿지 (5) — TitleLogic이 이제 직접 메서드 호출
`titleupdate` `titlecombo` `titleremove` `titleanim` `titlestop`

### 지역/낚시 이관 브릿지 (5) — 대상 .sk 삭제됨
`regionquery` `regionresync`(→ `/데이터리로드`로 완전 대체) `fishshadow`

### 플레이어데이터 백업 브릿지 (2) — Skript 시절 백업 트리거, 이미 자체 no-op 선언
`pdbackfilone` `pdbackup`(PlayerDataBridgeCommand)

### 칭호 부여/장착 브릿지 (2) — QuestManager/TitleGuiManager가 직접호출로 전환
`titlegrantmsg` `titleequip`

### 개발 테스트/디버그 잔재 (2)
`itemcodectest`(ItemCodec 1회성 검증) `durcacheset`(`/durread`.sk 삭제로 고아)

### 경제 콘솔 브릿지 (2) — PartShopGui가 MoneyBridge 직접호출로 전환
`moneyop` `moneytake`

### 사이드바 브릿지 (1)
`sidebarchatmode`(GuildChatGui가 `sidebar.setChatMode()` 직접호출로 대체)

### 도감 메뉴 브릿지 (1)
`maindex`(MenuManager가 `performCommand("도감")` 직접호출로 전환)

### 조합대 폴백 (1)
`craftopen`(NpcInteractListener에서 항상 도달 불가능한 죽은 분기)

### 강화(축복 폐지) 잔재 (2)
`enhancerefresh` `enhmigrate`(2026-06-13 축복 폐지+RID 통일 마이그레이션 완료 후 잔존)

### 도핑(요리 전환) 잔재 (1)
`dopingrefresh`

### 포탈/물텔포 임포트 잔재 (2)
`portalimport` `watertpimport`(좌표 1회성 이관용, 이관 완료됨)

### 도감/스킬/낚시레벨 브릿지 (4)
`dexdiscover` `skillexp` `fishexp` `fishbarsync`

### 페리 임포트 잔재 (2)
`ferryimportloc` `ferryimportval`(ferries.json 이미 존재, 이후 수정은 `/페리설정`으로만)

### 플레이어데이터 복원 브릿지 (1)
`pdrestoretrigger`

### 방치된 백업 소스 파일 (1, 컴파일 안 됨)
`src/main/java/com/blockship/region/RegionCommand.java.orig` — `.orig` 확장자라 빌드에 안 잡히지만 저장소에 방치된 구버전 diff. 삭제 대상.

## 중복 의심 (SUSPECT_DUPLICATE)

- **`프리미엄상점`** — `캐시상점`+`추천상점`을 인자로 분기하는 순수 래퍼. CLAUDE.md/design.md/balance.md/docs-index.md 어디에도 안 나옴, HelpManager에만 등록. 커밋 `515266b`(경제 이관 #5) 당시 3종이 한꺼번에 이식되며 남은 잔재로 추정.

## 죽은 코드는 아니지만 발견된 버그/주의사항 (1차 조사, 미해결)

- **`페리탑승` 권한 불일치** — HelpManager 안내문은 "일반 플레이어용"인데 실제 코드 권한은 `blockship.admin`. 설계 의도와 코드가 어긋나 있어 일반 유저가 이용 못 하고 있을 가능성.
- **`월드` 라벨 충돌 가능성** — memory(`project_castle_showcase_world`)에 따르면 별도 미니플러그인 `BarkanWorldWarp.jar`도 `/월드 castle_show`를 처리한다고 기록돼 있음. BlockShipPlugin도 `월드`를 동일 라벨로 등록 중 — 두 플러그인이 같은 명령어를 등록하면 나중에 로드되는 쪽이 이길 수 있어 확인 필요(죽은 코드는 아니고 잠재 충돌).
- **`fishback`** — 실사용 트리거(자동 게임플레이 연동)가 전혀 없는 수동 OP 데코 툴. 의도된 미완성 기능인지 판단 불가(UNCLEAR).

## 2차 조사 (2026-07-25) — 중복 기능 재조사

`프리미엄상점`(1차에서 이미 발견) 외에 추가로 확인된 것들:

- **`/스탯` ↔ `/레벨`** — **완전 중복**. 인자 없이 호출 시 둘 다 `skillManager.openHub(p)`만 호출, 동일 GUI. 코드 주석 자체가 "`/스탯`=숙련 허브(=`/레벨`)"라고 인정하고 있음.
- **`/아이템확인` ↔ `/인벤보기`** — `/인벤보기`(InvSeeListener)가 제공하는 기능(36칸+갑옷4+오프핸드 읽기전용 열람)을 `/아이템확인`(ItemViewGui)이 완전히 상위호환(본인열람+OP편집모드+치장까지 포함). `/인벤보기`가 딱히 더 하는 게 없음.
- **`/스폰` ↔ "스폰 도시 스크롤"(500원 아이템)** — `SpawnCommand.java` 클래스 주석이 스스로 "2026-07-16에 `/스폰`이 OP전용→무료 일반명령으로 풀리면서 유료 스크롤(500원)과 중복"이라고 인정. 스크롤이 이제 의미 없는 유료 아이템이 됨.
- **부품상점/통발상점/조합대/강화 — "껍데기 4종"**: 진짜 중복은 아니지만 넷 다 GUI 없이 "마을 NPC 가세요" 메시지만 출력하는 동일 패턴(NPC 중심 전환 이후 등록만 남고 기능은 빠짐). 문제는 아니고 참고사항.
- 오인 소지가 있었지만 **실제로는 다른 기능**으로 재확인된 것: 캐시지급/추천코인지급(다른 통화), 데이터리로드/섬상점리로드(스코프 다름), 도감류 4종(별개 매니저), 능력치/낚시레벨/스탯·레벨(각각 다른 정보).

## 2차 조사 (2026-07-25) — 검증누락/보안 감사 (카지노 참가 사례 계기)

| 명령어 | 문제 | 심각도 |
|---|---|---|
| **`/돈 송금`, `/돈 보내기`** (MoneyCommand) | `/송금`(TransferCommand)과 동일한 P2P 송금 기능이 **레벨15 제한도 수수료10%도 없이** 중복 구현돼 있음 — 아무나 이 경로로 정식 송금 규제를 완전히 우회해 무료·무제한 송금 가능 | **높음** |
| **`/questgui`** | `setPermission` 없음(누구나 실행 가능). 원래는 `/퀘스트`나 NPC 대화가 콘솔로 위임하는 내부 전용 브릿지인데, 플레이어가 직접 `/questgui <다른유저> journal`을 쳐서 **동의·근접 없이 남의 화면에 강제로 GUI를 열 수 있음**. 연타 시 상대가 진행 중인 다른 GUI(예: `/거래` 창)를 강제로 닫아 거래를 취소시키는 등 그리핑/DoS 가능 | **높음** |
| **`/카지노 참가 <id>`** (유저 최초 제보) | 근접 검증이 anchor 기준 15블록 3D 직선거리만 체크하고 벽/층을 무시함 — 벽 뒤나 위층에서도 `/카지노 참가 <id>`만 치면 가장 가까운 빈 좌석에 자동 순간마운트됨. 실제로 걸어가서 앉을 필요가 없음 | 중간 |
| `/cashfx success/brag` | 권한 미설정. 아무나 다른 플레이어 닉네임으로 캐시이펙트(오라 등)를 강제 재생시킬 수 있음(순수 코스메틱 브릿지라 실질 피해는 적음) | 낮음 |
| `/캐시지급`, `/추천코인지급` (admin) | `Long.parseLong`만 하고 음수/하한 미검증. `addCash`는 클램프가 아예 없어(반면 `addRecommendCoins`는 `Num.clampMoney` 적용) 음수 지급 시 잔고가 음수로 떨어질 수 있음 — admin 전용이라 공격표면은 낮으나 실수 방지 차원에서 클램프 추가 권장 | 낮음 |
| `/스크롤지급` (admin) | 수량 인자에 try-catch/음수검증 없음, 숫자 아닌 값 넣으면 미처리 예외 발생 | 낮음 |

나머지 전 구간(NPC/지역/날씨/드릴/채집/섬/거래/통발/작물/짚라인/포탈/잠긴문/페리/레벨/스킬 등)은 소유권·근접·동의 플로우가 정상 검증됨을 확인.

## ✅ 2026-07-25 유저 결정 반영 (커밋 `b1ec7e1`)

- **`/스탯`↔`/레벨` 완전중복** — 유저 확인: **의도된 것, 그대로 유지**. 손대지 않음.
- **`/스폰`↔스폰스크롤 중복** — 유저 확인: **스크롤도 있어야 함, 그대로 유지**. 손대지 않음.
- **`/프리미엄상점` vs `/캐시상점` vs `/추천상점`** — 유저 요청으로 상세 비교만 하고 삭제는 보류(아래 "3종 상점 비교" 섹션 참고).
- **`/돈 송금`·`/돈 보내기` 경제규제 우회 구멍** — 레벨15/수수료10% 게이트 대신 **다른 NPC와 동일한 튜토리얼 완료 게이트** 적용(`QuestManager.checkRoleGate(p,"money")`, 게이트 퀘스트="튜토11"). `TransferCommand`(`/송금`)는 그대로 레벨15+수수료10% 유지, `MoneyCommand`의 송금/보내기만 게이트 추가.
- **`/questgui` 보안 구멍** — 완전 제거. 유일 호출자였던 `NpcDialogueManager`의 "퀘스트목록" NPC대화 액션은 콘솔 dispatch 대신 `questGui.openNpcQuests()` 직접 호출로 리팩터해 기능은 그대로 유지.
- **캐시지급/추천코인지급/스크롤지급** — 유저 확인: admin 전용인 게 맞고 의도된 것. 변경 없음(숫자 검증 미비는 낮은 우선순위로 보류).

## 3종 상점 비교 (`/캐시상점` vs `/추천상점` vs `/프리미엄상점`)

- `/캐시상점` → `CashShopGui.openTab(p,"게임")` — 54칸, 캐시(cash) 통화로 강화·버프·배스킨·낚시이펙트 등 7개 탭.
- `/추천상점` → `CashShopGui.openCoin(p)` — 별도 36칸, 추천코인 통화 전용 상품(게임+특수블록 카테고리 중 코인가격 있는 것만).
- `/프리미엄상점` → 인자 없이 부르면 `/캐시상점`과 **완전히 동일한 화면**(같은 메서드 호출), `추천` 인자를 주면 `/추천상점`과 동일한 화면. 즉 둘을 하나의 명령으로 묶은 상위 래퍼일 뿐 자체 콘텐츠는 없음.

## 카지노(슬롯머신→전체 GUI) 관련 재조사

유저가 "슬롯머신에서 도박 전체 GUI로 가는 버튼이 있었던 것 같다"고 해서 카지노 전체 코드(`CasinoHubGui`, `CasinoManager` 슬롯 렌더링, `CasinoCommand`, `CasinoTableCommand`)를 다시 훑었으나 **그런 기능이 현재 코드에 없음**을 확인. `CasinoCommand.java` 주석 자체가 "2026-07-14 GUI 카지노(로비/카드게임/룰렛 GUI) 폐지 — 게임은 전부 현장 물리 테이블"이라고 명시돼 있어, 설계상 테이블/기계 간 이동은 의도적으로 없음(각 테이블이 독립적). `/카지노테이블 목록`은 있지만 OP 전용 관리 명령이라 플레이어가 보는 GUI 버튼이 아님. 기억하시는 기능이 혹시 이 2026-07-14 개편 이전 것이거나 다른 서버/버전 기억일 수 있음 — 재확인 필요.

## `/cashfx` 설명

낚시 성공/고등급 공지 시 유저가 캐시상점에서 구매한 코스메틱 이펙트를 재생하는 내부 브릿지. `success <player> <등급>`은 매 낚시 성공마다 호출돼 등급 조건(예: 번개=A+, 황금빛=S+) 충족 시 파티클+사운드 재생, `brag <player>`는 A등급 이상 물고기로 전체 공지가 뜰 때만 호출돼 "자랑" 카테고리 이펙트(팡파레/무지개 등)를 재생. 유저가 이펙트 미장착이면 그냥 조기 return. `FishingListener`가 콘솔 dispatch로 호출.

## ✅ 2026-07-25 추가 삭제 (커밋 `3001f34`)

- **`/cashfx`** — 리팩터 후 제거. `FishingListener`가 콘솔 dispatch 대신 `CashEffectManager`를 직접 호출하도록 바꿔서 낚시 캐시이펙트 기능은 그대로 유지, 권한 없는 커맨드 표면만 제거.
- **`/프리미엄상점`** — 완전 삭제(캐시상점+추천상점 순중복 래퍼). `HelpManager` 안내 문구도 같이 제거.
- 카지노 "슬롯머신→전체GUI" 건은 유저가 예전 기억(현재 코드와 무관)이라고 확인 — 조치 없음.

## ✅ 2026-07-25 전체 권한누락 재점검 완료 (조치 불필요, 발견 없음)

154개 남은 명령어 전체를 4구간으로 나눠 "admin/OP여야 하는데 권한 체크가 아예 없는 것" 기준으로 재점검. **`/fix`/`/questgui`/`/cashfx` 외에 동일 패턴 추가 발견 없음.** 서브커맨드 단위 사각지대, 임의 대상 플레이어 지정 명령도 전부 확인했으나 전부 정상.

발견된 낮은 우선순위 위생 이슈(취약점 아님, 참고용):
- `DrillCommand.tabComplete`가 `isOp()` 체크 없이 탭완성 목록을 노출(실행 자체는 막혀있어 정보노출 수준)
- `/watertp`가 plugin.yml에 선언 안 된 커스텀 권한노드 `watertp.admin` 사용 — Bukkit 기본값(미등록 권한=OP 전용)으로 안전하게 동작하긴 하나 plugin.yml에 정식 선언 권장

## ✅ 2026-07-25 유저 최종 확인 + 라벨 충돌 수정 (커밋 `d6811af`)

- **`/카지노 참가`** — 유저 확인: 애초에 일반 유저용이 아님. `CasinoCommand.java` 클래스 주석도 "물리 테이블 진입점(**버튼 백업·봇 경로**)"라고 명시하고 있어 코드로도 재확인됨 — 실제 플레이어는 물리 버튼(`TableInputService`/`SlotMachineService`의 `onClick`)으로 입장하고, 이 명령은 백업/자동화(dev 테스트봇) 전용으로 의도된 설계. **조치 없음, 종결.**
- **`페리탑승`** — 유저 확인: 탑승 등록은 배 출발 시 대기열(`waiting`) 전원이 함께 이동하는 구조라 admin 게이트가 맞다고 확인. **조치 없음, 종결.**
- **`월드` 라벨 충돌 — 실제로 존재함을 코드로 확인, 수정 완료**: `BarkanWorldWarp.jar`(plugin.yml `commands: 월드: {aliases: [성전시]}`)가 `/월드`를 선점 등록하고, `BlockShipPlugin`도 별개 기능(OP용 월드목록+임의월드 텔레포트 디버그툴)으로 동일 라벨 `/월드`를 동적 등록하고 있었음 — Bukkit 특성상 나중에 enable되는 쪽은 bare `/월드`를 뺏기고 `blockship:월드`같은 접두사로만 남아 사실상 안 쓰임. **`BlockShipPlugin` 쪽을 `/월드이동`으로 개명해서 충돌 회피**(BarkanWorldWarp.jar는 소스가 없어 직접 수정 불가라 우리 쪽을 옮김). `/성전시`(BarkanWorldWarp 별칭)는 원래도 충돌과 무관하게 항상 정상 작동했음.
- **`fishback`** — 여전히 UNCLEAR, 미해결(우선순위 낮음, 안 건드림).

## ✅ 2026-07-25 OP 명령어 탭완성(명령어 목록) 노출 전수조사+수정 (커밋 `7bf68ba`)

유저 지적: 실행 권한이 있어도(`isOp()` 인라인 체크) **`setPermission(...)` 호출이 없으면 Bukkit이 명령어 이름 자체를 비OP 플레이어 탭완성 목록에도 그냥 보여준다** — `/드` 쳤을 때 `/드릴`이 후보로 뜨는 문제. 실행 자체는 막혀도 "이런 명령이 존재한다"는 정보 자체가 새는 것.

전체 ~154개 명령어(인라인 154개 + 별도 클래스 30개) 재점검 결과 **5개**가 "전체 기능이 완전히 admin 전용인데 `setPermission` 누락" 패턴으로 확인돼 전부 수정:

| 명령어 | 파일 | 조치 |
|---|---|---|
| `/드릴` | DrillCommand.java | `setPermission("blockship.admin")` 추가 + OP전용 규칙 위반이던 영타 별칭 `emflf` 제거 |
| `/초음파탐지기` | DetectorCommand.java | `setPermission("blockship.admin")` 추가 |
| `/카지노테이블` | CasinoTableCommand.java | `setPermission("blockship.admin")` 추가 |
| `/말대여` | BlockShipPlugin.java(인라인) | `setPermission("blockship.admin")` 추가 + 영타 별칭 `akfeodu` 제거 |
| `/섬상점리로드` | BlockShipPlugin.java(인라인) | `setPermission("blockship.admin")` 추가 |

**제외(정상, 손 안 댐)**: `/금지템`·`/스탯`·`/돈`·`/잠수`·`/카지노`·`/작물`·`/낚시대회`·`/조합대`·`/마켓`·`/마켓등록`·`/아이템확인`·`/팁` — 이들은 서브커맨드 일부만 admin이고 전체 명령 자체는 일반 유저에게도 정당한 기능이 있어서, 전체를 탭목록에서 숨기면 정상 기능까지 숨겨지므로 대상 제외.

기존 `execute()` 내부 `isOp()` 체크는 전부 그대로 유지(이중 방어). 빌드 성공.
