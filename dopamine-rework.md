# 도파민 리워크 (Dopamine / Reward-Feel Rework)

> 2026-07-08 설계. "할 건 많은데 짜릿한 순간이 없다"는 체감 문제를 **연출·리듬·기대감** 관점에서 개선한다.
> 콘텐츠(시스템)를 더 얹는 게 아니라, **이미 있는 순간을 감각적으로 폭발시키는 것**이 이 문서의 목표.

## 1. 진단 — 왜 도파민이 약한가

도파민의 핵심은 **보상 자체가 아니라 "예측 오차"** (예상보다 좋은 일이 터질 때 분비). 낚시 코어 루프
(캐스팅 → 등급 → 판매)는 구조적으로 세 군데가 샌다:

1. **획득의 "순간"이 이벤트가 아니다** — 매 캐스팅이 거의 똑같이 생겼고, S~G를 낚아도 채팅 한 줄 + 경험치
   오브 소리뿐. 잭팟이 잭팟처럼 안 터진다.
2. **보상이 대체 가능한 돈** — "득템"의 짜릿함이 약함. 가끔 터지는 windfall(보물·대박)이 없음.
3. **성장이 517시간짜리 완만한 XP 언덕** — 레벨 사이 구간이 도파민 사막.

우리 서버는 **시스템 수는 이미 충분**하다(도감·강화·요리·길드·섬·랭킹…). 문제는 양이 아니라 **순간의 연출과
리듬**이다.

## 2. 표준 RPG 도파민 도구

| 메커니즘 | 원리 | 우리 적용 지점 |
|---|---|---|
| 변동비율 보상(가챠/드랍) | 언제 터질지 모름 = 슬롯머신 | 등급 롤·크리·더블/트리플 (이미 있음, **연출 부재**) |
| 연출/저즈(juice) | 희귀 이벤트에 화면·사운드·빛 폭발 | **#1 이번 작업** |
| 개봉 의식 | 천천히 열리는 상자 = 기대 지연 | #3 보물상자 |
| 숫자 상승감 | 끊임없는 미세 증가 | #2 레벨 사이 미세보상 |
| 항상 보이는 다음 목표 | 손 닿을 듯한 당근 | #2 |
| 연속/콤보 | 모멘텀 + 끊길까 두려움 | #4 콤보 실이득화 (콤보 시스템은 이미 있음) |
| 천장(pity) | N번 실패 시 확정 = 좌절 이탈 방지 | #5 (grade pity 카운터는 코드에 일부 존재) |
| 과시/랭킹 | 남이 보는 희귀 = 아스피레이션 | #1 상위 등급 서버 공지 강화 |

## 3. 로드맵 (우선순위)

| # | 항목 | 상태 |
|---|------|------|
| **1** | **등급별 계단식 획득 연출** (사운드/파티클/타이틀/불꽃 + 크리·더블 스택) | **← 이번 작업** |
| 2 | 레벨 진행도 표시 + 레벨 사이 미세보상 + "다음 해금까지 N" 상시 노출 | 부분 적용: 낚시 성공 경험치 메시지에 현재 Lv/EXP 진행도 표시 |
| 3 | 잭팟형 랜덤 대박 (젖은 보물상자 + 개봉 의식) | 구현: 낚시 성공 시 낮은 확률 드롭, 우클릭 개봉 |
| 4 | 콤보 실이득화 (경험치·더블·트리플 확률) + 일일 첫 낚시 훅 | 부분 구현: 5콤보마다 +1%, 최대 +20% |
| 5 | 피티/확률 예고 UI | 보류: 낚시는 반복 빈도가 높아 피로도 우려. 확률 표시는 숨기고 결과/연출 중심 유지 |
| 6 | 물속 실루엣 + 화면 앞 획득 애니메이션 | 구현: 미니게임 중 실루엣, B등급 이상 획득 전 개인 전용 reveal |
| 7 | 전 숙련 연결형 특성 트리 + 활동별 발동/잭팟 | 기획: [숙련 특성 트리](skill-tree-dopamine-design.md), 1차 채굴 파일럿 권장 |

---

## 4. #1 등급별 계단식 획득 연출 (구현됨)

### 4.1 설계 원칙
- **확률 = 연출 강도.** 흔한 등급은 조용히, 희귀할수록 감각을 폭발시킨다. (기존 확률/밸런스는 **손대지 않음** —
  연출만 얹는다.)
- **한 순간에 하나의 절정.** 등급 티어 연출 위에 크리티컬·더블/트리플이 **스택**되어, 겹칠수록 화면이 부서진다.
- **자동판매 분기에서도 동일하게 발동** (인벤 가득이어도 대박은 대박).
- **볼드(§l) 금지** (전역 정책). 색코드·타이틀만 사용.

### 4.2 등급 → 연출 계단

| 등급 | rank | 확률 | 연출 |
|------|:---:|:---:|------|
| E·D | 1·2 | ~64% | 추가 연출 없음 (기존 경험치 오브 사운드만) |
| C | 3 | ~11% | 가벼운 벨 차임 + `HAPPY_VILLAGER` 반짝 |
| **B** | 4 | ~6.3% | 자수정 차임 + 상승 벨 + 작은 `END_ROD` 링 + `ENCHANTED_HIT` + **짧은 타이틀** `◆ B등급 ◆` |
| **A** | 5 | ~3.1% | 비콘 선택음 + 자수정 차임 + 도전완료 팡파레(저볼륨) + `END_ROD` 링 + 토템 + **타이틀** `◆ A등급 ◆` |
| **S** | 6 | ~2.2% | 비콘 활성 + 도전완료 팡파레 + `END_ROD` 링 + 토템 + **타이틀** `✦ S등급 ✦` + 서버공지(기존) |
| **M** | 7 | ~0.5% | 습격 뿔피리 + 비콘 + 토템 대량 + **타이틀** `✦ M등급 · 에픽 ✦` + 서버공지 |
| **L** | 8 | ~0.15% | 엔더드래곤 그르렁 + 토템사용 + **실제 불꽃 1발** + 드래곤브레스 + **타이틀** `✦ L등급 · 전설 ✦` + **강화 서버공지(테두리)** |
| **G** | 9 | ~0.015% | 드래곤 죽음 사운드 + **불꽃 3연발** + 드래곤브레스 대량 + **타이틀** `『 신화 』` + **최고 강화 서버공지 + 전 서버 소프트 종소리** |

### 4.3 스택 모디파이어 (등급 무관, 위에 얹음)
- **더블/트리플**: 자수정 차임 "카칭" 연타 (더블=2연타, 트리플=3연타, 피치 상승). 기존 `★`/`★★★` 채팅 메시지와 함께.
- **크리티컬**: `CRIT` 파티클 버스트 (기존 크리 경험치 사운드=`LEVELUP`는 그대로 유지).

### 4.4 색상 규약 (`rarityColor`)
기존 `gradeColorSkript`(E§7 D§f C§a B§b A§e S§6) 그대로 확장:
- M `§d` (매지엔타) · L `§5` (진보라) · G `§6` (골드, 타이틀은 골드+노랑 셔머로 S와 구별)

### 4.5 상위 등급 서버 공지 강화
- 기존: rank≥6(S/M/L/G) → `§6★ ...낚았습니다` 한 줄 (차단플래그 `isBlockFishNotice` 존중).
- 변경: **L·G(rank≥8)는 테두리 3줄 팡파레**로 격상, **G는 전 서버에 소프트 종소리**(`BLOCK_BELL_RESONATE`)까지.
  차단플래그·소리 모두 개별 플레이어 기준 존중.
- 물고기 이름 hover: **물고기 이름 / 등급 / 크기 / 낚은사람 / 판매가 / 지역 / 낚은시간**만 표시.
- 공지 문장: 물고기 이름 뒤에 목적격 조사 자동 적용 (`왕연어를 낚았습니다`, `강의제왕을 낚았습니다`).
- hover 판매가: `FishItem.calcPrice(...)`를 재사용해 실제 판매 로직과 동일하게 표시(최종크기 기반 크기점수·판매보너스·신선도 반영).
- 지역 표기: `RegionManager.getCatchRegionName` 사용. 부모 체인에서 **최상위 부모 + 현재 지역**만 표시한다
  (`바르칸 -> 사막 -> 오아시스` = `바르칸_오아시스`, `바르칸 -> 정상` = `바르칸_정상`).

### 4.6 구현 위치
- 파일: `fishing/FishingListener.java`
- **호출 1곳**: `onMinigameDone(...)` 보상 계산 직후, 결과창/자동판매 분기 진입 전 →
  `playCatchFx(c.p, c.grade, isCrit, extra, c.fish, finalSize)`.
  - 2026-07-08 체감 튜닝: 결과창을 연 뒤 호출하면 A 액션바/파티클이 GUI에 묻혀 "별 느낌 없음". 호출 위치를 앞으로 당김.
- **신설 메서드**: `playCatchFx`(계단 스위치) + 헬퍼 `ring`(파티클 원) · `spawnFirework` · `showGradeTitle` · `rarityColor`.
- **서버 공지 블록**(line 436~443) L/G 팡파레로 확장.
- 스케줄러(불꽃 3연발·카칭 연타)는 `JavaPlugin.getProvidingPlugin(FishingListener.class)`로 취득 (생성자 무변경).

### 4.7 변경 파일
| 파일 | 변경 |
|------|------|
| `fishing/FishingListener.java` | `playCatchFx` 외 헬퍼 신설 + 호출 1곳 + A+공지 블록 확장 |

### 4.8 검증 / 배포
- `/낚시테스트 <등급>`으로 각 등급 1회 강제 catch → 연출 육안 확인 (`setForceGrade` 이미 존재).
- 빌드: `cd /Users/user/development/blockship-plugin && ./gradlew build` → **서버 풀 재시작 필수**(jar lazy-load 규약).
- dev 먼저, prod는 명시 요청 시.

---

## 5. #2 레벨 진행도 표시 (부분 적용)

### 5.1 이번 범위
- 레벨 시스템을 스탯 포인트 기반으로 바꿀 가능성이 있어, 보상/해금 예고는 아직 넣지 않는다.
- 낚시 성공 후 경험치 메시지에 **현재 낚시 레벨 + 현재/필요 경험치 + 진행률**만 표시한다.

예시:
`낚시 경험치 +42exp (+35%) [Lv.12 1,230/2,000exp]`

### 5.2 구현 위치
- `FishingLevelManager.addExp(...)`가 최종 `ExpProgress(level, currentExp, requiredExp)`를 반환한다.
- `FishingListener`는 `levels.addExp(...)` 이후 반환값으로 경험치 메시지 suffix를 만든다.
- 경험치 획득량 옆에는 기준 경험치 대비 최종 상승률을 표시한다. 기준 경험치는 `등급 기본 경험치 + 크기점수`까지만 반영하고,
  장비 경험치/환경/크리티컬/2배버프/콤보는 상승률에 포함한다.
- 레벨 진행도는 가독성을 위해 정수 경험치만 표시하고 진행률 소수 표기는 제거한다.

### 5.3 남겨둔 것
- 다음 보상 레벨 안내
- 다음 해금까지 N레벨 안내
- 레벨 사이 25/50/75% 미세보상

---

## 6. #3 젖은 보물상자 (구현됨)

### 6.1 의도
- 평범한 낚시 루프 중 낮은 확률로 **상자 아이템**이 튀어나오게 해서, 물고기/돈만 반복되는 감각을 깨뜨린다.
- 상자는 바로 보상이 아니라 우클릭 개봉 아이템이다. `상자를 여는 중...` 타이틀, 상자 소리, 물 파티클 후 보상을 공개한다.

### 6.2 드롭
- 낚시 성공 1회당 기본 `1.2%`.
- 장비/강화/환경의 `행운` 스탯으로 보정한다: `최종확률 = 1.2% × (100 + 행운) / 100`, 최대 `3.0%`.
  - 예: 행운 `+50`이면 `1.2% → 1.8%`.
- 결과창이 열리는 경우 물고기/재료와 같은 `낚시` 결과창에 표시한다.
- 결과창이 꽉 차거나 직접 지급 분기면 인벤토리로 지급하고, 인벤토리도 꽉 차면 바닥에 떨어진다.

### 6.3 보상풀
| 보상 | 확률 | 내용 |
|---|---:|---|
| 지역 낚시 재료 대량 | 45% | 현재 지역 또는 부모 지역의 재료 드롭테이블에서 1종, `3~6개` |
| 돈 | 20% | 해당 낚시 풀의 평균 수익 기준으로 산정한 지역별 금액 |
| 별빛진주 | 12% | `1~2개` |
| 요리재료 | 10% | 특수 작물 수확물 `2~5개` (수박은 `1~2개`) |
| 미끼 | 8% | 플레이어 레벨로 사용 가능한 미끼 `3~8개` |
| 레시피 | 5% | 미해금 잠금 레시피. 히든 낚싯대 레시피를 우선, 없으면 낚시/통발 잠금 레시피 |

### 6.4 제외
- “강화조각”은 별도 실체가 없는 일반 RPG식 표현이었으므로 보상풀에서 제외한다.
- 현재 서버에서 강화/조합에 실제로 쓰이는 공통 희귀 재료는 `별빛진주`다.

### 6.5 구현 위치
- `fishing/WetTreasureChestManager.java`
- `FishingListener`에서 보상 계산 후 `treasure.rollChest(...)` 호출
- `EquipmentManager.grantBait(...)` 추가: 돈 차감/구매 메시지 없이 보상용 미끼 지급

---

## 7. #4 콤보 실이득화 (부분 구현)

### 7.1 이번 범위
- 콤보가 단순 표시/업적 카운터가 아니라, 실제 낚시 보상에 작은 모멘텀 보너스를 준다.
- 판매가와 젖은 보물상자 확률은 건드리지 않는다. 경제 인플레와 잭팟 확률 과증폭을 피하기 위함.

### 7.2 공식
- 현재 저장된 콤보 기준 `5콤보마다 +1%`, 최대 `+20%`.
- 적용 대상:
  - 낚시 경험치: 최종 경험치에 `+N%`
  - 더블찬스: `+N%p`
  - 트리플찬스: `+N%p`
- 이번 성공으로 올라갈 콤보가 아니라, **이번 보상 계산 전 이미 쌓여 있던 콤보**로 계산한다.
  - 예: `4콤보 -> 5콤보`가 되는 낚시는 보너스 `0%`.
  - 5콤보 달성 후 다음 낚시부터 `+1%`.

### 7.3 구현 위치
- `RewardMath.comboBonusPct(combo)`
- `FishingListener.onMinigameDone(...)`
  - 보상 계산 전 `PlayerData.combo`를 읽어 콤보 보너스 산정
  - 경험치, 더블찬스, 트리플찬스에만 적용
  - 콤보 증가는 기존처럼 보상 처리 후 수행

---

## 8. #6 물속 실루엣 + 획득 애니메이션 (구현됨)

### 8.1 방향
- 피티/확률/스택 UI는 기본 노출하지 않는다. 낚시는 반복 행동이라 숫자 예고가 피로도를 만들 수 있다.
- 대신 물고기가 문 순간부터 월드 안에서 작은 변화를 보여준다.

### 8.2 구현
- 미니게임 중 찌가 있던 물속 위치에 플레이어 개인 전용 `ItemDisplay` 실루엣을 띄운다.
  - 다른 플레이어에게는 `hideEntity`로 숨김.
  - 짧은 물방울 파티클만 개인에게 표시.
  - 성공/실패/타임아웃 시 자동 제거.
- B등급 이상 성공 시 결과창을 바로 열지 않고, 잡은 물고기 아이템을 플레이어 화면 앞에 0.7초 정도 띄운 뒤 결과창을 연다.
  - 불사의 토템 사용처럼 아이템이 앞으로 튀어나오는 느낌.
  - E/D/C는 기존처럼 바로 결과창/보상 처리.

### 8.3 구현 위치
- `FishingListener`
  - `startSilhouetteFx(...)`
  - `FishingSilhouetteFx`
  - `playFrontReveal(...)`
  - `finishCatchReward(...)`

---

## 9. #7 전 숙련 특성 트리 (기획됨)

- 낚시, 채굴, 재배, 요리, 탐험, 수집 레벨업마다 숙련 포인트를 지급한다.
- 포인트를 연결형 트리에 투자하며, 전 단계 투자와 계열 누적 투자량으로 다음 노드가 열린다.
- 레벨은 콘텐츠 해금, 특성은 플레이 방식과 발동 사건을 담당한다.
- 활동별 대표 잭팟은 만선, 응축광맥, 황금 수확, 완벽한 한 접시, 숨겨진 보급품, 진품 감정이다.
- 상세 규칙, 초기 수치, GUI와 구현 순서는 [skill-tree-dopamine-design.md](skill-tree-dopamine-design.md)에 정리한다.
