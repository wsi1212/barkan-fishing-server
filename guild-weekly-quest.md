# 주간 길드 퀘스트 — 설계 (2026-08-16)

> 상태: **기획안.** 구현 전 결정이 필요한 갈림길이 아래 「§8 물어볼 것」에 있다.

---

## 1. 지금 있는 것 (소스 실측)

| | |
|---|---|
| 주간 퀘스트 | **16개**, `카테고리: "주간"`. 보상 5,000~45,000원. 레벨 게이트 1/15/30/50 4단 |
| 주간 리셋 | `KoreanTime.sundayWeekKey()`(일요일 기준). **플레이어가 접속할 때** `QuestManager.checkWeekly`가 주차를 비교해 리셋 |
| 진행도 저장 | 전부 `PlayerData` — `weeklyQuests` · `questProgress[qid][goalIdx]` · `questStatus` |
| 길드 데이터 | `treasury`(금고) · `submitTotal/Season` · `members{role, contributed}` · `buffs{id→lv}` · `maxMembers` · 시설 레벨 5종 · 길드섬 |
| 길드 레벨 | `guildLevel(g)` = `guildScore/levelFromScore` → **0~15**. 점수 = Σ(멤버 낚시레벨)×100 + 제출누적 + 멤버수×500 + 금고/10,000 |
| 길드 버프 | 6종 × 3레벨. 가격 **500,000 ~ 20,000,000원** |

### ★찾은 것 — 길드 경제가 굶고 있다

버프 6종을 만렙까지 사면 **총 9,900만원**이다(+ 섬 확장 `100,000 × 1.3^n`, 슬롯 확장).
그런데 **금고로 들어오는 길은 「기부」 하나뿐이다.** 개인이 주간 16개를 다 밀어도 한 주에
약 30만원이라, 5인 길드가 수입 전액을 기부해도 버프 만렙까지 **몇 년**이 걸린다.

반대로 **금고에서 개인 지갑으로 나가는 경로는 없다** — 소비처가 버프 구매·슬롯 확장·섬 확장뿐이다
(`GuildManager:483`, `GuildCommand:813`). 즉 **금고에 돈을 넣는 건 인플레가 아니라 닫힌 루프**다.

⇒ 주간 길드 퀘스트의 1차 존재 이유는 「할 일 추가」가 아니라 **길드의 유일한 집단 수입원**이다.

---

## 2. 핵심 문제 넷

**① 길드 단위 진행도 저장소가 없다.** 모든 퀘스트 진행도는 `PlayerData`에 있다.
길드퀘는 「길드원 전체의 합산」이라 `GuildData`에 주차별 진행도가 따로 필요하다.

**② 리셋 트리거가 없다.** 개인 주간은 「그 사람이 접속할 때」 리셋된다. 길드는 접속이라는 게 없다.
→ **길드원 아무나 접속할 때 길드 주차를 확인**하는 방식이어야 한다.

**③ 무임승차.** 20명 중 1명이 다 하고 20명이 보상을 받으면 길드퀘가 아니라 「부지런한 사람 착취」다.
→ **개인 기여도를 기록하고 배당을 비례로** 줘야 한다.

**④ 목표가 합산 가능해야 한다.** 여러 명이 나눠 미는 물건이라 **가산적(additive) 목표만** 쓸 수 있다.

| 쓸 수 있음 | 쓰면 안 됨 | 이유 |
|---|---|---|
| `fish` `harpoon` `sell` `material` `forage` `mine` `harvest` `craft` `eatdish` `submit` `trap` `enhance` | `visit` `area` `equip` `level` | 한 사람이 하면 끝 — 길드로 나눌 게 없다 |
| | `farmland` `islandvisit` | 상태 목표라 합산 자체가 성립 안 함 |
| | `dogam` `collectible` | 개인 도감/수집이라 합산이 의미를 잃는다 |

---

## 3. 저장소 — `GuildData` 확장

```java
private String   questWeek;                  // "2026-W34" (개인과 같은 sundayWeekKey)
private Map<String,Integer> questProgress;   // "qid|goalIdx" → 진행도
private Map<String,String>  questStatus;     // qid → 진행 / 완료대기 / 완료
private Map<String,Long>    questContrib;    // 멤버UUID → 이번 주 기여 점수
private Map<String,String>  questClaimed;    // 멤버UUID → 수령한 qid 목록(CSV)
```

정의는 **새 데이터 파일 `guild-quests.json`** — `quests.json`을 오염시키지 않는다.
개인 주간 로직(`weeklyQuests`·`isSpecial`·`hasOtherMain`)과 완전히 분리해야
「한 번에 하나」 잠금과 안 얽힌다.

---

## 4. 티어 — 길드 레벨 3단, 주당 3개

인원과 화력이 다른 길드에 같은 숫자를 주면 한쪽은 불가능하고 한쪽은 시시하다.
**이미 있는 `guildLevel`(0~15)로 나눈다** — 멤버 레벨·인원·자산·제출을 이미 종합한 값이라
따로 지표를 만들 이유가 없다.

| 티어 | 길드 Lv | 활성 인원 가정 | 목표 크기 |
|---|---|---|---|
| **소규모** | 0~4 | 2~3 | 개인 주간 × **3** |
| **중견** | 5~9 | 4~6 | 개인 주간 × **6** |
| **대형** | 10~15 | 8~12 | 개인 주간 × **12** |

주당 **3개**. 「조업 / 생산 / 기여」 세 축을 하나씩 — 더 늘리면 아무도 안 본다.

### 4-1. 구체안

개인 주간 실수치(`fish 45` · `C 18` · `B 12` · `A 8` · `sell 30` · `material 30` ·
`craft 8` · `eatdish 7` · `submit 20` · `mine 45`)에 위 배수를 곱했다.

| 티어 | 퀘스트 | 목표 | 금고 |
|---|---|---|---|
| **소규모** | &a함께 그물을 | `fish\|아무\|아무\|150\|0` | 150,000 |
| | &a곳간을 채워라 | `material\|아무\|90` | 150,000 |
| | &a어시장 진출 | `sell\|90` | 150,000 |
| | | **주당 합** | **450,000** |
| **중견** | &b길드의 이름으로 | `fish\|아무\|C\|110\|0` | 500,000 |
| | &b가공 라인 | `craft\|아무\|48` + `eatdish\|아무\|42` | 500,000 |
| | &b제출소 점거 | `submit\|120` | 500,000 |
| | | **주당 합** | **1,500,000** |
| **대형** | &6대양 원정 | `fish\|아무\|A\|96\|0` | 1,500,000 |
| | &6작살 부대 | `harpoon\|아무\|A\|96\|0` | 1,500,000 |
| | &6길드 창고 | `submit\|240` + `mine\|아무\|540` | 1,500,000 |
| | | **주당 합** | **4,500,000** |

대형 길드가 매주 다 밀면 연 2.3억 → **버프 만렙(9,900만)이 반년쯤**. 「달성 가능한 장기 목표」다.
지금은 사실상 불가능하다.

---

## 5. 보상 — 금고 + 기여 배당, 두 갈래

### 금고 (위 표) — 인플레 아님
금고는 **개인 지갑으로 나갈 수 없다.** 소비처가 버프·확장뿐이라 넣은 만큼 전부 싱크로 돌아온다.
그래서 금액을 크게 잡아도 서버 경제에 안 샌다. **길드가 뭉칠 이유**가 여기서 생긴다.

### 개인 배당 — 금고액의 **20%**를 별도 풀로, 기여 비례
```
내 배당 = 배당풀 × (내 기여 점수 / 길드 전체 기여 점수)
```
- 기여 **0이면 배당 0.** 한 번이라도 밀면 지분이 생긴다.
- 대형 기준 배당풀 900,000원을 10명이 나누면 1인 ≒ 90,000 — 개인 주간 전문 티어(4개 15.7만)의 절반.
  **개인 주간을 대체하지 않고 얹히는** 크기다.
- ★이쪽은 진짜 인플레다. 그래서 20%로 묶고, 절반은 **현물**(미끼·요리·특수작물 씨앗·자심권)로 준다.

### 곁다리 후보
- **다음 티어 버프 1주 무료 체험** — 사기 전에 맛보게 해서 구매를 유도한다. 금고 싱크와 맞물린다.
- 칭호는 **안 준다.** 개인 칭호라 집단 성과와 안 맞는다. 굳이 준다면 **길드 엠블럼 프레임** 쪽.

---

## 6. 리셋 · 정산

| 시점 | 동작 |
|---|---|
| 길드원 아무나 접속 | `checkGuildWeekly(g)` — `questWeek`가 다르면 진행도·기여·수령기록 전부 리셋하고 그 주 티어 3개를 배정 |
| 목표 달성 | **금고에 즉시 자동 입금.** 「받기」를 안 거친다 — 금액이 커서 아무도 안 눌러 날리면 억울하다 |
| 개인 배당 | 완료 시점에 확정. `/길드` GUI에서 수령. **오프라인이었으면 `MailboxManager`로 적재**(만료 7일) |
| 주 넘어감 | 미완료 진행도는 버린다(개인 주간과 동일). 미수령 배당은 우편함에 남아 있으므로 안 사라진다 |

## 7. 어뷰징 점검

| 경로 | 막는 법 |
|---|---|
| **길드 갈아타기** (주중에 옮겨 두 번 수령) | 기여는 「그 길드 소속인 동안」만 쌓임 + **가입 후 24시간은 배당 무자격** |
| **유령 1인 길드** 가 소규모 티어 반복 | 티어가 길드 레벨로 갈리므로 1인 길드는 소규모 양만 받고 보상도 그만큼. 문제 없음 |
| **금고 세탁** | 출금 경로 자체가 없다(실측 확인). 구조적으로 불가 |
| **막판 무임승차** (완료 직전 가입) | 배당은 기여 비례라 기여 0 = 0 |

## 8. 물어볼 것 — 여기서 갈린다

1. **티어 기준** — 길드 레벨(추천, 이미 계산돼 있음) vs 활성 멤버 수?
2. **개인 배당** — 넣는다(추천, 무임승차 방지) vs 금고 몰빵(단순)?
3. **주당 개수** — 3개(추천) vs 더?
4. **버프 가격표** — 위 금액은 지금 가격표(최대 2,000만)를 **그대로 두고** 맞춘 값이다.
   가격을 내리는 게 맞다고 보면 그건 별건이라 `balance-audit`으로 따로 봐야 한다.

## 9. 자바 작업 목록

| # | 대상 | 규모 |
|---|---|---|
| 1 | `GuildData` 필드 5개 + getter/setter | 소 |
| 2 | `guild/GuildQuestManager` 신설 — 정의 로드 · 주차 리셋 · 진행 가산 · 기여 기록 · 정산 | **중** |
| 3 | `QuestManager`의 이벤트 훅 26개에서 길드로 **fan-out** (`guildQuests.bump(p, verb, ...)`) | **중** — 훅은 이미 다 있어서 한 줄씩이지만 **빠뜨리면 그 목표만 조용히 0**이 된다 |
| 4 | `guild-quests.json` + 로더 | 소 |
| 5 | `GuildGui` 슬롯 31에 「이번 주 길드 임무」 탭 + 클릭 핸들러 (목표 3개 · 내 기여 / 전체 · 기여 상위 5명 · 배당 받기) | 중 |
| 6 | `/길드 임무` 서브커맨드 (기존 명령에 통합 — 새 명령 등록 불필요) | 소 |

★3번이 이 작업의 진짜 위험 지점이다. 마인팜 때와 같은 실패 모드 —
**데이터는 들어갔는데 훅 하나가 안 걸려서 그 목표만 영원히 0.**
구현 시 마인팜처럼 **연결점 전수 검증 스크립트**를 같이 만들 것.
